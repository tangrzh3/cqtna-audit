# 正文压缩方案：逐段清单，供勾选

**日期**：2026-08-24
**现状**：全文 15,930 词；**主文（Abstract → Discussion 末）10,682 词**
**目标**：主文 7,000–8,000 词（Genome Biology / Cell Genomics 量级）
**须削减**：约 **2,700–3,700 词**

⚠ **本文件不执行任何删改**，只列出候选与代价。每一项都注明**移到哪里**——
本方案里没有"删除"，只有"移到补充材料并在正文留指针"，因此全部可逆。

---

## 0. 先说三条我认为**不应该动**的

| 段 | 词 | 为什么不动 |
|---|---:|---|
| `The nomination is the outcome GWAS…`（引导段）| 998 | **这是全文的中心主张**，恒等式、阈值换算、GWS 分解、条件化背景、MC1R 合并都在这里 |
| Discussion 开头的机制段 | 228 | 把代数必然与经验观察分开的那一段，删了会退回过推 |
| `Part 4 — These checks are rarely reported` | 309 | 唯一支撑"领域级问题"的证据 |

---

## 1. 候选清单（按"省词 ÷ 代价"排序）

### A 级：代价低，建议直接做（合计 **1,676 词**）

| # | 段落 | 词 | 移到 | 代价 |
|---|---|---:|---|---|
| **A1** | `Both axes crossed` | **1,180** | 新 S48 | 六格网格的完整叙述。正文留一张 6 行小表 + 两句判读（约 150 词），**净省 ~1,030** |
| **A2** | Discussion 的 **(i)–(viii) 八项检查**（L820–878） | ~700 | 新 S49 | 八项检查是**交付物**，本就该在补充材料与包文档里。正文留一句"八项检查见 S49，R 实现见 `cqtna`"（约 40 词），**净省 ~660** |

⚠ A2 有一个反对意见值得记下：八项检查是论文"给出可用东西"的证据，移走会削弱建设性观感。
**折中**：正文保留**编号 + 一行标题**（八行，约 120 词），细节移走，净省 ~580。

### B 级：代价中等，取决于你想保留多少"诚实装置"（合计 **1,015 词**）

| # | 段落 | 词 | 移到 | 代价 |
|---|---|---:|---|---|
| **B1** | `The whole-blood rheumatoid arthritis cell is void` | **519** | S39 已有 | RA 作废格的完整论证。⚠ **这是论文主动报告失败的最显眼一处**，移走会让"我们报告 void"这件事只剩一句话 |
| **B2** | `Which disease is nominated as the mismatch changes the verdict` | **498** | S39 已有 | 错配名单选择会改变判定的论证。⚠ 同上，且这一段是**给别人用的设计教训**，不只是自辩 |

**若两段都移**，正文各留 2–3 句（约 120 词），净省 ~900。

### C 级：代价较高，我不建议但列出（合计 **1,229 词**）

| # | 段落 | 词 | 代价 |
|---|---|---:|---|
| **C1** | `Three further exposure resources, and why they settle nothing` | 478 | 三个额外暴露资源为何不解决问题。移走后读者会问"为什么不多加资源"——**而恒等式恰恰回答了这个问题**，可考虑压成 3 句并入引导段 |
| **C2** | `Which unit the significance belongs to` | 452 | 四种推断单位的敏感性。这是审稿人常问的技术点 |
| **C3** | Discussion 的 TPI1 状态表 + 后续段（L884–905）| ~300 | TPI1 已降为示例，此表可移入 S47 |

### D 级：不属于压缩，属于**改写**

| # | 段落 | 词 | 说明 |
|---|---|---:|---|
| **D1** | `Limitations`（Discussion 末） | **692** | 不建议移走，但可**重排**：现在是一长段流水，改成分组小标题（数据 / 估计量 / 诊断 / 可复现性）后可压到约 450 词而不丢内容 |
| **D2** | 摘要 | 368 | 若定 NC（150 词）或 Genome Biology（250 词），需**重写而非删句**。⚠ 会与"精确报告 supportive/void/inconclusive"直接冲突——**这个取舍只有你能定** |

---

## 2. 三个组合方案

| 方案 | 采纳 | 主文降至 | 落点 |
|---|---|---:|---|
| **保守** | A1 + A2(折中) | **~9,070** | 仍偏长，但结构清爽 |
| **推荐** | A1 + A2(折中) + B1 + B2 + D1 | **~7,930** | **落进目标区间**，且未动中心主张 |
| **激进** | 上述 + C1 + C2 + C3 | **~6,700** | 达标有余，但会移走三处审稿人常问的技术点 |

⚠ **三个方案都不动 §0 的三段**，也都不删除任何内容——全部是"移到补充 + 留指针"。

---

## 3. ⚠ 我对"推荐方案"的保留意见

B1 与 B2 合计 1,017 词，是本文**主动报告失败与设计教训**的两处。
移走它们能达标，但会产生一个副作用：

> 正文会读起来更像"我们发现了一个规律"，
> 而不是"我们发现了一个规律并列出了它在哪里失效"。

而本文全部可信度都建立在后者上。**若你倾向保留 B1/B2，
我建议改采 A1 + A2 + C1 + C2 + D1（约 8,000 词），代价是移走两处技术敏感性
而不是两处失败报告。**

**这两种取舍没有正确答案，取决于你希望审稿人先记住哪一面。**

---

## 4. 执行方式

选定后我按以下顺序做，每项单独提交，可逐项回退：

1. 建补充文件（S48 / S49），**逐字**移入，不改一个字；
2. 正文替换为指针 + 摘要句；
3. 跑 `step127`（数字对账）；
4. 重跑词数统计，报实际降幅——**不承诺预估值，只报实测值**。

⚠ 预估词数是按整段计的，实际会因为要补写"指针 + 摘要句"而少省一些，
上表的"净省"已经扣过这部分，但仍是估计。

---

## 5. 结案（2026-09-11）：**决定按 8,519 词投稿，不再压缩**

### 5.1 先更正本文件自身

⚠ **§1 的清单已过期**，按它动手会砍错地方：

| 项 | 本文件所写 | 2026-09-11 实测 | 说明 |
|---|---:|---:|---|
| A1 `Both axes crossed` | 1,180 | **378** | 上一轮已压过 |
| A2 Discussion 八项检查 | ~700 | **99** | 上一轮已移入 S49 |
| `Part 4` | 309 | **690** | C6 手工编码与 S51 撤回加进来了 |

### 5.2 实测词数（Word 口径，空白切分）

| 部分 | 词 |
|---|---:|
| Background | 346 |
| Results | 6,199 |
| Discussion | 1,972 |
| **主文合计** | **8,517** |
| 摘要 | 291 |
| 图注 | 1,273 |
| Methods | 4,149 |

⚠ 此前评审简报报的 **8,752 是错的**（口径不明或版本过期）。缺口是 **517 词，不是 800**。

### 5.3 ⚠ 「收紧句子」这条路经实测走不通

先试了不删内容、只改写长句。**结果三处改写只省 2 个词**，原因有二：

1. **按空白切分计数，去掉连字符后缀不省词。** `lung 3.85-fold` → `lung 3.85`
   读起来更清爽，但两者**都是一个 token**。同理 `; pancreas` → `. Pancreas` 零收益。
2. **这些句子本来就不啰嗦。** 逐句读过五个最长的，真正的语法冗余
   （`which is simultaneously` → `at once`）每句仅 1–2 词。
   **它们长是因为装了很多信息**——五个结局的倍数与 P、两个统计量的反向排序、
   三项内部一致性检查——不是因为话多。

外推：54 个 ≥45 词的句子全部改写约省 **25–30 词，覆盖缺口的 5%**。
而每次改写都有微妙改变语义的风险，**尤其那两处自我批评的长句**
（145 词的「本分析设计于其所评论的结果之后」、104 词的「裁定理由被我们自己的脚本销毁」）。
**为 5% 承担 54 次这种风险不划算。** 已应用的三处保留（确实更好读），其余不做。

### 5.4 作者决定：接受超出

**按 8,517 词投稿，超目标上限 6.5%。**

理由：`COMPRESSION_PLAN` §1 的 7,000–8,000 是**目标区间上限，不是期刊硬性拒稿线**；
而超出的部分装的恰恰是本文最不利于自己的内容——
RA 作废格的完整论证（B1，519 词）、错配名单选择改变判定（B2，498 词）、
Part 4 的领域级证据（690 词）。**先压这些，等于用可信度换字数。**

若编辑坚持 8,000，届时可动的是 §5.5 所列六段，而**不是** B1/B2/Part 4。

### 5.5 若必须再压，可动的六段（每段净省约 250–330）

`Part 3 — One nomination…`(456)、`Does the pipeline attribute the right gene`(399)、
`The reproducible part of the list…`(379)、`Five nested power levels`(381)、
`A second disease`(378)、`Both axes crossed`(378)。任选两段即可达标。
