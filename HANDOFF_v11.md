# 项目交接文档 v11

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-25
**取代**：`HANDOFF_v10.md`（其 §四技术坑、§五方法论纪律仍有效，本文件继承并扩充）
**工作树**：干净。本轮 `4afcb4c` → `4662223`，共 **64 个提交**，**未 push**

> **新会话阅读顺序**
> 1. 本文件，尤其 **§一（论文变了）** 与 **§三（还剩什么）**
> 2. `REVIEW_BRIEF_v12.md`（给第三方的简报，含两轮评审的逐条处置）
> 3. `manuscript/MANUSCRIPT_GB.md` 的 Abstract 与 Part 1 第一节
> 4. `manuscript/SUPP_estimator_identity.md`（S42）——**中心主张的全部依据**

---

## 一、★★ 论文的中心主张变了

**v10 时**：melanoma 上观察到 4.96 倍的已知位点富集，P = 0.0048。
**现在**：那个数字**被我们自己拆开了**，中心主张换成关于**估计量**的：

1. **单变异 Wald cis-MR 的检验统计量恒等于结局 GWAS 的检验统计量。**
   一工具 + 一阶 SE ⇒ `|z| = |β_out|/se_out`。暴露决定符号、尺度、
   哪些变异有资格被检验；**不决定哪些显著**。
   数值核验：`se` 差 **0**、`p` 差 3×10⁻¹⁶、符号在 51% 记录上相反（S42）。
2. **4.96 倍由结局自己已找到的位点承担。** 八个显著位点里三个自身达 5×10⁻⁸，
   **且三个全在已发表名单上**；剔除后 **1.98 倍 / P = 0.413**
   （背景同步条件化 2.07 / 0.399；按共享 lead SNP 合并 **5.28 / 0.0140**）。
3. **换估计量不改变这一点**：七个口径（Wald / IVW / IVW-MRE / weighted median /
   coloc×2 / SMR-HEIDI）剔除 GWS 位点后**无一显著**（最小 P = 0.213）。

**标题也换了**：`Target nomination from single-variant cis-eQTL Mendelian
randomization re-reads the outcome GWAS`。

⚠ **恒等式是初等代数，本文不主张它是新数学。** 主张的是它对提名名单构成的后果与量。

---

## 二、本轮做完了什么（压缩版）

**S40 前提一查明**：melanoma 的独立外部队列**结构上不存在**。排除名单查全
（Landi Supp Table 1 的 23 个数据集 + Rashkin 的 `UKB|GERA` + FinnGen），
§2 的 leave-cohort-out 路径**作废**（Landi 吸收了整个领域）。
门槛 R ≥ 4（合格覆盖率下限 R ≥ 5），最好的表型合格资源 **R = 0.43**，缺口 9–13 倍。

**新增六份补充材料**：S41（跨疾病迁移）· S42（恒等式与分解）· S43（匹配功效 + 机会校正量 A）·
S44（七口径基准）· S45（精细定位分解 + 代理 LD 的两种失效模式）· S46（诊断的 I 类错误标定）·
S47（工作示例全文）· S48（双轴交叉网格）· S49（八项检查）· S50（推断单位）

**包**：模块 H `cqtna_decomposition()`，`R CMD check` OK，**271 项断言**。

**两轮第三方评审全部处置完毕**，逐条见 `REVIEW_BRIEF_v12.md`。

---

## 三、★ 还剩什么

### A. 需要作者本人（我做不了）

1. **真实维护者身份**：`cqtna_r/DESCRIPTION` 仍是 `MR audit project <noreply@example.com>`；
2. **仓库 DOI**：正文仍是 `⟨repository DOI⟩`；
3. **容器镜像**。⚠ **比包清单更要紧**——S45 已证明**与 BLAS 无关的随机性也能改变结果**，
   而 `150a_environment.lock`（226 个包的确切版本）锁不住 OS/BLAS/编译器。

### B. 下一个窗口要解决的四条（第三方与第四方评审留下的）

1. **★★ 恒等式覆盖多大范围，正文没说。** `step151` 已量出：
   154 篇缓存全文里 **84% 提到 IVW**，但 **46%（71 篇）明确说 cis 工具是单变异**，
   且 130 篇提多工具的论文里 **65 篇同时**说 cis 工具是单变异。
   ⚠ **46% 是覆盖面下界、84% 是逃脱者上界，都不是比率**——词组匹配说不出
   一篇论文用哪个估计量产出候选名单。**正文应加入这组数字并写明其性质。**
2. **重构没传达到位。** 有读者读完仍认为主张是"没找到所以管道不行"。
   实际主张是代数恒等式，**不依赖结局质量或 coloc 参数**。
   ⚠ 这是**表达问题**，解法是三张核心图 + 更短摘要，**不是删自我披露**。
3. **摘要 365 词**，主文 8,627 词（全文 13,875）。NC 摘要 150 词、
   Genome Biology 250 词。⚠ **"更精确"与"更短"在此直接冲突**，本轮选了准确优先。
4. **正文压缩还差约 600 词**才进 8,000。剩余候选见 `COMPRESSION_PLAN.md`，
   但**再砍就是在决定把多少"诚实装置"移出读者视线**。

### C. 明确不做

- **加第三个暴露资源**——恒等式说明暴露侧不决定显著性，**加了不改变任何结论**；
- **为 TPI1 补湿实验**——救一个我们自己已判为不可升级的东西；
- **虚拟扰动 / 通路富集**充"验证"——继承训练数据相关性，会被读成补软证据；
- 追 S40 的外部验证——结构上不可得，不是资源问题。

---

## 四、技术坑（v10 §四之外的新增）

58. **★★ 一个不报错的流程，输出的可能不是你以为的那个。** `susie_rss` 在 KANSL1 上
    无种子三次给 **0 / 1 / 0** 个 credible set，**每次都 `converged=TRUE`、`niter=2`、零警告**。
    根因是 `susie_get_cs` 对大 credible set 的 purity **随机抽 100 个变异**估计，
    而那个 1,851 变异的 set 正好卡在阈值上。**加种子使之可复现，但可复现 ≠ 稳健**——
    须跨种子测敏感性。
59. **★ 报告 credible set 只报个数会骗人。** PARP1 的 35 变异 / 68 kb 与
    KANSL1 的 1,851 / 650 kb 在"1 个 credible set"里无法区分，而后者是
    17q21.31 倒位单体型整块在动，**什么也没定位到**。**必报大小与跨度。**
60. **★★ 静默失败会一直静默下去，直到有人用到它。** `step94b2` 在 rsID 批查询
    抛异常时 `continue`，**整批丢弃不留痕**：lung 316 个 rsID 建出 **0** 个位点、
    prostate 1,175 建出 **35**（3%）。**从未暴露**，因为唯一会用到这些名单的
    `36e` 根本不用它们。修复后 S39 **三格判定改变**，其中一格是头号格子。
61. **★ 绝对门槛拦不住百分比失败。** `MIN_LEAD = 30` 拦住了 lung(0) 和 breast(6)，
    **没拦住 prostate 的 35（源名单 1,175，建成率 3%）**。**须同时设建成率闸门。**
62. **`index()` 会取到第一个匹配。** 我用 `t.index('**Conclusions.**')` 定位
    Discussion 里那个，结果取到了**摘要**里的，拼接把摘要之后的整篇复制了一遍
    ——**793 行重复、全文涨到 22,210 词**。靠词数对不上发现，靠数各级标题
    出现次数定位。**编辑长文档后必须数一次标题出现次数。**
63. **正则 `i+v?` 匹配不到 `(v)`。** 抽八项检查的标题时**静默丢掉第五条**。
64. **docx-js 的 `lineNumbers` 必须在 section properties 顶层**，
    嵌在 `page` 里会被**静默丢弃**，XML 里连 `w:lnNumType` 都没有，
    渲染出来就是没行号且毫无提示。
65. **plink2 的 `.pvar` 若为 zst 压缩，须 `--pfile <p> vzs` 且分两阶段**
    （先 `--make-pgen` 设 `chr:pos:ref:alt` 变异 ID，再算 LD），
    因为那是 `step60c` 对齐 sumstats 的键。
66. **FinnGen sumstats 的标准误列名是 `sebeta` 不是 `se`**，
    而 `step60a` 产出的区域文件已改名为 `se`。**两套文件不能走两条代码路径。**

---

## 五、方法论纪律（v10 §五之外的新增）

56. **★★ 事前写下你预期的失败方向，比事后解释结果更有用。**
    S46 §1 在运行前写明"密名单**很可能超额拒绝**"。**结果相反**，
    密名单反而更保守。**正因为预测在前，这条结果才可信**，
    而且它顺带排掉了一个替代解释（S41 的 void 格不是诊断失准）。
57. **★★ 一个数字"占上限的百分比"可能根本不是归一化。**
    我把 fold ÷ (1/p_bg) 当密度归一化报出，**它恒等于 p_sig**——
    完全不校正背景。fold 完全除以背景、p_sig 完全不除，**两者是同一根轴的两端**。
    正确的是机会校正量 `A = (p_sig − p_bg)/(1 − p_bg)`。
58. **★ 剔除了分子里的东西，分母也要同步剔除。** 只从显著集剔除已达 5×10⁻⁸ 的位点、
    却把它们留在背景里，**问的是一个混合问题**。
59. **★★ 分区限制了窗口宽度，不等于保证相邻窗口独立。**
    两个 MC1R bounded locus 各自 1 Mb 内的 Landi lead SNP **是完全相同的七个**
    ——在重复计同一个已发表区域。合并后 P 从 0.0048 变 **0.0140**。
    ⚠ 但**按间距合并不可用**：250 kb 就把背景从 655 压到 84，
    等于把 `fixed_centre` 当初要避免的单连锁串联请回来。
60. **★ 审计脚本自己也会腐坏。** `step127` 把"最宽格子"**点名硬编码**成
    `HCC_low × Soskic_CD4`，名单修复后最宽已换人；且子串匹配让 `4.84`
    靠正文里的 **4.87** 假通过。**点名式期望看不出排名变了；数字匹配须加边界。**
61. **★ 移动一段正文之前，先想清楚里面有没有主要证据。**
    我把双轴交叉整段移入补充，`step127` 当场抓到三个缺失数字，
    其中**密度置换 4.87 与频率匹配背景 6.66 是主要证据，不是网格叙述**。
    **这正是那个审计存在的理由。**
62. **★ 判定标签要能表达"成功"的所有形态。** 我的名单复现标签只有
    `exact` 和 `superset-in-repo`，其余一律 `no`——于是
    lung 那种"仓库条目全部复现、另有 Catalog 新增"的**成功**情形被判成失败。
63. **★★ 声称覆盖一个领域之前，先量一量覆盖多少。**
    "恒等式只适用单工具"这条批评是关于**文献的经验主张**，
    可以用手上的 154 篇全文直接量（`step151`），而我们直到被第四次质疑才去量。

---

## 六、文件地图（v10 §六之外的新增）

**决策与评审**
`REVIEW_BRIEF_v12.md`（**给第三方，从这里开始**）· `REVIEW_BRIEF_v11.md`（上一版）·
`ASSESSMENT_gap_to_ten.md`（不客气的自评）· `COMPRESSION_PLAN.md`（压缩取舍）·
`DESIGN_LADDER.md`（两级目标框架）· `NEXT_EXPERIMENTS.md` ·
`BENCHMARK_vs_NC_reference.md`（对照一篇 NC）

**S40 外部验证**
`S40_COHORT_SCREEN.md`（队列筛查）· `S40_POWER_GATE_SPEC.md` ·
`S40_QUERY_TO_PROTOCOL_AUTHOR.md`（退回给方案作者的两个问题）· `S40_LOCK.md`（锁定记录）·
`S39_RERUN_AFTER_LIST_REBUILD.md`

**补充材料**（`manuscript/`）
`SUPP_estimator_identity.md`（**S42，中心主张**）· `SUPP_power_matched_transport.md`（S43）·
`SUPP_finemap_decomposition.md`（S45）· `SUPP_worked_example.md`（S47）·
`SUPP_crossed_grid.md`（S48）· `SUPP_eight_checks.md`（S49）· `SUPP_inference_unit.md`（S50）·
`PREREG_transport_grid.md`（S41）
根目录：`SUPP_method_benchmark_spec.md`（S44）· `SUPP_typeI_calibration_spec.md`（S46）

**脚本**（本轮新增）
`step132`–`step135`（S40 功效闸门）· `step136`（锁定清单）· `step137`（名单重建）·
`step138`（迁移网格）· `step139`（独立性诊断）· `step140`/`141`（恒等式与分解）·
`step142`（匹配功效）· `step143`（名单出处）· `step144`/`145`（七口径基准）·
`step146`/`147`（精细定位扩展）· `step148`（文献语料发布）· `step149`（I 类错误标定）·
`step150`（环境锁）· `step151`（文献里的估计量用法）

**投稿件**
`manuscript/MANUSCRIPT_GB.docx`（55 页，US Letter，双倍行距，连续行号，九图各占一页）
构建：`manuscript/parse_md.py` + `manuscript/build_docx.js`。**`.docx` 是构建产物，未入库。**

**图**：`figures/Fig1_locus_attribution` … `Fig9_patients`（各 `.pdf` + `.png`）。
⚠ 目录里还有 `Fig5_compartment`、`Fig6_glycolysis_TPI1`、`Fig7_crosscancer`、
`Fig9_part2`、`FigA_*` 等，属**另一套编号方案**，别拿错。
对照见 `manuscript/FIGURES_GB_mapping.md`。
⚠ 该文件 §五记着一条**未处理**的问题：**Fig 7 panel b 与正文措辞冲突，当时按表画的**。

---

## 七、下一轮建议顺序

1. **把 `step151` 的数字写进正文**（§三-B1）——最便宜，且直接回应最有力的批评
2. **三张核心图 + 摘要重写**（§三-B2、B3）——解决"读者记住的是啥也没捞着"
3. 正文再压约 600 词（`COMPRESSION_PLAN.md`，取舍需作者定）
4. 核 `FIGURES_GB_mapping.md` §五那条 Fig 7 panel b 的冲突
5. 作者补：维护者身份、DOI、容器

⚠ **改任何一个数之前**，先在 `manuscript/NUMBER_MIGRATION_fixed_anchor.md` 找到它的来源；
改完跑 `python step127_audit_manuscript_numbers.py <repo>`，当前 **CLEAN**。
**它只查数字，查不了措辞**——本轮 §五-60/61 两条都是它抓到的，
而 "must recur"、"六个有可用名单"这类是它**抓不到**的。
