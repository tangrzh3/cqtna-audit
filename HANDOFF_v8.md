# 项目交接文档 v8

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-18（Step 124 后，三轮外部复核已处理）
**取代**：`HANDOFF_v7.md`（其 §四强项、§五技术坑、§六方法论纪律仍有效，本文件继承并扩充）
**本轮 commit**：`46ef2a8` → `dd14af2`，共 **26 个**，工作树干净

> **新会话阅读顺序**
> 1. 本文件，**尤其 §二-C（我犯的审计链错误，必须先修）与 §三（还剩什么）**
> 2. `manuscript/PREREG_locus_partition.md`（**已冻结的分区决策记录**）
> 3. `123d_fixed_anchor_full_grid.tsv`（**完整网格，新口径**）
> 4. `CONVENTION_REVIEW_PACKET.md`（口径之争的完整证据链）
> 5. `manuscript/MANUSCRIPT_GB.md`（投稿母稿，**尚未按新网格更新**）

---

## 一、当前定位

结论未变，但**统计单位换了，且论文数字尚未更新**。

**主分析口径（已冻结）**：非递归固定锚定分区、最大跨度 1000 kb、归属口径 C1 `any_record`。
**500 kb 为敏感性；LD 区块待样本内 LD；1 Mb 单连锁 + C2 仅作历史复现，不承担推断。**

⚠ **不适合冻结投稿版**。阻塞项见 §三-A，其中第 1 项是我自己造成的。

---

## 二、本窗口做了什么

### A. GB 九图全部建成（Step 119–121）

`python figures/make_gb_all.py` 一条命令重现 `Fig1`–`Fig9`（PDF + PNG）。
对照表见 `manuscript/FIGURES_GB_mapping.md`。

顺带修的三处正文错误（详见 v7 §二-C）：零模型"无一超过"是假的、
`+0.56 vs +0.74` 混用两个 signature、Okada 2014 被引三次却不在编号表里（已补为条目 47）。

### B. R 包 `cqtna_r/`（0.3.0）

`R CMD check` **OK**，**229 项断言**，四套测试：论文数字复现 / Python oracle /
13 组合成机制 / 输入契约。Python 参照实现同步保留在 `cqtna/`。

关键能力：`locus_method`（三种分区）· `known_from`（三种归属口径）·
`cqtna_locus_spans()`（串联诊断）· `cqtna_permutation_control()` + `_sensitivity()`
（密度匹配置换与容差扫描）· 错配对照自身显著时 `cqtna_audit()` 报警。

### C. ★★ 我犯的审计链错误——**下一窗口第一件事就是修它**

**我把一个事后稳健性分析升成了"预注册主口径"，并在代码注释里写成 `pre-registered`。**

事实（`manuscript/PREREG_noncancer_outcome.md`）：

- §4：**N1 = Soskic CD4 × RA 是主格**，N2 = eQTLGen 是"次"；
- §5 主检验定义：FDR<0.05 独立位点落在 Okada 1 Mb 内的比例，**没有任何 MHC 排除**；
- MHC 排除**只出现在结果登记表**（§ 约 113 行），与主格并列——**是看过 RA 结果之后的稳健性分析**。

而我在 `step124_full_grid.R` 第 66–78 行，**在看到 RA×eQTLGen 作废之后**，
把 MHC 排除版换成主输入，并注释为 "pre-registered (S33)"。

⚠ **这正是本文用来批评别人的那种错误**：看过结果再改口径，还给它贴上预注册标签。
必须改回去，并在 `CONVENTION_REVIEW.md` 里留下这次更正的记录。

### D. 分区之争：三轮复核的结论

| 轮次 | 结论 |
|---|---|
| 一轮 | 指出模块 A 混用三套记录集（分母全部记录／分子仅显著记录／基因标签按单条记录）。**基因标签那条是无争议 bug，已修** |
| 二轮 | 我改成 C1 后，**错配对照失效**（eQTLGen 3.90× P=1.7e-4）。根因是单连锁把染色体臂串成 30.8 Mb 的块 |
| 三轮 | 判 **C+D**：问题在位点分区 + 必须用密度匹配置换。并查出我的置换函数有推断缺陷、复现链断裂、README 数字是部分匹配下算出的 |

**换成固定锚定分区后**：跨度由构造封顶（30,778 → 999 kb），错配对照恢复干净，
置换在容差 0.25–2.0 间不再翻转。**口径之争是串联的副产品。**

### E. 完整网格（`123d_fixed_anchor_full_grid.tsv`）

主分析 fixed_centre 1000 kb + C1：

| 格子 | 倍数 | P | 错配对照 | 判定 |
|---|---|---|---|---|
| melanoma × CD4 | 4.96× | 0.0048 | 0.00× P=1 | ✅ |
| melanoma × eQTLGen | 6.31× | 1.4e-15 | 1.59× P=0.291 | ✅ |
| HCC-high × CD4 | 7.88× | 0.123 | 0.00× P=1 | ✅ 未达显著 |
| HCC-high × eQTLGen | **10.87×** | **0.0015** | 0.00× P=1 | ✅ |
| RA × CD4（MHC 包含，**应为主格**） | 3.91× | 2.3e-7 | 1.71× P=0.102 | ✅ 对照不算干净 |
| **RA × eQTLGen** | 3.94× | 3.3e-17 | **2.05× P=2.9e-4** | ❌ **作废** |

**五格可评价、方向全部 >1、四格达 P<0.05。**

⚠ 三件必须写进正文：

1. **RA × eQTLGen 按预注册判据作废**，非癌泛化只剩 CD4 一格；
2. **"未达显著的两格是同一疾病"不成立**——HCC-high × eQTLGen 从 P=0.0515 变 0.0015，
   现在只有 HCC-high × CD4 未达；
3. **HCC-high × eQTLGen 的变化不是数据变了**：背景从 562 个长块拆成 1,666 个有界块，
   背景已知率 7.83% → 5.52%，同时一个显著块在 C1 下成为 known（2/5 → 3/5）。

### F. 中间数据落盘（本窗口最有价值的工程改动）

`step108`/`step94d` 此前边扫 sumstats 边算、**只存汇总行**，
导致换分区就无法重算、也无从复核。现已永久落盘：

- `123b_ra_records.tsv.gz`（35,795 条，5 个 RA 变体）
- `123b_ra_known_positions.tsv`（Okada 解析出的 83 个 GRCh38 坐标，此前只活在内存）
- `123c_eqtlgen_hcc_records.tsv.gz`（25,059 条，2 格）

**列口径 = `cqtna::as_cqtna_mr()`，可直接重算。**
`108a`/`94f` 重跑后**逐字节相同**——加落盘没动任何已发表数字。
已改 `.gitignore` 加例外让这两个 `.gz` 入库（它们正是审计链本身）。

---

## 三、★ 还剩什么

### A. 投稿前必须做（按顺序）

1. **★ 修 §二-C 的审计链错误**：`step124_full_grid.R` 的 RA 主格改回 **MHC 包含**，
   MHC 排除行明确标为 `post-hoc sensitivity`；
   `legacy` 复现必须用 **MHC 包含**输入，恢复 **3.46 / 3.57**
   （现在的 3.03 / 3.32 是 MHC 排除版跑旧算法，**不是论文复现**）。
2. **给网格加两个显式字段**，杜绝同类混淆：
   ```
   region_filter   = all_genome | MHC_excluded
   analysis_status = prereg_primary | posthoc_sensitivity | legacy_reproduction
   ```
3. **RA × eQTLGen 在 1000 kb 下保持作废**，**不得用 500 kb 选择性挽救**
   （冻结记录 §4 明写主分析失败即作废；500 kb 只能写成分区尺度敏感性）。
4. **加逐位点错配诊断表**：1000 kb 下 19 个错配已知显著块里，
   **5 个块没有任何显著记录靠近黑色素瘤 lead**，纯粹靠块内非显著记录被 C1 标为 known
   （chr2:202.0–202.9、chr5:34.7–35.6、chr20:34.6–35.6、chr22:21.5–22.4 Mb 等）。
   500 kb 下这类块只剩 1 个。**这是"C1 状态在宽块内扩散"，既不是单连锁串联、也不只是 MHC 共享。**
5. **正文承认错配名单不是正交的阴性对照**：RA 与 melanoma 共享免疫区域，
   且在密集全血资源下还存在非 MHC 的状态扩散。
6. **按新网格更新正文数字、S30/S36、图 1/2**，并校准措辞（§四给了建议句）。
7. **置换估计目标定稿**：`manuscript/PREREG_permutation_estimand.md` 已冻结六项，
   容差留三条路待选（建议 B：固定 1.00 + 报整张扫描表）。

### B. 应该做但不紧急

- 用**密度匹配置换 + 多个预先选定的不相关疾病名单**替代单一错配名单；
- LD 区块分区（`locus_method = "blocks"`）作为关键敏感性，待样本内 LD 后升为主分析；
- References 的 7 条未引条目与 DepMap release（见 v7 §三-A2/A3）；
- NC 稿补 Methods（能一次消化 31 条未引条目）；
- R 包发布：GitHub + Zenodo，占位符 `OWNER` 待替换，见 `cqtna_r/RELEASE.md`。

### C. 明确不做

- CRAN、百万级性能、X/Y 支持——投稿后再说。

---

## 四、正文可用的措辞（第三方给的，我认为可直接用）

> 显著信号与已知疾病区域存在方向一致的重叠，但富集幅度和统计显著性对位点分区、
> 归属口径及密度匹配方案敏感，因此该结果应作为描述性归属证据，
> 而非已被证明独立于区域密度的富集。

配套的一句：

> 保留五格可评价、四格显著的结论；撤回"MHC 排除是预注册主口径"的说法；
> RA 非癌症泛化只由 CD4 主格支持，eQTLGen 格按规则作废。

---

## 五、技术坑（v7 第五节之外的新增）

41. **★ 看过结果之后改口径，即使新口径更合理，也必须标成 post-hoc。**
    我把 MHC 排除升为"预注册主口径"就是踩了这条。
    **判断某口径是不是预注册的，去读预注册文件的"设计"节，不要读"结果登记"节**——
    后者里的行是事后加的。
42. **`.gitignore` 的 `*.gz` 会挡住自己派生的中间表。** 派生数据是审计链，第三方数据才该忽略。
43. **Bash heredoc 在长内容上会截断，且会把 `\t`/`\n` 解释成真实字符。**
    生成含转义序列的代码时用 Write 工具，或用 `chr(9)`/`chr(92)` 绕开。
44. **R 的 `round()` 与 `sprintf()` 在恰好 .005 边界不同**（5.325 → 5.32 vs 5.33）。
    真值高于中点时 sprintf 对。**存全精度，只在渲染时取整。**
45. **`review_convention.py` 曾写死 1 Mb**，却被评审包引用来复现四窗口扫描。
    **文档引用某脚本前，先跑一遍确认它真能产出那张表。**

---

## 六、方法论纪律（v7 第六节之外的新增）

36. **★ 阴性对照是裁判，不是装饰。** 本轮两个关键决定——否决 C1、判 RA×eQTLGen 作废——
    都是错配对照给出的，而不是我看倍数看出来的。
37. **一个恰好让自己结果更强的"修正"，是最该被独立复核的那类。**
    C1 会把 eQTLGen 从 4.44 抬到 4.88、把 HCC-high 从不显著抬到显著——
    正因为如此才必须跑对照，而对照说它错。
38. **不要在坏分区上挑口径。** 三种归属口径在不串联的分区上完全一致，
    在串联的分区上才分歧。**分歧本身是分区坏了的症状。**
39. **"某窗口不串联"是个需要验证的断言。** 我在评审包里写过"100 kb 不串联"，
    实际最大显著块仍有 1,182 kb、5 个超窗口 5 倍。已更正。
40. **同一张网格不得混用两种统计单位。** 这是本轮重跑两个大扫描的唯一理由，
    也是"只报四格"不可接受的理由。
41. **中间数据不落盘 = 未来无法换口径重算。** `step108`/`step94d` 的教训：
    只存汇总行，一年后想换分区就只能重扫 1 GB，且无从复核。

---

## 七、文件地图（本窗口新增）

**决策与复核**：`manuscript/PREREG_locus_partition.md`（**已冻结**）·
`manuscript/PREREG_permutation_estimand.md` · `CONVENTION_REVIEW.md` ·
`CONVENTION_REVIEW_PACKET.md` · `CQTNA_R_REVIEW_PACKET.md` · `CQTNA_R_PACKAGE_PLAN.md`

**R 包**：`cqtna_r/`（0.3.0，`RELEASE.md` 有冻结清单）
**Python 参照**：`cqtna/`（module G 已补，与 R 同步）

**脚本**：`step119`（网格重建）· `step120`（TPI1 窗口）· `step121`（窗口敏感性）·
`step122`（口径对照）· `step123_fixed_centre_grid.R` · `step124_full_grid.R` ·
`review_convention.py`（`--table` / `--loci` / `--spans`）

**表**：`119a`–`119c` · `120a` · `121a`–`121c` · `122a` ·
`123a`–`123d` · `123b_ra_records.tsv.gz` · `123c_eqtlgen_hcc_records.tsv.gz`

**图**：`figures/Fig1_locus_attribution` … `Fig9_patients`（各 PDF + PNG）·
`figures/make_gb_all.py`（总驱动）

---

## 八、下一轮建议顺序

1. **修审计链错误**（§三-A1–A2）——RA 主格改回 MHC 包含，legacy 恢复 3.46/3.57，
   加 `region_filter` 与 `analysis_status` 两列
2. **加逐位点错配诊断表**（§三-A4），把"5/19 靠非显著记录"写进补充材料
3. **按新网格更新正文数字与图 1/2、S30/S36**（§三-A6）
4. **置换估计目标定稿**（§三-A7）
5. 然后才谈冻结投稿版本

⚠ **在 §三-A1 修完之前，不要更新正文任何 RA 数字**——现在的主表 RA 行是错的。
