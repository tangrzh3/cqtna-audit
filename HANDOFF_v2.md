**⚠ 已被 `HANDOFF_v3.md` 取代（2026-08-11，Step 56–82）。第七节纪律与第八节技术坑仍有效，v3 在其基础上补充。**

# 项目交接文档 v2

**项目**：CD4⁺T 细胞活化动态 eQTL → 黑色素瘤的孟德尔随机化研究
**最后更新**：2026-08-11
**取代**：`HANDOFF.md`（v1，2026-08-08，其技术坑清单仍有效，见第八节）

> 新会话阅读顺序：本文件 → `FINDINGS_step5_pigmentation.md`（按步骤的完整日志）
> → `manuscript/`：`FIGURES_plan.md`（图序与两处数字裁定）、
>   `INTRODUCTION_draft.md` / `RESULTS_draft.md` / `DISCUSSION_draft.md`

---

## 一、一句话定位

**这是一篇质疑「动态 eQTL + MR 能否可靠发现癌症免疫靶点」的方法学论文。**
答案：可信度远低于文献隐含的水平。TPI1 是应用示例，**不是卖点**。

三篇参考文献（`D:/文章/孟德尔/pdf_text/`）用同一框架，**七项检查一项都没做**。

---

## 二、核心论证（写作骨架）

### 主论断：失败率本身就是结论

八次试图坐实靶点主张，八次被自设的检验推翻。**这些检验都是为了"加强"主张而做的，不是为了证伪。**

| # | 尝试 | 检验 | 结果 |
|---|---|---|---|
| 1 | PADI4 为新免疫候选 | 多工具变量敏感性 | OR 1.087→IVW 1.033 (p=0.085) |
| 2 | FinnGen 轮名单 | 换高功效 meta 结局 | 无一存活，**交集为零** |
| 3 | HLA-C 经增殖起作用 | 独立队列复现 | 方向反转，后查明是归一化假象 |
| 4 | bulk ICB 支持 CD4 特异 | 空转 + 细胞类型阳性对照 | 组织信号是肿瘤糖酵解 |
| 5 | 糖酵解高 CD4 的表型 | 按谱系而非仅深度匹配 | 全部归零（14/14 恰好随机）|
| 6 | 通路级 MR | 数通路内工具变量 | 28 个基因仅 2 个有，第二个不显著 |
| 7 | 通路级 GWAS 富集 | 表达/MAF 匹配置换 | p=0.52（n=9，功效低）|
| 8 | 跨癌种证明位点归因特异 | 按独立位点而非记录计数 | 黑色素瘤 4.09× p=0.028，前列腺 2.38× p=0.031 |

**另有三类结果不属这八次，不可并列写进同一张表**：

| 类别 | 实例 | 为何分开 |
|---|---|---|
| 层面不匹配 | 染色质检验（eQTL 的时点特异是基因型×时点交互，平均可及性非其必要或充分对应）| 检验设计本身不对，阴性不含信息 |
| 数据不可得 | TPI1 在 8/8 刺激态 T 细胞 eQTL 数据集未被定量；Cytoimmgen 即同一实验 | 技术限制，非科学否定 |
| **预防性检验** | **Step 52**：先验证"糖酵解是否个体水平性状"，前提不成立 → 取消分析 A | **主动检验前提并据此取消后续，是纪律的正面实例，不是失败** |

第三类在 Discussion 里应作为方法论纪律的例证单独提及，**不是失败清单的第九行**。

### 七个方法学发现（递进，非清单）

① **信号从哪来**：FDR 显著信号 100% 落在已知色素/痣位点（meta 轮 12/21，5.26×，p=2.7e-7）
② **SMR/HEIDI 能排除 LD 混淆吗**：不能，coloc 判为不同因果变异的 291 条中 253 条（86.9%）被放行
③ **加功效行吗**：MR 增强（10→21）而 coloc 减弱（0.249→0.207），二者依赖信息不同
④ **名单稳不稳**：coloc 分辨力由结局功效决定，误差双向，**两轮交集为零** ← 最重要
⑤ **那靠什么定位**：正交功能证据。共位点基因归属（TPI1 vs SPSB2，三条正交证据）+ 区室归属（空转裁决为否定，HLA-C 镜像证明有分辨力）
⑥ **MR 到底点名了谁**：**有工具变量的那个**。功能上 22 个酶有信号，遗传上 1 个可见（1/22）
⑦ **功能验证自身的陷阱**：群体内按评分切分会同时切出纯度梯度；深度匹配无效，须按谱系匹配；**cluster 层面对照会通过而细胞层面失败**

### ★★ 收口（Step 55，全文最强）

**候选名单中可复现的部分，恰恰是不构成发现的那部分。**

| 病例数（占 12,530） | 已知位点基因恢复 | 新位点基因恢复 |
|---|---|---|
| 1,253 (10%) | 46.0% | **0.4%** |
| 6,265 (50%) | 85.8% | **22.8%** |
| 11,277 (90%) | 97.4% | 90.2% |

恢复 50% 所需：已知 2,506 例，新位点 8,771 例（**3.5 倍**）。
同类研究常在 3,000–8,000 例 → **该区间新位点恢复率仅 6%–40%**。

---

## 三、TPI1：应用示例的确切边界

### 证据结构是**倒置**的（遗传弱、功能强），须主动交代

| 遗传 | 功能 |
|---|---|
| MR p=1.4e-3，**FDR=0.119 不过** | 应答分层 Pre FDR 0.046 / Post 0.017 |
| coloc **PP.H4=0.51**（判据边缘） | 通路级 Post 20/22（二项 p=6.1e-5）|
| SMR 3.0e-3、HEIDI 0.45 | 跨五癌种全阴（p=0.086–0.69），**特异** |
| 敏感性 IVW 3.8e-4、WMed 2.6e-3 | GSE115978 独立复现耗竭相关 |

入选靠的是四重验证的**合取**，不是任何单一阈值。

### 16h 活化窗口（原创性最高的观察）

| profile | 0h | **16h** | 40h | 5d |
|---|---|---|---|---|
| Naive | 8.4e-05 | **1.7e-12**（148 SNP）| 8.7e-03 | 3.5e-03 |
| Memory | 6.2e-03 | **1.2e-09**（53 SNP）| 3.7e-02 | 7.1e-03 |

两谱系独立复现；各 profile 工具变量产率持平（51.3–55.0%）故非假象；
**同位点 SPSB2 为组成型**（0h 即 1.4e-11）——第三条归属证据。
类水平：糖酵解基因最强 eQTL **回避静息态**（10.4% vs 25.3%，OR=0.343，p=0.019）。

### 机制（Step 43–50，GSE282266 纯化 CD4 multiome）

- 糖酵解与活化**近乎正交**（R²=0.024，40,495 核）← 可回应"这不就是活化吗"
- 非核/胞质污染假象（4.7% 方差）
- 轴的两端：合成代谢（糖酵解+核糖体 12%/10.3×+OXPHOS）↔ T 细胞身份（TOX/IKZF2/MYB/TXK/CAMK4/SESN3）
- 染色质：高端 **AP-1**（扣掉活化后 23/23 motif 保留，中位 odds 1.81→1.52），低端 **IRF/STAT1-STAT2**
- ⚠ 染色质轴仍有 **42%** 与活化共享（r=0.647）；限定活化不变 peak 是补救非分离

### 不能写的

- **不能写"靶点"**：DepMap 必需基因、pLI=0.87
- **不能把风险因果与应答关联连成因果链**：MR 建立的是发病风险因果；应答关联是观察性、单队列
- **不能声称独立复现活化窗口**：8/8 刺激态数据集不定量 TPI1，Cytoimmgen 即同一实验（循环）

### 其他候选

- **SMC2 泛癌**（黑色素瘤/肺/乳腺/前列腺四癌种同向显著）→ 普适增殖机制，**不可作免疫佐证**；
  连带作废 FINDINGS Step 13 的「ZFYVE19+SMC2 细胞分裂模块」定位
- **HLA-C 方向反转**（其余癌种一致保护），机制解释须保守
- ZFYVE19 遗传最强（FDR 0.0019、PP.H4 0.99、峰位 0 kb）但**功能全空**且影响痣

---

## 四、当前进度

### 已完成
MR/coloc/SMR/敏感性/Steiger/共病/多效性成药性 · GWAS meta · 空转区室归属 ·
单细胞三队列（含**两处数据错误的发现与全部数字重算**）· 跨癌种六疾病 ·
体外机制轴 + motif · **功效-稳定性曲线（六疾病 + 位点分层）** ·
**Fig 4（功效-稳定性）新作 + 图序定稿** · Introduction / Results / Discussion 初稿

### 分析 A / B 已完成（Step 58–60，2026-08-11）
预注册与全部判读见 `manuscript/PREREG_power_trajectory.md`（§8 = 分析 A，§9 = 分析 B）。
**两个替代解释各排除了一半**，正文已按此改写，**不得升格**：
- 解释(2) 数据集异质性：已知位点支拿到 FinnGen 五个真实功效水平的纵向证据（12/12 预测命中）；
  新位点支拿不到——该功效区间内新位点候选**根本不出现**
- 解释(1) 单因果假设：**PARP1 排除**（两个功效下均只有 1 个 credible set，对 L 稳健）；
  ZFYVE19 与 TPI1 **无法判定**（两区在两个功效下均无 credible set）
- ⚠ **新增 TPI1 限定**：其 PP.H4=0.51 所在区域无可分辨的结局侧信号，已写入正文与 Limitations

### 待做
- 补充图 S1–S8（清单见 `manuscript/FIGURES_plan.md`）；FigG（release 轨迹）是否入正文待定
- 参考文献；Abstract 是否纳入 release 轨迹与多信号敏感性的结论
- Fig 7a（按独立位点的跨癌种富集）是否并入 Fig 1，待定
- Methods 的三处 ⟨⟩：软件版本逐一核对、耗竭评分基因集的声明、脚本/结果表发布范围
- Abstract 与 Title；参考文献

### 已定稿的文稿
`manuscript/` 下 Introduction / Results / Methods / Discussion 四份初稿齐备，
数字口径已统一（位点级 4.09×；校准点 10.2 vs 10）。

### 已完成并关闭的方向
**外周血可测的相关物 —— 关闭**（Step 52，GSE199994）
患者内部 CD4 糖酵解 ICC=0.0285，未超过同子群重算的随机地板（95 分位 0.0403）。
10 供者的 0.104 由健康vs患者/性别/批次三个混杂驱动，不可解读。
→ **分析 A（IRC 检验）前提失效，不做**。IRC 比较是患者组内 4v4，
而患者内无超出噪声的个体间结构，阳性为假、阴性无信息。
预注册设计 `manuscript/GSE199994_prespecified_design.md` 存档，标注"前提未通过，未执行"。

### 明确不做
药筛/分子对接（必需基因，自相矛盾）· 46-profile 扩展（瓶颈在结局侧）·
更高分辨率空转（换数据救不了层面错配）· 新的单细胞描述层（继承发现⑦的缺陷）

---

## 五、两个已修正的数据处理错误（影响过全部单细胞数字）

**① `GSE120575_CD4_clusters_1_5_6_12_15.rds` 被二次归一化**
把 log2(TPM+1) 当 counts 喂进 `NormalizeData()`；与正确对象非零值 ρ 仅 0.874。
正确对象：`24_checkpoint_GSE120575_annotated_obj.rds`（反解每细胞和 9.94e5）。
**该 rds 只能取细胞 ID，不可取表达值。**

**② `19i_patient_tp_with_response.tsv` 应答标注不可用**
GEO 中 Post_P1 是 Responder、Post_P1_2 是 Non-responder（P5 反向），19i 合并成单一样本单一标签。
**须用对象的 GEO `response` 字段 + `patient_tp` 作样本 ID。**

**修正后定论数字见 `32a`–`32h`（Step 26），旧 `19*`/`28*` 患者层面数字全部作废。**
⚠ GSE115978 **没有**此问题：其原始文件本就未归一化（CV=1.15，跨 46 倍），
LogNormalize 是正确处理，Step 18 结论有效。
**判据是原始文件是否已归一化，不是 rds 与原始文件是否一致。**

---

## 六、数据与脚本索引

### 关键数据
| 用途 | 位置 |
|---|---|
| 暴露 eQTL 全 cis 窗口 | `D:/Downloads/CD4_eqtl_step1_clean/*.parquet` |
| 结局（主用） | `meta_melanoma_final.tsv.gz` |
| 跨癌种提取 | `cancer_extracts/*.tsv` |
| GSE120575 正确对象 | `24_checkpoint_GSE120575_annotated_obj.rds` |
| GSE282266（纯化 CD4 multiome）| `D:/Downloads/GSE282266/` |
| GSE199994（外周血 multiome）| `D:/Downloads/GSE199994/` |

### 定论结果表
| 文件 | 内容 |
|---|---|
| `32a`–`32h` | GSE120575 修正后全部数字（**取代 19*/28***）|
| `36a`–`36e` | 跨癌种 MR + 位点归因 |
| `37a`–`37d` | GSE282266 pseudobulk + 类水平可及性 |
| `43a`–`45a` | 糖酵解轴：正交性、无偏刻画、污染排除 |
| `47a`–`50b` | 差异 peak + motif（含活化不变版）|
| **`53a`–`55a`** | **功效-稳定性曲线（校准/六疾病/位点分层）** |

### R 环境
**干活的是 `D:/R/R-4.4.1`**（Seurat 5.5.1、CellChat、motifmatchr、JASPAR2020、
BSgenome.Hsapiens.UCSC.hg38、chromVAR 均已装）。**不是 R 4.5.0，那个库是空的。**
Python 侧不依赖 statsmodels（与本机 scipy 不兼容），OLS/BH-FDR 已自实现。

---

## 七、方法论纪律（本项目反复验证有效，务必延续）

1. **每个检验必须有阳性对照，且对照失效即废掉该检验**
   实例：空转的 HLA-C 镜像、motif 的 AP-1 活化对照、ICC 的 XIST 上界 + 随机模块地板。
   反例（教训）：Step 43 只给增殖设了对照，未检查各免疫模块是否可测，
   结果用测不到的基因（IL5 0.6%、IL17A 0.6%）检验该体系产生不了的状态，阴性无信息。
2. **判废条件、检验方向、窗口大小，全部写在跑之前**，且写进脚本注释
3. **阴性之后不得另起炉灶找阳性**——那是"阳性对照失效仍继续"的镜像
4. **模拟必须有外部校准**：Step 53 的模拟预测了一个独立测得的真实值
   （Jaccard 0.455 落在 [0.364,0.727]；命中数 **10.2 模拟 vs 10 真实**，比值 1.02）
   ⚠ 此前本行写的 "9.6/0.96" 是 `53a` 中 5,000 例那一行，不是校准点，已更正（Step 56）
5. **sanity check 要能自己抓错**：Step 53 两次外推失败均被"观测功效下命中数须匹配真实值"抓住；
   Step 47 v2 的 bug 靠"top peak 全在 chr1"暴露 → 已把染色体分布做成常规输出
6. **样本/供者是统计单元，细胞不是**

---

## 八、技术坑（重踩会很贵）

v1 的第 1–13 条仍然有效（GRCh38 不做 liftover、不用 OpenGWAS 的 R5 快照、
`sum(x & NULL)` 静默返回 0、`AnnotationDbi::select` 遮蔽 `dplyr::select`、
TwoSampleMR 的 R² 无上界、meta 后 p 下溢、Cochran Q 的 df=1、Open Targets 旧 API 停用、
matplotlib 无中文字形、statsmodels/scipy 不兼容、Thrane 空转坐标在列名里），另补：

14. **`nzchar(NA)` 返回 TRUE** —— GSE120575 表头前导空字段用 `nzchar` 过滤不掉，
    导致 n_cells 多 1、**列标签整体错位一位**。表头须用 `readLines`+`strsplit`，
    并加 `!is.na()` 与 `stopifnot(n==16291)`
15. **10x Multiome 的 RNA 是 snRNA-seq**：MALAT1 居首、GAPDH 仅 18.7% 检出属正常；
    **per-cell 检出率不可与 scRNA 比较，RNA 须按样本 pseudobulk**
16. **每样本 peak 集不同**（GSE282266 为 94k–134k）：跨样本比较**必须按基因组区间对齐**。
    此坑连踩两次——v1 按名字精确匹配（交集 0），v2 用 `reset_index(drop=True)`
    销毁全局索引（**所有染色体映射到 chr1**）。v3 用八样本区间合并的共识集（199,740）
17. **10x Multiome 的 GEX 与 ATAC 用两套条码白名单**：DOGMA-seq 的 ADT 在 ATAC 侧捕获，
    要配对须 10x 的 `737K-arc-v1` 翻译表（公开单文件副本未找到）
18. **eQTL Catalogue 按基因查询的结果按位置排序且分页**，取一页的最小 p 是无意义的；
    须按 `rsid=` 查询
19. **Cytoimmgen（eQTL Catalogue）就是 Soskic 数据**（16h 样本量 Naive 99 / Memory 95
    与我们记录完全一致）→ 用其"复现"即循环论证

---

## 2026-08-11 大幅更新（Step 63–70）

**稿件主文件已改为 `manuscript/MANUSCRIPT_v2_dual_thread.md`（双主线），
v1 被取代但保留。**

新增分析：锁定糖酵解 signature（16 基因，三集嵌套）+ leave-TPI1-out（Step 63）·
工具变量所在元件的染色质不变性（64）· motif disruption 预设检验，AP-1 假设**否定**（65）·
GSE316760/GSE300445 探针法 Visium **不可用**（66/67）·
**区室归属的细胞层面验证 + 深度匹配 + 独立队列复现**（68/69）·
TCGA-SKCM 区室问题演示（70）。

**当前最强的三个结果**：
1. 功效-稳定性收口（Fig 4）+ FinnGen release 轨迹 12/12 预测命中
2. **区室归属**：TPI1 在恶性细胞比 CD4 高 6.7×，两队列 16/16 与 11/11，深度匹配后仍在，
   bulk 生存关联可被免疫含量解释掉
3. 患者侧糖酵解应答分层：锁定集 + 去 TPI1 后仍成立（Pre 13/15、Post 14/15）

**仍欠**：参考文献（三篇对照已有完整题录，见 `SUPP_comparator_studies.md`）·
Fig 5 需按新结构重做（细胞层面为主）· Fig 9 已完成 · Abstract 关键词
