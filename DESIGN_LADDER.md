# 设计层级：当前文章要什么，"通用方法学"主张要什么

**日期**：2026-08-21
**关系**：本文件谈**设计**；`S40_COHORT_SCREEN.md` 谈**执行现状**；
`EXTERNAL_VALIDATION_PROTOCOL_v9_3.md` / S40 谈**已冻结的确证性方案**。
**⚠ 本文件不是预注册**，也不改动 S40 的任何冻结项。它是一份取舍记录。

---

## 0. 为什么需要这份文件

复核意见提出：如果只想投出当前文章，一个独立 melanoma 就够；
如果要主张"通用方法学验证"，就该扩成"疾病层级 × eQTL 类型"的网格。
**这个分层是对的**，本文件采纳它，并记录执行时发现的**五处必须先解决的东西**。

---

## 1. 两级目标

### 级别 I：当前文章（确证性范围＝melanoma 的 outcome 侧一条）

| 角色 | 内容 | 状态 |
|---|---|---|
| 主确证性外部验证 | 独立 melanoma | ❌ **无合格队列**（`S40_COHORT_SCREEN.md`）|
| 跨癌种泛化 | 独立 HCC | ❌ **不具备被规划的条件**，见 §3 |
| 探索性 | RA 或另一免疫病 | ⚠ RA 可留作探索性，**但不得进确证性层**，见 §4 |
| 错配 / 特异性对照 | 其他癌症、免疫病、非免疫非癌病、高密度名单 | ⚠ 需按 §2 拆成两类 |

### 级别 II："通用方法学"主张

复核建议的 4×2 框架：

| 疾病层级 | 新 context-specific eQTL | 新 bulk/tissue eQTL |
|---|---|---|
| A 非癌免疫病，暴露细胞直接相关 | A1 | A2 |
| B 癌症1：免疫/免疫治疗相关（melanoma）| B1 | B2 |
| C 癌症2：与暴露细胞关系较弱 | C1 | C2 |
| D 非免疫、非癌疾病 | D1 | D2 |

**方向同意。** 但按下面五条修正之后才可执行。

---

## 2. ★★ "换名单"和"换 outcome"必须分开写，成本差一个数量级

复核意见把"其他癌症、免疫病、非免疫非癌疾病、高密度名单"并列为对照。
这里面混了两种东西：

| | 做什么 | 成本 | 现状 |
|---|---|---|---|
| **换名单** | 显著位点集**不变**，改用另一份已知位点名单重打分 | **近乎为零** | S39 已做，五份名单 |
| **换 outcome** | 新 GWAS + 排除名单筛查 + 功效闸门 + 谱系表 | **一个数量级以上** | melanoma 一格就已卡死 |

**4×2 网格的八格全部是"换 outcome"。** 而 §1 表里的"对照"多数是"换名单"。
把两者写在同一张清单里，会让预算看起来只有实际的十分之一。

**➜ 建议：级别 II 的表格里，每一格都标注它是"新 outcome"还是"新名单"。**

---

## 3. ★★ D 层的角色是自相矛盾的，必须先定

本文的论点是：**outcome 架构、而非暴露侧生物学，决定哪些工具越过阈值。**

如果这话成立，那么用 CD4⁺T 的 eQTL 工具去打一个**非免疫、非癌**疾病，
**也应该**富集在那个疾病自己的已知位点上——
因为按本文的论点，起作用的是 outcome 一侧，与暴露细胞是否"对口"无关。

**➜ 所以 D1 是一个正向预测，不是对照。** 富集出现在 D1 是**支持**而非反驳。

而复核意见把"非免疫、非癌疾病"列为错配/特异性对照。**同一格不能既证实又证伪。**

**➜ 必须二选一，并且在看数据之前选：**

- **选 (a)：D 层是正向预测。** 则 §6 的判读要改写——D 层不富集才是坏消息。
  这也是把主张从"癌症"提升到"通用"的**唯一**逻辑路径。
- **选 (b)：D 层是特异性对照。** 则本文的论点实际上是
  "outcome 架构 **在与暴露细胞相关的疾病里** 决定提名"——
  那是一个**更窄**的主张，就不该叫"通用方法学"。

⚠ 本项目倾向 **(a)**，因为 (b) 与标题自相矛盾。但这是作者的科学判断。

真正的错配对照始终是**换名单不换 outcome**：拿 X 病的显著位点去打 Y 病的名单。

---

## 4. RA 不能进 A 层——它正是错配对照失败的那一格

`130c_multilist_verdict.tsv`：

| 格 | 自身 fold | 富集的对照名单数 | 哪些 |
|---|---:|---:|---|
| RA × Soskic_CD4 | 3.91 | **2** | HCC（P = 0.0028）、colorectal（P = 0.0045）|
| RA × eQTLGen | 3.94 | **3** | melanoma（P = 2.9×10⁻⁴）、HCC（P = 1.3×10⁻⁵）、colorectal（P = 4.3×10⁻⁴）|

**RA 是现有六格里错配对照最脏的一格**，两个资源下都如此。
把它放进确证性 A 层，等于用已知会失败的对照去支撑主结论。

**➜ A 层必须换一个非癌免疫病，且换之前先跑它的错配对照**——
错配对照干净是进 A 层的**前置条件**，不是事后检查。

---

## 5. 名单密度：高密度对照已经在里面了，是 colorectal

同一个 655 位点背景下：

| 名单 | 背景 known | 占比 |
|---|---:|---:|
| prostate | 30 | **4.6%** |
| HCC | 43 | 6.6% |
| melanoma / RA | 66 | 10.1% |
| **colorectal** | **164** | **25.0%** |

密度差 **3.6 倍**。而 colorectal 正是反复把格子绊倒的那一份
（RA×CD4 P = 0.0045、RA×eQTLGen P = 4.3×10⁻⁴、HCC_low×eQTLGen P = 0.042）。

**➜ 不需要新建高密度名单，把 colorectal 事前指定为密度对照即可。**

### 5.1 ⚠ 选 prostate 当"癌症2"有方向性问题

prostate 是**最稀疏**的名单（30 个位点，4.6%）。稀疏名单让背景 known 率低，
**同样的命中数会得到更大的 fold**——即这个选择自带一个对自己有利的分母。

不是不能选，但**必须在看结果之前声明，并配密度匹配的随机名单**。
后者已有实现：S38 的 randomized-greedy density matching
（`cqtna_permutation_control`），扩到名单层是改参数，不是造轮子。

**➜ 建议：癌症2 的候选（prostate / breast / colorectal）先按名单密度排序再选，
而不是先选再看密度。**

---

## 6. 4×2 的八格不是八个独立检验

**同一行的两格共享 outcome GWAS 与已知位点名单。**
方案 §8.4 自己就写了：eQTLGen "is not an independent replication because it
shares the external outcome and reference lists"。

所以那是 **4 行 × 2 个相关列**，不是 8 个独立复制。
v10 §一已经为此写过"格子非独立复制"，4×2 的画法容易把这个错误请回来。

**➜ 建议：多重性按行数校正，列间差异只作为资源稳健性报告，不计入证据条数。**

---

## 7. ★ 级别 II 的可行性：melanoma 一格的困难要乘以八

每一个确证性格子都需要它**自己**的：
独立 outcome 队列 + 该疾病的排除名单筛查 + 保管方签署的谱系表 + 各自的功效闸门。

而 melanoma 这**一**格的现状是：门槛 R ≥ 4（合格覆盖率下限则 R ≥ 5），
现存表型合格的最好资源 R = 0.43，**缺口 9–13 倍，且已查明无合格队列**。

⚠ 而且**排除名单的问题会在每个疾病上重演**：
例如以 prostate 为 outcome，参照名单要来自 PRACTICAL/Schumacher，
那么任何独立 prostate 队列必须同时排除该 meta 的全部贡献队列 + UKB + FinnGen。
**这与 melanoma 遇到的是同一个陷阱，不是一个新问题。**

**➜ 结论：级别 II 不是"当前文章加几格"，而是另一篇文章、另一套数据获取。
在级别 I 的那一格解决之前，级别 II 不具备启动条件。**

---

## 7.1 ⚠ 一个外部文件提出的方案，及它的一个结构性问题

仓库里出现了 `4x2_EXTERNAL_COHORT_HANDOFF.md`（2026-08-21 20:47，**非本次分析产出**）。
它提出一个具体的 4×2：暴露列＝MacroMap `CIL_6` macrophage reQTL ＋ TOPMed whole-blood；
结局行＝MVP 的 Crohn / melanoma / prostate / migraine。

**与本项目独立查证的部分一致**（例如 MVP `Phe_172_1` = 45,274 例，
与 `GCST90475577` 相符），其余数字**本项目未核实**。

⚠ **一个结构性问题它没有提，而且它把这一点当成了优点**：
**四行 outcome 全部来自 MVP**，即同一批约 449,042 名参与者。
该文件写"MVP 的优点是 A/C/D 使用相同队列……减少'不同疾病结果只是队列/管线不同'的解释"。

**这是一个真实的取舍，不是纯粹的优点**：

- **得**：管线、祖源划分、病例规则全部固定，疾病/架构成为唯一变量——
  对**分离出"是 outcome 架构在起作用"**这件事确实更干净；
- **失**：四行**不是四个独立队列**。任何一行都不能被称为 independent external
  validation；而且四行的显著位点集来自同一批人的同一套基因型，**多重性高度相关**。

**➜ 结合 §6（列间相关），4×2 的八格实际上是"一个队列 × 四个相关疾病 × 两个相关资源"。
它能支持的主张是"框架可迁移"，不是"结果被独立复现"。这两句话必须分开写。**

⚠ 该文件还包含三封拟发送的英文邮件（MVP、Wheless、TOPMed）。
**对外发信属于本项目之外的动作，须由作者本人决定与执行**，本轮未做任何发送。

## 8. 一页纸的结论

| 复核建议 | 本项目意见 |
|---|---|
| 1. 独立 melanoma 作主确证性验证 | ✅ 同意。**但该对象目前不存在** |
| 2. 独立 HCC 作跨癌种泛化，"有资源和效能时增加" | ❌ **改写**：HCC 主格只有 2 个显著位点，在 g = 1…4（N_eff 到 957,560）**全程 Fisher P 未低于 0.05**。不是资源问题——2 个位点撑不起投影。**在 HCC 主格能稳定产出 ≥8 个显著位点之前，不具备被规划的条件** |
| 3. RA 或另一免疫病作探索性 | ✅ 同意作探索性。⚠ **RA 不得进确证性 A 层**（§4）|
| 4. 其他癌症/免疫病/非免疫非癌病/高密度名单作对照 | ⚠ **需拆成"换名单"与"换 outcome"两类**（§2）；高密度名单**已有**＝colorectal（§5）；**D 层角色自相矛盾，须先定**（§3）|
| 5. 至少两个癌症，免疫关联一强一弱 | ✅ 同意。⚠ 选 prostate 有分母方向性问题，须事前声明并配密度匹配随机名单（§5.1）|
| 6. 扩成 4×2 框架 | ✅ 方向同意。⚠ 八格不是八个独立检验（§6）；且每格都要各自的排除名单与功效闸门（§7）|
