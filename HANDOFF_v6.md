# 项目交接文档 v6

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-14（Step 118 后，第四轮审稿意见已全部处理）
**取代**：`HANDOFF_v5.md`（其第五节技术坑、第六节方法论纪律仍有效，本文件继承并扩充）
**本轮 commit**：`67de114` → `519789b`，共 **15 个**，工作树干净

> **新会话阅读顺序**
> 1. 本文件（尤其 §三：还剩什么）
> 2. `manuscript/MANUSCRIPT_GB.md`（**投稿母稿**，现含完整 Methods 与 References）
> 3. 全文源 `manuscript/MANUSCRIPT_v2_dual_thread.md`（**唯一正文源**）
> 4. **十四份预注册**（新增 S34、S35）
> 5. `NEXT_PROJECT_myeloid.md`（本篇之后的方向，已定稿）

---

## 一、当前定位（一句话）

**单变异 eQTL-MR 产出的是"在这份数据条件下通过筛选的候选"，不是稳定靶点；
其可复现的部分由已知结局位点、阈值邻近性与基因/区室错归属决定。**

⚠ 本轮新增的边界：**这一结论只在两个暴露资源上成立**
（Soskic CD4 时程、eQTLGen 全血）。**尝试扩到第三个资源失败**（§二 S35），
且该失败有结构性原因——**CD4 的"动态"这一维在公开数据里只由一个资源支撑**。

---

## 二、本窗口做了什么（Step 109–118）

### A. 第四轮审稿的 §九 前五项：**全部完成**

| 项 | 结果 |
|---|---|
| 1 全文搜因果动词 | GB/NC/全文源三份都扫。章节标题与诊断 (iii) 不再把 MR/coloc 背离归因于功效 |
| 2 统一网格口径 | 主网格 = melanoma/HCC-high/RA × 2 资源 = **六格跨三疾病**，HCC-low 降为功效敏感性 |
| 3 恢复 accepted-gene 限定 | GB/NC 都加回；并**改为并报严格/宽松两个口径** |
| 4 图号 + 注释 + Methods + References | 全做；Methods 约 2,900 词（含 S28–S33），References **1–46** |
| 5 摘要重排 + TPI1 压缩 | 摘要 **435 → 382 词**，改以 HEIDI 253/291 与 MC1R 自检开头；TPI1 压约三分之一 |

### B. 顺带查出并修掉的既有错误（**都不是审稿人提的**）

1. **网格显著格数少算了**：HCC-low × eQTLGen 的 P=0.017 < 0.05，
   故六格中**四格**达显著，不是三格。错误源于 S24 §8.1 一句含混措辞。
   预注册原文保留 + 加日期更正批注（更正使结果**更强**，已写明以免被读成事后择优）。
2. **一处句子被 RA 段落插断**（`literature.` 一词被吃掉），已修复并把 RA 前移为第三疾病。
3. **图目录错位比三幕重排更早**：self-attribution check **从未**被正文引用，
   TPI1 时序**从未**进过目录。现九图内部自洽。
4. **诊断 (v) 与自己的正文段落矛盾**（写"组织测量就是肿瘤测量"，而正文明确撤回过），已改。
5. **已发表的 6/10 是宽松口径**，严格口径为 **2/10**（仅 IRF4、MX2）。现并报。
6. **`96a` 无生成脚本、三列基因被截断** → `step111` 重建，
   **恢复出的口径**：位点提名 = 该位点上 FDR<0.05 **且自身工具变量落在已知 lead 1 Mb 内**的基因。
   验证 20/20 与 17/17 吻合，**6/10 得到确认**。

### C. C 组三项：**全部有了明确结论，但都没能把分数推过 9**

| | 结论 |
|---|---|
| **C3 外部归属基准（S34）** | **做不成**，落 E 格。可评位点仅 16（性状匹配仅 2），低于预注册下限 30。⚠ **约束是覆盖不是循环**——排除共定位证据类只损失 2 个位点 |
| **C1 第二个动态资源（S35）** | **缺口未闭合**。唯一达门槛的 DICE **负对照失败**（错配 HCC 名单 6.58× P=0.033 > 匹配名单 3.36× P=0.11），按 S24 判据作废 |
| **C2 多信号/条件化 coloc** | **未动**。已知硬障碍：FinnGen 对该 endpoint 只精细定位 19 个区域，**候选位点一个都不在其中** |

### D. 两个可写进正文的副产品

1. **该领域缺乏黑色素瘤的策展位点→基因真值**：
   Open Targets 2,435 条里只有 40 条涉黑色素瘤相关性状，且 1,755 条是药物靶点类
   ——真值跟随的是**治疗关注度**而非等位基因结构。
2. **★ CD4 的动态轴只由一个资源支撑**（`step117` 扫全部 466 个数据集）：
   多条件刺激 eQTL 资源**存在但全在髓系**（MacroMap 24 条件 n=199、
   Quach 4 刺激 n=198、Nedelec、Nassiri），
   **CD4 只有 Cytoimmgen/Soskic 一个**，其余带刺激的 CD4 资源都是单一条件。
   → 已写进 Limitations，并成为下一课题的起点。

---

## 三、★ 还剩什么（下一轮的工作）

### A. 投稿前必须做（都不需要新分析）

1. **⚠ GB 图目录与实际图件文件对不上。**
   `figures/` 里没有 "instrument availability at three levels"、
   "self-attribution check"（已从目录移除）对应的文件，Fig 2 的"泛化"也没有单一文件承载。
   **这不是编号问题，是图件本身没按 GB 的九图口径导出。躲不掉。**
2. **ORCS release 号**须与正文 1,471 这个分母核对（条目 40 已核实著录，release 未核）。
3. **DepMap release 号**须与脚本核对（条目 39，v5 就欠着）。
4. **References 有 7 条未被 GB 正文引用**（17、18、32、33、36、37、38）。
   若期刊不允许列未引用文献，须删除或在方法学补回相应句子。见 `REFERENCES.md` 末尾。
5. **NC 的正文引用密度过低**（全文只引 2 条）。NC 是次要格式，但投稿前须补。

### B. 应该做但不紧急（v5 §三-D 的残留）

- **1 Mb 窗口敏感性**：100/250/500 kb 各跑一遍；报**连续距离分布**而非二分类。**仍未做。**
- **跨格综合统计**：S35 §3.1 已**预注册** CMH + 随机效应 meta + I²，
  但因合格格子只有一个而**无法运行**。若日后有新格子，设计现成。

### C. 若要再冲一次分数

**C2 是唯一没试过的。** 但须先解决 LD 参照系：
自跑 SuSiE 只有 proxy LD，在 MC1R 上已被证明会过度拆分（19 vs 3），只能单向解读；
而 FinnGen 的 in-sample credible set 到不了候选位点。
**没有新的 LD 来源就不要开工**，否则会重蹈已知的失败。

---

## 四、审稿人认可的强项（不要在压缩中砍掉）

- **MC1R 自我校准**：框架在最明确的位点都可能不命名 MC1R
- 候选名单完全翻转、known-vs-novel 恢复率差异
- 预注册 + 错误披露 + 多层反证
- RA 排除 MHC 后仍稳健

---

## 五、技术坑（v5 第五节之外的新增）

30. **eQTL Catalogue API 全端点 500**（`/api/v2/*` 与 `/api/*` 皆然），
    但 **api-docs 页与 FTP 树可达** → 是 API 后端故障。
    **绕法**：其元数据另有静态副本
    `github.com/eQTL-Catalogue/eQTL-Catalogue-resources/data_tables/dataset_metadata_r8.tsv`。
31. **WebSearch / WebFetch 工具在本会话全程不可用**（harness 侧模型配置错误），
    但 **Python 直连正常**（NCBI E-utilities、CrossRef、GWAS Catalog REST、GitHub、EBI FTP 全通）。
    → 检索受阻时先分清是工具坏了还是网络坏了。
32. **eQTL Catalogue 的 `permuted.tsv.gz` 是 0.4 MB，`all.tsv.gz` 是 1.7 GB。**
    每基因一个 lead 变异，正是工具变量选取所需。**不要下 all。**
33. **新研究的 sumstats 常未发布**：元数据表里有，FTP 上 404
    （IBDverse QTS000045、MacroMap QTS000059、Nassiri QTS000058）。
    规律：**QTS 编号越新越可能没有**。
34. **Windows 控制台 GBK 编码**会让含 `\xa0` 等字符的 print 崩掉。
    Tee 类须写成 `s.encode(enc,"replace").decode(enc,"replace")`。
35. **eQTL Catalogue 是 GRCh38**，与本文结局同坐标系 → 按 chr:pos 匹配，
    不要用 eQTLGen 那条 rsID 变通（那是因为 eQTLGen 2019 是 GRCh37）。

## 六、方法论纪律（v5 第六节之外的新增）

26. **"跑之前的门槛检查"真的会救人。** S35 §3.2 的循环性手查查出的不是循环性，
    而是**我自己在 S35 里写错的两处**（Nathan 不是刺激资源、网格不再全欧洲血统）。
    **读原文，不要读数据集标签。**
27. **负对照失败就是失败。** DICE 那一格倍数 3.36、方向对，
    但错配名单给出 6.58 且显著 → 作废。
    ⚠ 分子只有 2/6 时，两份名单各命中 2 个而不是同一对，**这就是噪声的样子**。
28. **事先写好的失败分支，实际失败时往往不是那一支。**
    S34 事先准备的失败理由是"真值受 eQTL 污染"，实际是"覆盖不足"。
    **必须按实际原因写，不得套用预备好的说法**（S34 §9.5 已单列）。
29. **不要把"某类里没有"推广成"没有"。** 我写过"没有多条件刺激 eQTL 资源"，
    扫描后发现存在且不少，**只是全在髓系**。更正后的陈述比原来强得多。
30. **元数据表之间会互相矛盾。** Nathan 的 n，API 旧读数 248/232、r7 与 r8 均为 147/42。
    两份静态表一致时取静态表，**但冲突要记录不要抹去**。

---

## 七、文件地图（本窗口新增）

**预注册**：`PREREG_attribution_benchmark.md`(S34) ·
`PREREG_multiresource_attribution.md`(S35)
**脚本**：`step109`–`step118`
**结果表**：`109a`–`109b` · `110a`–`110d` · `111a` · `112a` · `113a`–`113c` ·
`114a`–`114b` · `115a`–`115b` · `116a`–`116c` · `117a` · `118a`
**其他**：`DYNAMIC_EQTL_SURVEY.md`（C1 侦察，已两次更正）·
`NEXT_PROJECT_myeloid.md`（下一课题方向，已定稿）

---

## 八、当前字数

| | 词数 | 摘要 | 状态 |
|---|---|---|---|
| **GB** | **7,886** | **382** | 正文在 6,000–8,000 内 ✅；摘要超目标 350 约 32 词 |
| NC | 3,809 | 148 | 均达标 ✅ |
| assembled | ~28,095 | — | 由 `assemble.py` 生成，**永不手改** |

⚠ GB 正文贴着上限，**再加内容须同时减内容**。

---

## 九、下一轮建议顺序

1. **图件导出**（§三-A1，唯一真正的拦路虎，且需要跑图脚本）
2. **References 的三处收尾**（未引用条目、ORCS release、DepMap release）
3. **NC 补引用**
4. **1 Mb 窗口敏感性**（§三-B，v5 就欠着）
5. 然后再决定要不要碰 C2——**没有新 LD 来源就别开工**
