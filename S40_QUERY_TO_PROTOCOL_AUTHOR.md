# 致 `EXTERNAL_VALIDATION_PROTOCOL_v9_3` 起草者：执行时遇到的两处需要澄清

**日期**：2026-08-21
**发信方**：本项目（S40 的采纳方）
**关于**：`EXTERNAL_VALIDATION_PROTOCOL_v9_3.md` §7 与 §6
**状态**：⚠ 在这两处澄清之前，§7 的 N_eff 门槛**不是一个确定的数**

---

## 0. 先说清楚这封信不是什么

**不是**要求修改方案的范围、成功标准或任何冻结项。
S40 §2.1 明写本项目不得那样做，本文件也不那样做。

**是**：本项目试着**执行** §7，发现它有两处按字面无法唯一执行；
两处都需要**起草者自己**补一句话，因为补的那句话是设计选择，
由采纳方来补就等于采纳方在改设计。

全部证据在 `S40_POWER_GATE_SPEC.md`、`132a`、`133a`、`134a`、`135a`，
代码在 `step132`–`step135`，规则与结果分提交、先后可由 git 核验。

---

## 1. ★★ §7 没有规定外部效应估计怎么抽

### 1.1 原文

> For each candidate validation cohort, external standard errors are projected as
> `se_ext = se_disc × sqrt(N_eff,disc / N_eff,ext)`.
> Ten thousand complete-pipeline simulations are run under effect-retention
> factors of 0.50, 0.75 and 1.00 …

§7 规定了 **se 怎么投影**、规定了三个 retention 因子、规定了跑一万次，
**但没有一句话说 `beta_ext` 是怎么来的**。两种自然读法：

| | 模型 | 随机性来源 |
|---|---|---|
| **读法 A** | `beta_ext ~ Normal(f · beta_disc, se_ext²)` | 抽样噪声 + 覆盖率 |
| **读法 B** | `beta_ext = f · beta_disc`（确定性）| **只有**覆盖率 |

### 1.2 读法 A 通不过自校准

在 **f = 1.00、R = 1.00** 下——即外部队列与发现集**效应量相同、样本量相同**——
读法 A 预测**中位 102 个显著 bounded locus**。本研究实得 **8 个**。

原因是它把含噪的 `beta_disc` 当作真值又在其上加一层噪声，
于是 Var(z_ext) = Var(z_disc) + 1。发现集里绝大多数记录本就是噪声，
这一步把噪声方差翻倍，尾部被人为撑开。

### 1.3 读法 B 自洽，但不是一个功效计算

读法 B 在 g = f·√R = 1 时**逐项复现**发现集：
ST = 8、SK = 4、fold = 4.962、P = 0.0048407（`step133` 的自校准断言）。
两个独立的 g = 1 格子（f = 0.5/R = 4 与 f = 1.0/R = 1）返回完全相同的输出，
说明判定确实只取决于 g。

**但读法 B 没有抽样方差**，于是 §7 的"至少 80% 的模拟通过"
只能靠覆盖率的波动来兑现——那不是功效，那是覆盖率的敏感性。

### 1.4 我们认为正确的做法，以及为什么不由我们做

统计上正确的是**第三种**：先从发现集的 z 估计真实 non-centrality 的分布
（两组模型 / 经验贝叶斯收缩），再从该分布抽 `z_ext ~ N(f·√R·θ, 1)`。

**这是一个设计选择**，S40 §2.1 不允许采纳方单方面做出。
**➜ 请起草者补一句话，指定 §7 用哪一种。**

### 1.5 两种读法给出的门槛（f = 0.50，全覆盖）

| 读法 | 通过所需 R | N_eff | 对照池极大时约等于病例数 |
|---|---:|---:|---:|
| A | ≈ 2.0 | ≈ 98,700 | ≈ 24,700 |
| **B** | **4.0** | **197,348** | **≈ 49,300** |

⚠ **不论用哪一种，结论都不变**：现存表型合格的最好独立资源是
MVP 的 5,364 例组织学确诊侵袭性病例，**R = 0.43**。两种读法都够不着。
这封信影响的不是"能不能验证"，而是**将来拿到队列时门槛定在哪里**。

---

## 2. ★ §6 第 3 条在高功效下会自我否定

### 2.1 现象

主终点是"显著位点集"相对"全部可检验位点"的富集。**功效越大，显著集越接近背景，
fold 必然趋向 1。** 读法 B 下逐格可见（全覆盖，melanoma × CD4）：

| g | 显著位点 | fold | Fisher P | §6 判定 |
|---:|---:|---:|---:|---|
| **1.000** | **8** | **4.962** | **0.00484** | ✅ ← 发现集本身 |
| 1.414 | 120 | 2.233 | 4.7×10⁻⁶ | ✅ ← P 的最低点 |
| 2.000 | 326 | 1.248 | 0.0232 | ✅ |
| 2.828 | 463 | 1.136 | 0.0444 | ✅ 勉强 |
| **3.000** | 477 | 1.123 | **0.0527** | ❌ ← 越线就在这一格 |
| 4.000 | 538 | 1.051 | 0.223 | ❌ |

**合格窗口是 g ∈ [1.00, 2.83–3.00]，即 R ∈ [1/f², 8.0–9.0/f²]。**
f = 0.50 时是 R ∈ [4, 32–36]。**队列可以大到不合格。**

### 2.2 §6 的写法看不出这件事

§6 第 3 条只写 "the primary fold enrichment is greater than 1 with one-sided
Fisher P < 0.05"。读者（包括我们）的直觉是"过了下界就一路通过"，**那是错的**。

**➜ 请起草者确认：这是刻意的（把过大的队列排除在外），还是应当在 §6 补一个上界说明。**
若是刻意的，请说明理由，因为它会让"更大的队列更好"这个常识失效。

---

## 3. ★ 附带发现：§9 的 HCC 扩展臂在任何样本量下都不通过

§9 允许独立 HCC 作为第二阶段单独预注册，并要求它 pass its own eight-locus gate。
用同一投影扫 g = 1 到 4（f = 0.50 下 N_eff 从 59,848 到 957,560）：

**八位点闸门从 g = 1.25 起就过了，但 Fisher P 全程没有一次低于 0.05**
（0.123、0.221、0.138、0.246、0.145、0.545、0.430、0.176、0.254）。
HCC 的 fold 从 2 个位点上的 7.878 掉到 14 个位点上的 2.251，之后贴着 1.2。

⚠ **正确的读法不是"HCC 会失败"**，而是：**2 个显著位点撑不起一个投影**，
整条轨迹由"当前哪几个位点恰好卡在阈值下方"决定。
**➜ 我们的判断是：在 HCC 主格能稳定产出 ≥8 个显著位点之前，
§9 的扩展臂不具备被规划的条件。请起草者确认或反驳。**

---

## 4. 我们已经做完的、不需要回信的部分

| S40 §3 前提 | 状态 |
|---|---|
| 合格外部队列 | **已查明不存在**，见 `S40_COHORT_SCREEN.md`。§2 的取数路径 (ii) 结构性作废 |
| 锁定容器与 SHA-256 清单 | **部分完成**，见 `S40_LOCK.md`：49 个文件已散列并先于任何外部统计；**容器镜像与 `renv.lock` 仍缺**，需作者/机构 |
| 公开注册取得 DOI | 未做，需作者 |

---

## 5. 一句话

**这两处澄清不改变"目前没有合格队列"这个结论**，
但**在拿到队列之前必须解决**——否则门槛会在看过数据之后才被确定，
而那正是本项目反复栽过的那类错误。
