# HANDOFF v14 — 2026-09-11

**仓库**：`D:\R_ex\MR`（git，`main`，**未 push，且尚未配置任何远端**，259 个提交）
**上一份**：`HANDOFF_v13.md`

---

## 0. 下一个窗口第一件事

**请作者按 S54 §9.11 重新确认 §9.6 的裁定。**

§9.6 的 (b) 裁定作于 2026-09-10，**其依据的覆盖率数字是错的**（41/41）。
正确数字已算出：**131/191 = 68.6%**（区间 68.6–69.1%），**高于 §8 的 50% 线**。
§9.6 里那一行已就地更正，并标明「裁定是否维持由作者按 §9.11 重新确认」。

⚠ **不要只看 68.6%。** S54 §9.11.2 完整登记了这个数字是怎么得出来的，
包括走错的两步。作者要确认的是那个过程，不是那个数字。

---

## 1. v13 §1 那个悬案已了结，但答案和预想的不一样

**42.9% 从来就不是在度量容器。**

第 7 组的 glob 从 `step1[2-5][0-9]` 扩到全仓库 `step*`（26 → 141 个候选脚本）后，
**覆盖率一个数都没动**，仍是 82/191。原因：

**158 个 `step*` 脚本里有 102 个写死 `MR = r"D:/R_ex/MR"` 且无任何覆盖。**
在容器内它们**败在路径上，不是败在数据上**。

| 区间 | 可移植 | 写死 |
|---|---:|---:|
| 6–99 | 16 | 79 |
| 100–119 | 1 | 20 |
| 120–161 | 39 | 3 |

**旧 glob 几乎恰好描出了这条可移植性边界**——它一直在报告「哪些脚本碰巧可移植」。

⚠ **v13 §1 用来论证 50.8% 的 `step53`/`step55`/`step104` 就在这 102 个里。**
本轮实测三者全部败于 `D:/R_ex/MR/13_meta_locus_annotation.tsv`。
**那三次「跑通」是在 Windows 上跑的**——与 v13 §1 自己指出的
「把已审计当成已重算」是同一类替换，**第三次**。

**作者授权修改路径解析后重跑**（S54 §10.3）。修完 102 个脚本重跑：
第 7 组跑通数 **50 → 102**，覆盖率 **131/191 = 68.6%**。

---

## 2. 本轮四处「做出来的检查本身是错的」

这是本轮最该带走的东西，四处全部是**检查通过了、但验的是别的东西**。

### 2.1 ⚠ 我报「已验证」的路径修改里有两个脚本是坏的

对 `git diff -U0` 的机械检查只放行三种行，报告**99 个文件、0 行越界**，
我把这个当成了验证。**它验的是被改行的形状，没验 `argv[1]` 原本是不是已有含义。**

- `step91_literature_audit.py`：`src = sys.argv[1]` 是 PMID 清单路径
- `step21_spatial_LR_coloc.py`：`argv[1]` 是 LR 表，`argv[2]` 是标签

两者都会静默把文件名当成仓库目录。
⚠ **我随后的第一次手工排查又漏掉了 `step21`**，因为它那行含有过滤条件
用来排除 MR 行的同一段子串。

**已修**：检查搬进 `step159` 的 `argv_guard()`，跑第 7 组之前报出任何
复用了自己取 MR 那个下标的脚本；已用构造冲突（被报出）与其修正版（未被报出）验证。
`step91`/`step21` 改用 `argv[2]`/`argv[3]`。

### 2.2 `step161` 两处系统性低估，且**每一处都朝同一个方向**

1. 表 `85e_…` 的产出脚本被按数字截成 `85`，运行记录的却是 `85e` →
   `step127` §1 的 17 个数被误记为「未重算」。
   ⚠ 修完把 C 从 82 推到 96，即 42.9% → **50.3%，恰好越线，只多一个数**。
2. 九个 section 不调用 `_find`（复用模块级 dataframe）→ 被记为「无来源表」。
   改为同时解析 section 标题里作者自述的出处；**解析不出的单列一档并给出区间**。

**我在第 2 处之后有意停止继续修度量工具。** 每一处缺陷都把数字朝同一个方向推，
而它们都只在「数字不够」之后才被去找——**这个搜索方向本身是偏的**。
68.6% 的可信度来自范围扩大后的真实执行，不来自继续修工具。

### 2.3 `WATCH` 没跟上 `step127` 从 41 个数扩到 174 个

`watch_guard()` 报出**18 张被对账、却从未被阶段 4 比较的表**，其中 **10 张真的被重算了**。
⚠ 那十张里若有一格移动，验收照样通过而那个数不会被报告。

**核实方式**（不能追溯比较，`159d_baseline/` 只有原始 18 张）：
十张表最后一次提交在 2026-08-19/24，**早于任何容器存在**，运行后未被修改
→ **在作者机与容器之间逐字节相同**。`WATCH` 已补全，本轮报告 29/29 全覆盖。

### 2.4 上一轮 `step52` 记为「跑通」是假象

它当时能找到 `GSE199994` 的 P4–P8，**只因为 `step51` 刚把 20.8 GB 下载进了仓库**。
排除 `step51` 后它如实报 `[missing] P4…P8`。**那次的覆盖率 credit 是下载的副产品。**

---

## 3. 两处「重跑即销毁证据」

### 3.1 `94d_grid.tsv` —— 本轮最有价值的单个发现

`step94b` 查**实时 GWAS Catalog**，`step94c` 据此重建 `94d`。
但 `94d` 不是活结果：`PREREG_generality_grid.md` §8.2 已判其 lung/colorectal
两行**作废**，而 `step119` 把这两行**原样**抄进重建网格标为 `void_bad_coords`，
旁边配一句固定说明——「背景只有 2/559 与 5/559，而 melanoma 是 58/554」。

按今天的 Catalog 重跑，两行回来时 `bg_known` 是 **70 与 203**。
**实测**：跑完 `step119` 得到的 `119a` 里，作废行带着更正后的数字，
而旁边那句话仍写着 2/559 和 5/559——**该行反过来否证了它自己被作废的理由**。

⚠ **是字符串顺序把它藏住的**：`step119` 排在 `step94c` 之前，
所以读到的是运行前的 `94d`。**换个顺序跑的读者会拿到自相矛盾的表。**
`94d`/`94e`/`119a` 已恢复，`step94b`/`step94c` 已排除。

### 3.2 `step51` 把 20.8 GB 下进了仓库

`D:/Downloads` 在镜像内解析为 `/repo/D:/Downloads`。磁盘当时只剩 14 GB。
**执行者在 1942 秒时终止了它**（记 `rc=-15`，是终止不是自然失败）。
每个文件都与作者机 `D:/Downloads` 下同名文件字节数相同，已删除。
`step33`/`41`/`51` 已排除。

⚠ **未**把 `D:/Downloads` 挂进镜像：S54 的前提是读者只有这个仓库。

---

## 4. 评审四条：全部处理完

| # | 意见 | 状态 |
|---|---|---|
| 1 | 41/41 不能证明全文覆盖率 | ✅ S54 §9.6 就地更正 + §9.11 新登记区 |
| 2 | 「无印出数字变动」需明确基准 | ✅ §9 开头加统一更正框，§9.6/§9.10.6 两行就地改；原文按 §9.4 既定做法保留 |
| 3 | 两包漏锁属「尚未修复」 | ✅ 实为**三包**（含 `testthat`）。新建 `150c_unlocked_packages.tsv`，installer 按版本钉死，闭包报告拆成两份 |
| 4 | `CONTAINER.md` 指引冲突 | ✅ L13/§4b/构建段全部改写；另修 §5 与缺失的 `--reuse-baseline` |

⚠ **第 3 条的诚实边界**：钉版写进了构建配方，**手头这个镜像早于它**。
已实测 `cqtna-audit:0.3.0` 内三个版本全部与 `150c` 相符、Bioc release 也相符，
**故该记录对这个镜像成立**；重建才能让钉版在安装时被**强制执行**。
`step159` 现在会明说自己看到的是哪一种——**缺报告和报告通过不能读起来一样**。

---

## 5. 仍未完成

1. **§9.6 裁定的重新确认**（见 §0）—— 最高优先级，只能作者做
2. **作者身份 20 处占位符**：姓名/邮箱/ORCID/GitHub、Zenodo DOI、四条声明
   （`python step152_set_identity.py` 列出全部位置）
3. **DOCX/PDF 逐页视觉检查**（Reviewer 3 硬门槛，仍未做）
   - ⚠ `MANUSCRIPT_GB.pdf` 停在 08-27，比 DOCX 还旧
4. **重建镜像**以强制执行 `150c` 钉版（数小时，非阻塞）
5. **配置远端并 push**：259 个提交，**目前连 remote 都没有**
6. 冻结、存档（版本号与哈希）

### ✅ 本轮已解决的旧遗留

- **DOCX 已重建**（09-11 19:22）。已逐项核验含
  `13.53→12.09`、`0.185→0.184`、`6.49→6.55`，且 TwoSampleMR 只剩 L1053
  那一处**公式引用**（本就该保留），软件清单里的版本号条目已无。
  9 张图全部嵌入，无 `[anchor missing]`/`NaN`/`undefined` 残留。
  ⚠ `MANUSCRIPT_GB.docx` 与 `blocks.json` 都在 `.gitignore` 里（可重建产物）。
- `REVIEW_BRIEF_v14.md` 的 8,752 → 8,517；另修摘要 293→291、图注 1,299→1,273、
  「~8,600 是地板」（实测 8,517 已低于它）。`COMPRESSION_PLAN.md` §5 标题 8,519 → 8,517。

---

## 6. 环境与操作

### Docker

⚠ **本轮又踩了 v13 §4 记的 AF_UNIX 套接字孤儿**，照 v13 的办法解决了：
把 `C:\Users\a1197\AppData\Local\Docker\run` **整个目录改名**，Docker 会重建。
（本次 `docker-secrets-engine\engine.sock` 不存在，只清 `run` 就够。）

Docker Desktop 可执行文件在
`C:\Users\a1197\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`，
**不在** `C:\Program Files\Docker`。

⚠ **机器睡眠会让 WSL2 虚拟机一起挂起**：本轮容器内进程 `etimes` 只有 2.5 小时，
而墙上时钟过了 11.5 小时。**看起来卡住不等于卡住**——
用 `docker exec <id> ps -eo etimes,pcpu,args` 看容器内的真实耗时。

### 跑验收

```
docker run --rm -v "D:\R_ex\MR:/repo" -w /repo cqtna-audit:0.3.0 \
    python3 step159_container_acceptance.py /repo --reuse-baseline
```

⚠ Git Bash 会把 `-w /repo` 改写成 Windows 路径而报
`working directory ... is invalid`。加 `MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*'`。

⚠ **必须带 `--reuse-baseline`**，理由见 `CONTAINER.md` §4b（本轮已补进去）。

### 常用命令（前五个当前全绿）

```
python step127_audit_manuscript_numbers.py .   # 正文数字 ↔ 表（174 个，约 95 秒）
python step153_prereg_inventory.py .           # 预注册计数（十七份）
python step154_crossref_audit.py .             # 交叉引用/图注/文献
python step155_renumber_references.py .        # 文献顺序（只报告）
python step160_audit_coverage.py .             # 审计覆盖率
python step161_container_number_coverage.py .  # C/A，带「来源未确定」区间
python step152_set_identity.py                 # 残留占位符（20 处）
```

重建 DOCX：

```
cd manuscript && python parse_md.py MANUSCRIPT_GB.md blocks.json && node build_docx.js
```

---

## 7. 给下一个窗口的三条提醒

**① v13 的两条仍然成立，且本轮又各犯了一次。**
「先找产出脚本，再下结论」——`94d` 那条线索（`step119` 抄作废行）
一直写在 `HANDOFF_v7.md` 和 `FIGURES_GB_mapping.md` 里。

**② 做完一个检查，先问它验的是不是你以为的那个东西。**
本轮四处（§2）全部是「检查通过了、但验的是别的」。
`git diff` 形状检查报 0 越界，而两个脚本是坏的；`step127` 全绿，
而 18 张表根本没被比较过。**「绿」的含义要能说出来，说不出来就是没验。**

**③ 在门槛附近，注意自己搜索的方向。**
每一处被找出来的度量缺陷都把数字朝同一个方向推，
而它们都只在「数字不够」之后才被去找。**该停的时候要停**，
并把时序写进登记区——这正是 S54 存在的理由。
