# 第三方评审简报 v13

**日期**：2026-08-26
**手稿**：`manuscript/MANUSCRIPT_GB.md`（全文 15,687 词；主文 8,896；图注 1,299；摘要 376）
**投稿件**：`manuscript/MANUSCRIPT_GB.docx`（US Letter，双倍行距，连续行号，九图各占一页，含完整图注）
**仓库**：`D:\R_ex\MR`（git，`main`，**未 push**）。本版 `dd4e37f` → `3f0ab60`，共 **8 个提交**
**上一版**：`REVIEW_BRIEF_v12.md`（2026-08-24）

---

## 0. 这一版是什么

**中心主张与 v12 完全相同，一个字没动。** 本轮做的是三件别的事：

1. **量了一个此前只靠断言的东西**——恒等式在被审计文献里覆盖多大范围（§2.1）；
2. **把三个"正文引了但对不上账"的数接回了表**（§2.2）；
3. **修了论文的装置层**：图注、交叉引用、参考文献、声明段。这一层此前是坏的，
   而且坏得比内容层更明显——**九张图有二十五个面板，一条图注都没有**（§2.3）。

第 3 点值得单独说一句：`step127`（数字审计）在发现这些问题的那天报的是 **CLEAN**。
它只查数字，而这一层的问题**没有一个是数字**。

---

## 1. 中心主张（与 v11、v12 相同）

1. **单变异 cis-MR 的检验统计量恒等于结局 GWAS 的检验统计量**
   （一工具 + 一阶 SE ⇒ `|z| = |β_out|/se_out`）。暴露决定符号、尺度、
   哪些变异有资格被检验；**不决定哪些显著**。
2. **4.96 倍的富集由结局自己已找到的位点承担。** 剔除后 1.98 倍 / P = 0.41
   （条件化背景 2.07 / 0.40；按共享 lead SNP 合并 5.28 / 0.014）。
3. **换估计量不改变这一点**——七个口径剔除后无一显著（最小 P = 0.213）。

⚠ 恒等式是初等代数，**本文不主张它是新数学**；主张的是它对提名名单的后果与量。

---

## 2. 本轮改了什么

### 2.1 ★ 恒等式的覆盖面：从断言变成有界的测量（新增 S51）

针对本文最有力的一条反驳——"恒等式只对单工具 Wald 成立，有能力的研究用 IVW，
所以靶子很小"——这是**关于文献的经验主张**，可以直接量，而我们直到被第四次质疑
才去量（`step151`，同 S23 的 154 篇缓存全文）：

| | 篇数 / 154 | |
|---|---:|---|
| 声明 cis 工具是单变异 | **71（46%）** | 覆盖面**下界** |
| 点名任一多工具估计量 | **130（84%）** | 逃脱者**上界** |
| 两者**同时**成立 | **65** | ← 关键 |
| 只谈 cis-eQTL 的 85 篇中声明单变异 | 48（56%） | |

**两个数不是互补的。** 点名 IVW **不等于** cis 提名名单是 IVW 产出的——那 65 篇
就是反例（在 trans 或放宽工具集上跑 IVW，而 cis 提名只用一个变异，是常见组合）。

⚠ **两个数都不是比率。** 词组匹配说不出一篇论文用哪个估计量产出了候选名单，
而那才是唯一要紧的问题。正文与 S51 都写明了这一点。

**这里最值得你攻的地方**：我们把范围说成"有界但未知"，是否已经足够诚实，
还是应该更进一步——比如抽 20 篇逐篇读方法与补充表，把 46% 这个下界收紧？

### 2.2 三个对不上账的数，现在能对账了

| 数 | 此前状态 | 现在 |
|---|---|---|
| "a model using \|z\| … reproduces **68–80%** of the gap" | `step101` 的 M2 块**算了但只 print 不落盘**，只存在于 S28 的散文表里 | 写出 `101d_zonly_model.tsv`；重跑 **PC 逐位通过、`101a`–`101c` 字节不变**；`step127` §1c 对账 |
| 恒等式覆盖面（§2.1） | 无 | `step127` §1b 按**正文渲染出的形式**对账（`71 (46%)`、`65 of the 130`） |
| 预注册文档数目 | 正文**自相矛盾**：两处 twelve、三处 fourteen，枚举 12 个编号 | **fourteen 是对的**；S34、S35（第十三、十四份，均 2026-08-14）从未被补进枚举。新增 `step153` 从文件头重建系列并对账 |

预注册那条顺带修了一个两头都错的指针：`Supplementary S12 and S18–S40` 把
S37/S38/S39/S40 这四份**自己声明不是预注册**的扫了进来，又漏掉奠基的 S9。
Methods 现在枚举十四份，并逐个点名那五份不是预注册的邻居与各自理由。

### 2.3 ★★ 装置层：此前是坏的

一次人工交叉引用普查（`step127` 报 CLEAN 的同一天）查出六类问题：

| 问题 | 规模 |
|---|---|
| 图注 | 九张图、约 25 个面板，**零条面板注**；正文从未提过任何一个面板 |
| 图的正文引用 | Fig. 5/6/7/8/9 **一次都没被引** |
| 补充材料 | S10、S11、S13、S16、S17、S19、S31 **正文够不着** |
| 参考文献 | 10 条列了从未引；且列表**不按引用顺序**（首次出现序是 1, 42, 44, 35, 45…） |
| 逐字重复 | 一句 35 词的 caveat 同时出现在 Results 与 Methods |
| 声明段 | **无作者、无单位、无通讯作者**；ethics / consent / competing interests / funding / contributions / acknowledgements **一条都没有** |

**共同成因是压缩**：`594ad9f` 与 `090e95a` 把整节移进补充材料，callout、
参考文献、指针全留在原地。全部已修。九条图注按脚本实际画的面板逐个写，
并把三条"不写就会被误读"的规则写进去：

- **Fig. 4b**：代理 LD 的 credible set 计数**只能单向读**——恰好 1 个是单信号的
  保守证据，多个**不是**多信号的证据（样本内 3 个 vs 代理 19 个）；
- **Fig. 7b**：增殖模块**必须**超过组成匹配零模型，否则零模型无信息——
  它是预设阳性对照，不是反例；
- **Fig. 9b**：跨病种治疗前 arm 是 4 应答者对 2 非应答者，**任何效应量都不可能**
  达到 P<0.05，这与"测了但不显著"不是一回事（S21 事前算出并登记）。

### 2.4 交付物

- `container/Dockerfile` + `CONTAINER.md`：⚠ **未构建过**，且 `CONTAINER.md` §3
  写明结果产自 Windows/mingw32/参考 BLAS，任何 Linux 镜像是 OpenBLAS，
  **容器锁版本不锁机器**。§4 的验收表五行全是 "not yet run"。
- `step152_set_identity.py`：一条命令填完维护者身份与 DOI 的 8 处散落占位符，
  并列出**任何**残留方括号（当前 21 处）。

---

## 3. 最希望你攻的四处

1. **§2.1 的两个界够不够。** 46% / 84% 都不是比率，我们说清楚了；但一篇论文
   究竟用哪个估计量产出候选名单，词组匹配答不了。**这个未知会不会大到
   让中心主张的适用范围站不住？**
2. **图注里的读法规则会不会被读成辩护。** Fig. 7b 那条尤其——
   "阳性对照必须超过零模型"在技术上正确，但读者可能读成"我们给超出的两个
   找了理由"。有没有更中性的写法？
3. **摘要 376 词、主文 8,896 词**（GB 摘要 250、正文 ~8,000）。
   `COMPRESSION_PLAN.md` 里还有候选，但再砍就是在决定
   **把多少"诚实装置"移出读者视线**。这是取舍，不是技术问题，想听你的判断。
4. **容器该是文档还是准绳**（`CONTAINER.md` §5）。(a) 论文数字保持 Windows 产出，
   容器供复跑；(b) 在容器里重跑整条管线、用容器的数字作为论文的数字。
   (b) 对一篇讲复现的论文更相称，但任何数字变动都要走
   `NUMBER_MIGRATION_fixed_anchor.md`，且判定翻转必须按"翻了"报。

---

## 4. 明确仍未完成

1. **作者本人**：维护者身份、仓库 DOI、四条声明（competing interests / funding /
   contributions / acknowledgements）、容器首次构建与验收表。
2. **S40 外部验证**：结构上不可得（合格队列不存在），不是资源问题。
   ⚠ S40 §3 列的三个前提里，第三个正是"锁定容器与清单"——
   **容器是这三件里唯一现在动手就能划掉的**。
3. **`FIGURES_GB_mapping.md` §五**三条正文问题已全部核实处置。

---

## 5. 你可以自己验的

```bash
python step127_audit_manuscript_numbers.py .   # 数字 ↔ 表        -> CLEAN
python step153_prereg_inventory.py .           # 预注册计数        -> CLEAN
python step154_crossref_audit.py .             # 交叉引用/图注/文献 -> CLEAN
python step155_renumber_references.py .        # 文献顺序（只报告） -> 44 条 0 变动
python step152_set_identity.py                 # 残留占位符        -> 21 处待填
```

`step154` 的图注检查是**跟着图脚本走**的：面板字母从
`figures/make_gb_figN_*.py` 的 `set_title` 解析，并跟一层本地 import
（Fig. 3 的 a/b/c 来自 power-stability 图，Fig. 8 整张是 compartment 图改号），
所以图改了而图注没改会立刻报错。

⚠ **但它查不了图注里的数字是否正确**——那仍是 `step127` 的活。
目前图注只用正文已有的数（4×10⁻³⁷、10.1%、3.73–4.55、15.99、47.6/63.0、
+5.2/+6.4、253/291/86.9%、21.3 倍），唯一新引入的是 Fig. 6b 的
`|β| = 5.45 × SE`——那是 5×10⁻⁸ 的定义常数，不是结果。
