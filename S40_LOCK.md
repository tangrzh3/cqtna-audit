# S40 前提三：锁定记录

**日期**：2026-08-21
**规则来源**：`EXTERNAL_VALIDATION_PROTOCOL_v9_3.md` §3（S40 全文采纳，冻结）
**产出**：`136a_lock_manifest.tsv`、`136b_environment.tsv`、`136c_console.log`
**脚本**：`step136_lock_manifest.sh`（提交 `0f2b415`，**先于本清单**）

---

## 0. 为什么现在做

§3 要求：**在外部关联结果传入之前**存放协议、可执行容器、`renv.lock`、
CQTNA 源码包、冻结的工具表、已知位点名单、对照名单、检验族清单与 SHA-256 清单。

⚠ **"之前"这个词是这条规则的全部内容。** 一旦打开过任何外部汇总统计，
此后做的任何锁定都只能证明"我们锁了"，不能证明"我们先锁了"。
本项目已经因为分不清这两件事栽过两次（S33、S39）。

所以这份清单**在触碰 MVP 汇总统计之前**产出。

---

## 1. 锁住了什么

| 类别 | 数量 | 内容 |
|---|---:|---|
| `frozen_records` | 6 | 六个格子的暴露×结局记录表（工具表由此决定）|
| `known_lists` | 7 | 自身名单 + S39 的五份对照名单 + RA 位置表 |
| `scripts` | 15 | 冻结分区之后的每一步，含本脚本自身 |
| `package` | 1 | `cqtna_0.3.0.tar.gz` |
| `decision_records` | 9 | 协议、S37/S38/S38-A1/S39/S40、功效闸门说明、队列筛查 |
| `result_tables` | 10 | 当下的全部结果表 |
| `environment` | 1 | R 版本与包版本 |
| **合计** | **49** | **缺失 0** |

- **清单自身 SHA-256**：`692b90cca50a07cd17a5616149f01ee3159a065675c9a66cdc34fc2f4fd49461`
- **git commit**：`0f2b4157cafbbff865bcc87e22fc5585543aa8f0`
- **被跟踪文件的改动**：**0**（工作树在锁定时刻是干净的）

⚠ 结果表也在清单里，是有意的：日后任何一个数被改动，
**对比这份清单就能看出来**，不必依赖记忆或改稿账本。

---

## 2. ⚠ 没锁住什么（这一节比 §1 重要）

§3 列了九项，本项目产出七项。**缺的两项都需要本项目之外的东西**：

| 缺项 | 为什么缺 | 谁来补 |
|---|---|---|
| **可执行容器镜像** | 需要容器构建与镜像仓库；本仓库没有，也不应当由分析脚本顺手生成一个没人验证过的镜像 | 作者 / 机构基础设施 |
| **`renv.lock`** | 本项目未使用 `renv`。**现在补装 renv 生成一份 lock，只会记录"今天装的是什么"，不是"当时跑的是什么"**——那是一份看起来合规、实际上追认的文件 | 作者决定：要么接受 `136b_environment.tsv` 作为如实的替代，要么从头用 renv 重跑一遍 |

`136b_environment.tsv` 记录的是**实际加载的版本**（R 4.4.1、cqtna 0.3.0 及 base 包），
**它不是 `renv.lock`，也不假装是**。§3 的合规性在这一项上**尚未满足**，
对外材料不得称"容器已锁定"。

---

## 3. 这份锁定证明了什么、不证明什么

**证明**：截至 `0f2b415`，上述 49 个文件的内容是这些；
此后任何改动都可由散列检出；且这一切**先于**任何外部关联统计。

**不证明**：不证明分析是预注册的（那要 DOI，见 S40 §3）；
不证明环境可复现（缺容器与 `renv.lock`）；
不证明设计是事前的（S40 是**采纳**记录，方案作者是第三方，写于看过全部结果之后）。

---

## 4. 重新验证

```
bash step136_lock_manifest.sh
```

清单是确定性的：同样的输入必然给出同样的 49 行与同一个自散列。
若自散列变了，就是有输入变了——**先查是哪一行，再谈别的**。
