# 项目交接文档 v12

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-09-07
**取代**：`HANDOFF_v11.md`（其 §四技术坑、§五方法论纪律仍有效，本文件继承并扩充）
**工作树**：干净。本轮 `dd4e37f` → `749225d`，共 **19 个提交**，**仍未 push**

> **新会话阅读顺序**
> 1. 本文件，尤其 **§一（覆盖面被量出来了，而且不利）** 与 **§三（还剩什么）**
> 2. `manuscript/SUPP_identity_reach.md`（S51）——**撤回声明 + C6 结果**
> 3. `REVIEW_BRIEF_v13.md`（给第三方的简报；⚠ 其 §2.1 已被本轮结果取代）
> 4. `manuscript/MANUSCRIPT_GB.md` 的 Abstract 与 Part 4

---

## 一、★★ 中心主张没变，但**适用范围**第一次有了数，而且这个数不利

**v11 时**：恒等式的覆盖面靠一次词组扫描"估计"为 46%（下界）/ 84%（上界）。
**现在**：那两个数**已被我们自己撤回**，换成一次双人人工编码的实测值。

### 1. 词组扫描是错的，已撤回

`step151` 的 `single` 正则**在全文任意位置**匹配，不要求靠近 cis-eQTL、
不要求描述主名单、不要求是 Wald。第三方评审指出这一点后核实：

- 命中的 71 篇里，**23 篇全文根本没提过 cis-eQTL**；
- 要求该词组落在 cis 提及的 **500 字符内 → 剩 13 篇**；
- 要求**同句 → 剩 10 篇**。

**一个在最低限度邻近性要求下从 71 崩到 10 的计数，是方向未知的词组命中数，
既不是下界也不是上界。** S51 §0 是正式撤回声明。

### 2. C6：实测覆盖面 = **22.2% [12.5, 36.3]**

在 C1/C3 用过的**同一批 46 篇**上，两位编码者独立判定
"哪个估计量产出了该论文的**主** cis 提名名单"，规则写于任何论文被读之前
（`156_C6_CODING_RULES.txt`）。

| 码 | 含义 | 篇数 |
|---|---|---:|
| **W** | 单变异 Wald | **10** |
| **M** | 多工具估计量 | **28** |
| **X** | 混合流程 | **7** |
| **U** | 不可判定 | **1** |

- 一致率 **71.7%**、Cohen's κ **0.534**（均为**裁定前**，按登记）；
- 13 篇分歧**共同裁定**，逐篇留证据引文；
- **可判定 45 篇中 W 占 22.2%**；把混合流程整体计入的上限为 **37.8%**。

⚠ **裁定前**就把 13 篇所有极端裁定下的边界（17.4%–45.7%）算出并入库
（`156f_C6_bounds.tsv`），且未给编码者看——**目的是让裁定无法被"知道数字该往哪走"牵着走**。
最终 22.2% 落在区间内，贴近两人各自解法（24.4% / 27.3%）。

### 3. 正文已按这个数改写

摘要、Part 4、Discussion、Methods 四处全改。Discussion 现在写着：

> **The design this paper analyses is a minority of current practice.**

并写明**未测**之事：工具变多时论证是否优雅衰减——**不主张**。
Conclusions 那句最可能被引用的话也已明确限定为 "a single-variant cis-eQTL MR design"。

---

## 二、★ 这个结果对全文影响多大——**结论不变，重要性主张缩小**

**必须分开看三层。**

### 层一：恒等式本身 —— **零影响**

一工具 + 一阶 SE ⇒ `|z| = |β_out|/se_out`。这是代数。

### 层二：全部实证结果 —— **零影响**

4.96 倍与其分解（剔 GWS 后 1.98/P=0.41）、跨结局名单全换、
基因归属 10 个位点命中 6、单独命中仅 2、MC1R 跨 16 个基因不含 MC1R、
coloc 2/284、双轴交叉网格、RA 作废格、五疾病迁移——
**这些都是关于我们自己这条流水线的结果，而我们这条流水线本来就是单变异 Wald。
C6 一条都不触及。**

### 层三：**这件事对领域有多重要** —— **这里缩小了，而且只有这里**

从"暗示很广"变成"实测约五分之一"。这是真实的损失，别粉饰。

**但也别过度自责，以下四条是实话：**

1. **45 篇里 10 篇不是小数目。** 这是一个检索定义的样本，
   若该领域年产上百篇，五分之一就是几十篇。
2. **那 28 篇 M 并未被免罪，只是不受这条恒等式管辖。**
   本文对它们另有一条独立批评（r² < 0.1 的 clumping 保留相关性而 IVW 假定独立，
   我们自己的放宽分析里 157 个暴露达 FDR<0.05），已在 Methods。
   ⚠ **但那是另一回事，不得与恒等式混为一谈。**
3. **九项检查与 `cqtna` 包与估计量无关。** 位点归属、名单稳定性、
   基因归属、区室归属——换成 IVW 一样要做。
   **论文的建设性交付物完全不受影响。**
4. **U 只有 1/46**，说明这个领域在这一点上的报告习惯比预想的好——
   这是个轻微的正面发现。

### 真正的风险是**读法**，不是这个数

- 若全文读起来像"我们找到了这个领域的病灶"，22% 会削弱它；
- 若读起来像"单变异 cis-MR 提名到底是什么、不是什么、占多少、以及一套与估计量无关的检查"，
  **22% 就只是一句诚实的范围声明，而论文因为测了它而更强**。

**而且：主动测出一个对自己不利的数，本身是这篇论文最强的可信度资产。**
Reviewer 1 说"S51 修正前不建议提交"——现在不但做完了，
而且做出来的结果让我们难看。**这正是让审稿人相信其余部分的东西。**

---

## 三、★ 还剩什么

### A. 需要作者本人（我做不了）

`step152_set_identity.py` 不带参数跑一次会列全，当前 **21 处**：

1. **姓名 / 邮箱 / ORCID / GitHub 仓库路径**；
2. **Zenodo DOI**（发 release 后自动铸出，见 `DEPOSIT.md` §5）；
3. **四条声明**：competing interests / funding / contributions / acknowledgements
   （ethics 与 availability 我已按事实写全）；
4. **容器首次构建**——`CONTAINER.md` §4 五行验收表仍全是 `not yet run`。

### B. 下一个窗口

1. **正文压缩**：主文 **8,752** 词（目标 8,000），摘要 **293**（GB 250）。
   保住 B1/B2 的前提下 8,600 左右是地板，见 §五-新增-3。
2. **第二环境的 DOCX/PDF 逐页视觉检查**（Reviewer 3 硬门槛，本轮**未做**）。
   `.docx` 已重建过一次（含九图与完整图注），但**只在一台机器上、只看了 XML 结构**。
3. **push 并冻结**：版本、哈希、存档。
4. `REVIEW_BRIEF_v13.md` **§2.1 已过期**（还在讲 46%/84% 的界），
   若要再送外审须先改成 C6 的结果。

### C. 明确不做

继承 v11 §三-C 全部四条，另加：

- **不得声称恒等式对多工具流程近似成立**——没测过（S51 末节写死）；
- **不得把 S53 的零结果写成"扰动无法验证 eQTL 提名"**（S53 §7 写死）；
- **不得为 TPI1 再找功能证据**（S52 §5 禁止条款，含一句被点名禁止的句子）。

---

## 四、技术坑（v11 §四之外的新增）

67. **★★ 一个在全文任意位置匹配的正则，不构成任何方向的界。**
    `single` 命中 71 篇，其中 23 篇没提过 cis-eQTL；要求同句只剩 10。
    **邻近性不是细节，它就是这个量的定义。**
68. **★ 决定性的证据可能在对照臂而不是处理臂。** TPI1 的 perturb-seq 记录里，
    `target expr = 0` 看起来像敲低成功，但 **NTC 臂 31 万个细胞也是 0**——
    该测定根本不测这个基因。**只看处理臂会得到完全相反的结论。**
69. **★★ "不表达"与"不被读出"是两回事。** TPI1 在同一批细胞的常规 bulk RNA-seq 里
    **379.5 CPM、78,986 个基因中排第 340 位**，却不在探针面板内——
    该面板把表达最高的一类（线粒体、核糖体蛋白、GAPDH）排除了，
    其自身 bulk 前 40 高表达基因只有 8 个进入读出。
70. **★ 补充材料用主文编号引文献，重编号会静默改变它指向谁。**
    S47 曾以 `[8]` 引 Nathan，重编号后 `[8]` 变成 Howe。
    **补充材料一律改用作者名。** `step155` 现在写入前会列出所有方括号数字，
    并声明它分不清引文与预测区间。
71. **★ 本会话的 shell 会把 `\\` 折成 `\`。** 用 heredoc 打补丁时，
    `"\\n"` 会变成真换行，制造语法错误。**改文件用逐行替换，或用 `chr(92)` 构造。**

---

## 五、方法论纪律（v11 §五之外的新增）

64. **★★ 把两端先钉在记录上，裁定就无法被牵着走。**
    C6 的 13 篇分歧在**裁定前**就算出全部极端情形（17.4%–45.7%）并入库，
    且不给编码者看。最终值落在区间内。
    **这既防事后调整，也防"我们本来就知道会是这个数"的事后追认。**
65. **★★ 事前写死读法的价值，在结果模棱两可时才显形。**
    TPI1 的 DE 基因数在第 90/94 百分位——**只看那一列就会写出一句被 S52 §5 明令禁止的话**。
    挡住它的不是判断力，是那张跑之前就写好的表。
66. **★ 预测错了要按"错了"记，不要顺势改叙述。**
    本轮两次预测都错：S52 押 A 实为 C（面板不含该基因）；
    S53 押边际负关联实为正（OR 2.11）。**两次都在登记区写明错因**——
    S53 那次的错因是"把尾部现象推广到整个分布"。
67. **★ 一份预注册应当事先写明"零结果长什么样、且零结果值得报告"。**
    S53 §5 写了，所以条件化后 OR 1.04 / P 0.73 是**结果**而不是失败。
68. **★★ 审计写完之后要看它抓到了什么。**
    `step153` 本轮抓到 S52/S53 入列后"fourteen"要变"sixteen"（**五处**加枚举表加范围）。
    **这是第三次"某个错误之后写的审计，抓到了同一错误的下一个实例"。**

---

## 六、文件地图（v11 §六之外的新增）

**新增预注册（均单独提交，顺序 git 可核）**
`manuscript/PREREG_perturbseq_lookup.md`（**S52**，TPI1 在 perturb-seq 图谱中的查询）·
`manuscript/PREREG_visibility_overlap.md`（**S53**，两平台可见性重叠）·
`156_C6_CODING_RULES.txt`（C6 编码规则，写于任何论文被读之前）

**新增审计脚本**
`step153_prereg_inventory.py`（预注册计数 ↔ 正文）·
`step154_crossref_audit.py`（补充材料/图/图注/面板/文献/重复句/占位符）·
`step155_renumber_references.py`（按引用顺序重编号，丢弃未引用，含补充材料方括号警告）·
`step152_set_identity.py`（一条命令填完身份与 DOI，并列出所有残留占位符）

**新增分析脚本**
`step156_estimator_provenance_coding.py`（C6：双盲编码 → κ → 裁定 → 分布）·
`step157_perturbseq_tpi1_lookup.py`（S52 执行）·
`step158_visibility_overlap.py`（S53 执行）

**新增交付物**
`container/Dockerfile` + `container/requirements.txt` + `container/install_r_packages.R` ·
`CONTAINER.md`（⚠ **镜像从未构建过**，§4 验收表全空）· `REVIEW_BRIEF_v13.md`

**正文与图**
九图现有**完整面板图注**（约 25 个面板，1,299 词），九图全部有正文 callout；
参考文献 **44 条**，按引用顺序，无未引用条目；
新增 Declarations 段与作者块占位。

---

## 七、下一轮建议顺序

1. **作者补身份 + DOI**（`step152` 一条命令），然后重跑 `R CMD check`
2. **构建容器并填 `CONTAINER.md` §4 验收表**——这是 S40 三个前提里唯一现在能划掉的
3. 正文压缩至 ~8,000、摘要至 250（取舍需作者定，见 `COMPRESSION_PLAN.md`）
4. 改 `REVIEW_BRIEF_v13.md` §2.1（已被 C6 取代），再送外审
5. 第二环境 DOCX → PDF 逐页视觉检查
6. push、冻结、存档

⚠ **改任何一个数之前**，先在 `manuscript/NUMBER_MIGRATION_fixed_anchor.md` 找到它的来源；
改完把**四个**审计一起跑：

```
python step127_audit_manuscript_numbers.py .   # 数字 ↔ 表
python step153_prereg_inventory.py .           # 预注册计数
python step154_crossref_audit.py .             # 交叉引用/图注/文献
python step155_renumber_references.py .        # 文献顺序（只报告）
```

当前四个全绿。`step127` 新增 §1b（词组扫描的崩塌）、§1c（68–80% 对账 101d）、
**§1d（C6 七个数对账 156e）**。
