# 请复核一个判断：位点归属该用哪种口径

**给第三方复核者** · 2026-08-18 · 仓库根 `D:/R_ex/MR/`

我做了一个判断，它**否决了一位外部评审的整改要求**，并且**保住了我自己已发表的数字**。
这两件事叠在一起，正是最该被独立复核的情形。本文件的目的是让你**推翻我**，
所以证据、反方理由、以及"什么能改变我的结论"都写在里面。

不需要读懂整个项目。第 §6 节有三条命令，跑完就能自己下判断。

---

## 1. 一句话背景

有一个工具，输入是别人做完的 cis-eQTL 工具化 MR 候选名单，
输出是"显著信号有多少落在该疾病**已知的**遗传位点上"。

核心统计量：

```
倍数 = (显著位点中"已知"的比例) / (背景位点中"已知"的比例)
P    = 单侧 Fisher
```

"位点"由**染色体内 1 Mb 单连锁聚类**定义；
某位点"已知" = 该位点上有工具变量落在某个已发表 lead SNP 的 1 Mb 内。

---

## 2. 争议点

外部评审指出：**分子和分母用了不同的记录集**。

| | 分子（显著位点算不算已知） | 分母（背景位点） |
|---|---|---|
| 已发表实现 | 只看该位点的**显著**记录 | 该位点的**全部**记录 |

这确实不自洽。评审要求统一成"全部记录"。

⚠ 评审同时指出的另一条——**基因标签按单条记录判，导致一个基因被标 novel
而它所在位点被算作 known**——是无争议的 bug，**已修，与本争议无关**。

统一的方式有三种，给出不同答案：

| 代号 | 分子 | 分母 | 自洽？ |
|---|---|---|---|
| **C1** any-record | 任一记录靠近 | 同 | ✅ |
| **C2** sig-record | 显著记录靠近 | 任一记录 | ❌（已发表的） |
| **C3** lead-variant | 该位点最显著的那条记录靠近 | 同 | ✅ |

**我的判断：不采纳 C1，维持 C2，并在正文写明其不自洽与保守方向。**

---

## 3. 我的证据

### 3.1 阴性对照（这是我判断的全部依据）

该研究预注册 §5/§6-E 写死：**用错配疾病的已知位点名单打分，若也显著，该格作废。**

**melanoma × eQTLGen 全血：**

| 口径 | 匹配名单 | **错配名单（HCC 名单打 melanoma）** | 预注册判定 |
|---|---|---|---|
| C1 | 4.88×，P = 1.3×10⁻¹³ | **3.90×，P = 1.7×10⁻⁴** | ❌ **作废** |
| **C2** | 4.44×，P = 3.7×10⁻¹¹ | 1.30×，P = 0.41 | ✅ 通过 |
| C3 | 6.32×，P = 1.6×10⁻¹⁴ | 2.66×，P = 0.096 | ⚠ 擦边 |

RA 那格同向：错配对照 C2 下 1.60×（P = 0.16），C1 下 **2.40×（P = 0.0078）**。

**melanoma × CD4 时程：**

| 口径 | 匹配 | 错配 |
|---|---|---|
| C1 / C2 / C3 | 4.09× / 4.09× / 4.66× | 0.00× / 0.00× / 0.00×（P = 1） |

### 3.2 机制：单连锁串联

| 格子 | 位点跨度中位数 | 最大 | >10 Mb 的位点数 |
|---|---|---|---|
| melanoma × CD4 时程 | **62 kb** | 4.6 Mb | **0** |
| melanoma × **eQTLGen** | **1,400 kb** | **30,778 kb** | **30** |
| HCC-high × CD4 | 63 kb | 8.7 Mb | 0 |

在 C1 与 C2 之间改变状态的那个位点是
**chr1:107,053,498–120,007,104，跨度 12,954 kb，105 条记录**，
其中**没有任何一条显著记录**靠近已知 SNP——是一条不显著的记录让整块变成 "known"。

我的解释：一块 13–31 Mb 的区域几乎必然包含**某个**疾病的已知 lead SNP，
所以 C1 在密集资源上问的是"这块有多宽"，不是"信号在哪"。

---

## 4. ★ 反方：我可能错在哪

请优先攻击这几点，我认为它们是我论证里最薄弱的。

### 4.1 我用阴性对照当裁判，但阴性对照本身是弱对照

该研究自己就写过：错配名单能排除"在所有疾病里都密集的位点"，
但**排除不了"本身就是疾病特异的密度"**。
若你认为这个对照不足以当判据，我的整个论证就塌了。

⚠ 更尖锐的版本：**C1 下错配对照失效，也可能说明对照终于开始工作了**
——即 C2 的错配对照"干净"只是因为它同样迟钝，而不是因为归属是真的。
我没有独立证据排除这一解释。

### 4.2 我保住了自己的数字

否决 C1 的结果是论文数字一个不用改。这在动机上可疑。
反向检查：**若我采纳 C1，melanoma × eQTLGen 会从 4.44 升到 4.88、
HCC-high × eQTLGen 从 P = 0.052（不显著）变成 P = 1.6×10⁻⁴（显著）、
"六格中四格显著"会变成五格**——也就是说 **C1 会让论文结论更强**。
我否决了一个对自己有利的改法，但这不构成清白证明。

### 4.3 C3 我处理得草率

C3 自洽、且不受串联影响，理论上比 C2 更该被采纳。
我以"错配对照 P = 0.096 擦边"为由没选它，但 0.096 不是失败。
**若你认为该采纳 C3，那论文数字要全面重算**（4.09 → 4.66，4.44 → 6.32 等）。
我没有充分论证为什么不选 C3——这是本判断里最弱的一环。

### 4.4 真正的问题可能不在口径，而在位点定义

若 1 Mb 单连锁在密集资源上产生 31 Mb 的"位点"，
那么**在那个分区上做任何位点级统计都不可靠**，C1/C2/C3 之争是次要的。
按这条读，正确处置是改位点定义（收紧窗口、限制跨度、或改用固定窗口），
而不是在坏分区上挑口径。我把这条写进了 Limitations，但**没有改位点定义**。

### 4.5 我只在四个格子上验证

RA 两格、eQTLGen × HCC 两格的 per-record 表当初没保存，
我是重跑脚本得到的；CD4/HCC-Soskic 四格是直接从表算的。
样本量小，且全部来自同一项研究。

---

## 5. 什么能改变我的结论

我会接受 C1（或 C3），如果出现下列任一情形：

1. 有人证明**阴性对照在 C2 下失去检出力**，即构造一个已知有假富集的情形，
   C1 的对照能报警而 C2 的报不出来；
2. 有人证明 **13 Mb 的单连锁块仍是合理的"独立位点"**；
3. 用**密度匹配置换**（按染色体、位点跨度、工具变量数、基因密度匹配）
   替代错配名单后，C1 的富集仍然显著而其零分布干净；
4. 在**位点定义不产生串联**的资源上（如收紧到 100 kb），C1 与 C2 给出相同结论
   ——那说明分歧只是串联的副产品，口径之争可以搁置。

第 4 条我做了一半：包里有位点窗口扫描（100/250/500/1000 kb），
但我没有在收紧后的分区上重跑三种口径的阴性对照。**这是最直接的下一步检验。**

---

## 6. 怎么自己跑（三条命令）

前置：R ≥ 4.1、Python 3 + numpy/pandas。R 包在 `cqtna_r/`。

```bash
# 安装
R CMD INSTALL cqtna_r
```

### 6.1 五分钟：让阴性对照说话

```r
library(cqtna)
mr <- as_cqtna_mr(cqtna_demo("mr"),          build = "GRCh38")
kn <- as_cqtna_known(cqtna_demo("known"),    build = "GRCh38")
mm <- as_cqtna_known(cqtna_demo("mismatch"), build = "GRCh38")

for (k in c("significant_records", "any_record", "lead_variant")) {
  a <- cqtna_attribution(mr, kn, known_from = k)
  n <- cqtna_attribution(mr, mm, known_from = k)
  cat(sprintf("%-20s matched %.2fx P=%.3g | mismatched %.2fx P=%.3g\n",
              k, a$fold, a$fisher_p_one_sided, n$fold, n$fisher_p_one_sided))
}
```

⚠ 包里附带的 demo 是 **CD4 那格**（三种口径都稳健的那个）。
争议出在 eQTLGen 格，其数据在 `92c_locus_annotated.tsv`，用 §6.2 的脚本看。

### 6.2 二十分钟：逐个位点看，而不是看汇总数字

```bash
python review_convention.py eQTLGen
```

对每个在两种口径下状态不同的位点，打印：坐标、跨度、该位点上**全部**工具变量、
各自到最近已知 lead SNP 的距离、以及是否显著。

**你要判断的是一道具体的题**：
chr1:107.1–120.0 Mb 这 13 Mb、105 个基因，算不算一个"已知位点"？

### 6.3 一分钟：全格旧→新对照表

```bash
python step122_locus_convention_recheck.py    # 落 122a_convention_recheck.tsv
```

### 6.4 位点跨度诊断

```r
cqtna_locus_spans(mr)          # 跨度分位数、超窗口 5 倍的位点数
```

---

## 7. 相关文件

| 路径 | 内容 |
|---|---|
| `CONVENTION_REVIEW.md` | 我的决定与证据（本文件的作者视角版） |
| `review_convention.py` | 逐位点摊开的复核脚本 |
| `step122_locus_convention_recheck.py` · `122a_convention_recheck.tsv` | 旧→新全格对照 |
| `cqtna_r/R/utils.R` → `cq_locus_known()` | 三种口径的实现，30 行 |
| `cqtna_r/R/modules.R` → `cqtna_attribution()` | 主统计量 |
| `cqtna_r/tests/testthat/test-mechanisms.R` | 机制 2 分离三种口径；机制 12 锁住"对照失效必须报警"；机制 13 锁住串联检出 |
| `manuscript/PREREG_generality_grid.md` §5/§6-E | 阴性对照判据的原文 |

---

## 8. 我想要的复核结论形式

请给出下列之一，并说明依据：

- **A. 维持 C2**（同意我的判断），并指出正文该怎么写这条不自洽；
- **B. 改用 C3**，接受论文数字全面重算；
- **C. 问题在位点定义**，先改分区再谈口径；
- **D. 阴性对照不足以当判据**，需要密度匹配置换才能下结论。

我自己认为 **C 与 D 最可能是对的**，而我目前的处置（维持 C2 + 写明局限）
是在没做完那两件事之前的**临时**处置，不是终点。
