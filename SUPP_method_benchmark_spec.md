# S44：五种提名方法的正面基准——口径先定死

**编号**：S44　**日期**：2026-08-23
**脚本**：`step144_relaxed_outcome_lookup.py`、`step145_method_benchmark.R`
⚠ **事后分析**（S42 之后提出的问题），不改动任何注册终点。
**本文件写于看到任何基准结果之前。**

---

## 1. 要回答的问题

S42 证明：**单变异 Wald** 的 |z| 恒等于 outcome 的 |z|，因此名单是 outcome GWAS 的重读。
恒等式**只对单变异成立**。审稿人必然会问：

> 实践中大家用 IVW、weighted median、coloc、SMR-HEIDI。你的批评还成立吗？

**本基准回答它。结论朝哪边都有价值**：若多工具方法同样是重读，批评范围扩大；
若不是，我们就得到一个可推荐的替代做法。

---

## 2. ★ 一个由数据本身决定的结构事实（先说，因为它改变基准的含义）

**coloc（`14_coloc_meta_results.tsv`）与 SMR/HEIDI（`15_SMR_meta_results.tsv`）
只跑在 MR_pval < 0.05 的记录上**（两表的 MR p 最大值均为 0.0498）。

**⇒ 它们是下游过滤器，不是独立提名器。** 候选池已经是单变异 Wald 的输出，
因此它们**继承**恒等式的后果：**只能缩小名单，不可能加入 outcome GWAS 之外的位点**。

**这一条本身就是结果**：标准工作流 MR → coloc → SMR 整体继承了恒等式。
真正的检验只剩 **IVW 与 weighted median**，它们在 relaxed 工具集上独立估计。

---

## 3. 五个方法与它们的显著性规则（**事前定死，不得事后调整**）

| 方法 | 数据 | 显著性规则 | 恒等式 |
|---|---|---|---|
| **单变异 Wald** | `13_meta`，3,556 条记录 | BH FDR < 0.05 | **适用** |
| **IVW** | `18_sensitivity_meta`，350 个暴露 | BH FDR < 0.05 | 不适用 |
| **IVW（乘法随机效应）** | 同上，350 个 | BH FDR < 0.05 | 不适用 |
| **Weighted median** | 同上，292 个 | BH FDR < 0.05 | 不适用 |
| **coloc** | `14`，371 条 | **PP.H4 > 0.8**（附报 > 0.5） | 继承 |
| **SMR / HEIDI** | `15`，369 条 | **FDR(SMR) < 0.05 且 P(HEIDI) > 0.05** | 继承 |

阈值全部取本领域惯例值，**不为让结果好看而调**。

## 4. 位点与归属

- 分区：`fixed_centre`、`locus_kb = 1000`，在**每个方法自己被检验的位置集**上计算；
- **多工具暴露的代表位置**：取该暴露**暴露侧 P 最小**的那个工具。
  ⚠ **不取 outcome 侧 P 最小的**——按 outcome 选位置会直接污染 §5 的分解；
- 归属：C1 `any_record`、`known_kb = 1000`、Landi 名单；
- 背景：**每个方法自己的全部可检验 bounded locus**（不是共用背景）。

## 5. 报告的量

每个方法一行：

| 量 | 含义 |
|---|---|
| `n_sig_loci` / `n_known` | 提名的 bounded locus 数与其中已知的个数 |
| `fold` / `fisher_p` | 相对该方法自身背景的富集 |
| `A` | 机会校正量 (p_sig − p_bg)/(1 − p_bg)，见 S43 |
| **`n_gws`** | 提名位点中**结局 GWAS 自身已达 5×10⁻⁸** 的个数 |
| **`fold_excl_gws` / `p_excl_gws`** | **剔除那些位点之后**的富集——S42 的分解，逐方法重做 |

**判读标准（事前）**：

- 若某方法的 `fold_excl_gws` 仍显著（单侧 P < 0.05），
  **它提供了超出全基因组显著性之外的位点层证据**——那是可推荐的替代做法；
- 若所有方法的 `fold_excl_gws` 都不显著，
  **恒等式的后果覆盖整个方法族**，而不只是单变异 Wald。

## 6. 已知的界限（先写下来）

1. **背景不同 ⇒ fold 不可跨方法直接比。** 这正是 S43 的教训，故同时报 A。
2. **relaxed 集与 strict 集的工具选择规则不同**（F ≥ 5、P < 0.05、LD clumping
   r² < 0.1），所以 IVW 与 Wald 的差异里混着工具集差异，**不是纯估计量差异**。
   ⚠ 这一条无法在现有数据上消除，必须与结论同时陈述。
3. **正文已记录 IVW 的显著性被 clumping 的残余相关性抬高**
   （157 个暴露 FDR < 0.05，对 strict 集的 21 个）。因此
   **IVW 名单更长这件事本身不能读作它发现得更多。**
4. coloc/SMR 的条件性见 §2；它们的行不作为独立证据，只用来展示继承关系。
