% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{cqtna_audit}
\alias{cqtna_audit}
\title{Run the audit}
\usage{
cqtna_audit(
  mr,
  known,
  mismatch = NULL,
  mr_alt = NULL,
  instruments = NULL,
  expression = NULL,
  peaks = NULL,
  target_cell_type = NULL,
  build = NULL,
  locus_kb = 1000,
  known_kb = 1000,
  fdr = 0.05,
  known_from = c("significant_records", "any_record", "lead_variant")
)
}
\arguments{
\item{mr}{MR results: a data frame, or a `cqtna_mr` from [as_cqtna_mr()].}

\item{known}{known loci for **this outcome**, or a `cqtna_known`.}

\item{mismatch}{a different disease's known-locus list, as the negative
control. Omitting it is allowed and warns. Used for module A and, when
supplied, for a mismatched window sweep reported as `G_nc`.}

\item{mr_alt}{MR results from a second outcome GWAS, for module C.}

\item{instruments, expression, peaks}{optional inputs for modules D, E and F.}

\item{target_cell_type}{the cell type the exposure was measured in, for module E.}

\item{build}{genome build; required unless `mr` is already a `cqtna_mr`.}

\item{locus_kb, known_kb, fdr}{the three conventions. If `mr` arrives pre-built
with a different `locus_kb` it is re-clustered, so the window named in the
report is always the window actually used.}

\item{known_from}{how a significant locus inherits its known/novel status;
passed to [cqtna_attribution()] and [cqtna_window_sweep()] so every module
uses one convention. The mismatched-list control is run under the same
convention and the audit warns if it is itself significant, which is the
check that exposes a convention inflated by single-linkage chaining.}
}
\value{
an object of class `cqtna_audit`.
}
\description{
Runs modules A-G and returns one object carrying the results, the per-gene
evidence fields, a run status for every module, and the three diagnostics
that were not run.
}
\examples{
au <- cqtna_audit(cqtna_demo("mr"), cqtna_demo("known"),
                  mismatch = cqtna_demo("mismatch"), build = "GRCh38")
au$A$fold
}
