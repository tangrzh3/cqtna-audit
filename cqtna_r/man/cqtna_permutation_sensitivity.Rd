% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.R
\name{cqtna_permutation_sensitivity}
\alias{cqtna_permutation_sensitivity}
\title{Sensitivity of a permutation control to its own matching specification}
\usage{
cqtna_permutation_sensitivity(
  mr,
  known,
  known_kb = 1000,
  fdr = 0.05,
  known_from = c("any_record", "lead_variant"),
  match_on = c("n_records", "span", "n_genes"),
  tolerances = c(0.1, 0.25, 0.5, 1, 2),
  n_perm = 1000,
  seed = 1
)
}
\arguments{
\item{mr}{a `cqtna_mr` object.}

\item{known}{a `cqtna_known` object for this outcome.}

\item{known_kb, fdr}{as in [cqtna_attribution()].}

\item{known_from}{`"any_record"` or `"lead_variant"`; see the section above.}

\item{match_on}{properties null loci must resemble the observed ones on. Any
of `"n_records"`, `"span"`, `"n_genes"`, `"chr"`. Matching on span is what
separates this from the mismatched-list control; matching on `n_genes` is
what makes "controls for gene density" true rather than aspirational.}

\item{tolerances}{the grid to sweep.}

\item{n_perm}{number of null sets.}

\item{seed}{passed to [set.seed()] so an empirical p-value is reproducible.}
}
\value{
a data frame, one row per tolerance.
}
\description{
Runs [cqtna_permutation_control()] across a grid of tolerances. If the verdict
moves across the grid, the test has not identified an estimand and no single
p-value from it should be quoted.
}
