# 第三方评审简报 v11

**日期**：2026-08-23
**手稿**：`D:\R_ex\MR\manuscript\MANUSCRIPT_GB.md`（16,510 词，含 Methods）
**仓库**：`D:\R_ex\MR`（git，本轮 52 个提交，分支 `main`，**未 push**）
**上一版简报**：`REVIEW_BRIEF_external.md`（v9.3，**已过时**——论文的中心主张此后变了）

---

## 0. 给评审者的一句话

**上一轮评审看到的是"melanoma 上观察到 4.96 倍富集"。这一版的中心主张已经不是那个了。**
现在的主张是关于**估计量**的，而那个 4.96 倍在本轮被我们自己拆开、
证明它由三个早已发表的位点承担。请按新主张评审，不要按旧的。

---

## 1. 现在的故事（三句话）

1. **单变异 cis-MR 的检验统计量恒等于结局 GWAS 的检验统计量。**
   一个工具 + 一阶标准误 ⇒ `|z| = |β_out|/se_out`。暴露决定符号、尺度、
   哪些变异有资格被检验；**不决定哪些显著**。因此提名名单是**结局 GWAS
   在更宽阈值下的重读**（此处 BH 0.05 ＝ 结局 P ≤ 2.1×10⁻⁴）。
2. **那个 4.96 倍的富集，全部由结局 GWAS 自己已经找到的位点承担。**
   八个显著位点里三个自身达 5×10⁻⁸（两个在 MC1R 区、一个 PARP1），
   **且这三个全在已发表名单上**。剔除后：5 个位点、1 个已知、
   **1.98 倍、P = 0.413**。
3. **换估计量不改变这一点。** 七个口径（Wald / IVW / IVW-MRE / weighted median /
   coloc×2 / SMR-HEIDI）**无一**在剔除后仍显著（最小 P = 0.213）。

---

## 2. 本轮新增的六条证据（编号对应补充材料）

| | 内容 | 文件 |
|---|---|---|
| **S41** | 跨疾病迁移：六个疾病各打自己的名单，五个有名单的**自身富集全部经 Holm 存活** | `manuscript/PREREG_transport_grid.md`、`138a/138b` |
| **S42** | **恒等式**（数值核验：se 差 0、P 差 3×10⁻¹⁶、符号 51% 相反）＋ **分解** | `manuscript/SUPP_estimator_identity.md`、`140b/140d/141a` |
| **S43** | 匹配功效后的跨疾病比较 ＋ 机会校正量 A | `manuscript/SUPP_power_matched_transport.md`、`142a` |
| **S44** | **七口径正面基准** | `SUPP_method_benchmark_spec.md`、`145a` |
| **S45** | 精细定位版分解 ＋ 代理 LD 的**两种**失效模式 | `manuscript/SUPP_finemap_decomposition.md`、`147a` |
| 模块 H | 把分解做成包里的第九项检查 `cqtna_decomposition()` | `cqtna_r/`，`R CMD check` OK，268 项断言 |

---

## 3. ⚠ 请评审者重点看的六处（我们自己认为最脆弱的地方）

1. **确证性主张压在一张 2×2 上**：显著且已知 4 / 显著非已知 4 /
   背景已知 62 / 背景非已知 585。leave-one-out 去掉最强位点 → P 从 0.0048 到 0.0255。
2. **S42 的恒等式只在 5 个格子里的 2 个上做了数值核验**——其余三张结果表
   未保存 outcome 侧列。代数上普遍成立，但我们只报核验过的两格。
3. **S44 的 IVW 对比混着工具集差异**：relaxed 集的选择规则与 strict 集不同
   （F ≥ 5、P < 0.05、clump r² < 0.1），**现有数据无法剥离**。
4. **S44 的 coloc 与 SMR 是下游过滤器**，不是独立提名器
   （两表 MR p 最大值 0.0498），因此它们"继承"恒等式而非检验它。
5. **S45 不是模块 H 的独立证据**：亚阈值位点给 0 个 credible set
   是功效使然，**不得读成"已证明亚阈值部分为空"**。
6. **外部验证结构上不可得**：门槛 R ≥ 4（按合格覆盖率下限 R ≥ 5），
   现存表型合格的最好资源（MVP 5,364 例组织学确诊侵袭性）只到 **R = 0.43**，
   缺口 9–13 倍。详见 `S40_COHORT_SCREEN.md`。

---

## 4. ⚠ 本轮我们自己发现并修正的错误（请核这些修正是否到位）

**这一节比 §2 更值得看**，因为它决定了前面那些数字能不能信。

| | 缺陷 | 修正 |
|---|---|---|
| **1** | **S39 的对照面板每格只有四份名单，设计要求六份** —— breast/lung 的 GRCh38 名单因工具静默失败只建出 6 个 / 0 个位点，被 `MIN_LEAD=30` 挡掉 | 重建（`step137`）：lung 0→271、breast 6→740、**prostate 35→1,160**。S39 重跑后**三格判定改变**，其中一格是头号格子 |
| **2** | **`36e` 被当成跨癌种迁移检验，实为错配对照表** —— 它把每个癌种都打在 **melanoma** 名单上，且用的是**单连锁**旧约定 | 正文 L133–137 整段换成 S41 的真迁移结果 |
| **3** | **摘要最会被引用的那句分母错了**："two-coder subsample **of 152**"——双编码子样本是 **46 篇**，152 是语料 | 改为"152 篇中出现 2 次，46 篇双编码子样本中 0 次" |
| **4** | **Methods 一句话混了三张表的数字**（2,141 变异 / min F 22.2 均属别的表）| 改为 2,126 / 36.1，并各自带出处 |
| **5** | **`step127` 审计有两个缺陷**：点名硬编码"最宽格子"（名单修复后已换人）、子串假通过（`4.84` 靠正文的 `4.87` 匹配上）| 改成计算 + 数字边界匹配；回归验证 |
| **6** | **两份名单无产出脚本** | `step143` 重查并 diff：**两份都精确复现**（colorectal 833/833；lung 316/316）|
| **7** | **`susie_rss` 在 KANSL1 上不可重复**：无种子三次给 0/1/0，每次都 `converged=TRUE`、零警告 | 定位到 `susie_get_cs` 的 purity 随机抽样；加种子 + 跨 5 种子敏感性 |
| **8** | 我把"占上限百分比"当作密度归一化报出，**它恒等于 p_sig，完全不校正背景** | 换成机会校正量 A，正文与 S43 都已撤回原说法 |

---

## 5. 已知缺口（不必再指出，我们知道）

- 无容器镜像、无 `renv.lock`（`S40_LOCK.md` §2）；
- `cqtna_r/DESCRIPTION` 维护者信息与正文 `⟨repository DOI⟩` 仍是占位符；
- 摘要 348 词（Genome Medicine 可，NC 150 / Genome Biol 250 需再砍）；
- 正文引文 47 条，偏少；152 篇审计语料尚未作为补充表发布；
- 诊断本身的 **I 类错误未标定**（`NEXT_EXPERIMENTS.md` 第 2 项，
  我们事先写明它可能对我们不利）。

---

## 6. 我们最想要的三个回答

1. **重构之后的中心主张站得住吗？** 恒等式是初等代数，我们**不主张它是新数学**；
   我们主张的是它对提名名单构成的后果与量。这个定位是否成立？
2. **§4 的八处自查修正，有没有哪一处修得不对、或还有同类未被发现？**
3. **在没有湿实验、外部验证又结构上不可得的前提下，这篇的合理落点是哪一档？**
   我们自评：eLife / HMG / Genetic Epidemiology 稳；
   Genome Medicine / Cell Genomics / Genome Biology 够得着但不稳；NC 目前不够。

---

## 7. 阅读顺序建议

1. 本文件
2. `manuscript/MANUSCRIPT_GB.md` 的 Abstract 与 **Part 1 第一节**（`### The nomination is the outcome GWAS, read at a lower threshold`）
3. `manuscript/SUPP_estimator_identity.md`（S42）——中心主张的全部依据
4. `SUPP_method_benchmark_spec.md` ＋ `145a_method_benchmark.tsv`（S44）
5. `ASSESSMENT_gap_to_ten.md`——我们自己的不客气自评
6. 其余按 §2 的表按需取用

⚠ **数字对账**：`python step127_audit_manuscript_numbers.py <repo>`，
当前状态 **CLEAN**。它只查数字，查不了措辞——本轮 §4 的第 2、3 条都是它抓不到的那类。
