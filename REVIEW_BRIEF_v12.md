# 第三方评审简报 v12

**日期**：2026-08-24
**手稿**：`D:\R_ex\MR\manuscript\MANUSCRIPT_GB.md`（15,893 词，含 Methods；摘要 364 词）
**仓库**：`D:\R_ex\MR`（git，`main`，**未 push**）
**上一版**：`REVIEW_BRIEF_v11.md`（2026-08-23）——**中心主张未变**，本版是对 v11 评审意见的处置

---

## 0. 这一版是什么

v11 的评审给了七项投稿前优先事项，并说"1–3 是正确性门槛，4–7 完成后 NC 才从
碰运气变成有理由首投"。**七项全部处理完毕**，其中三项需要作者本人才能收尾。

**中心主张没有改变**，改变的是它的措辞精度、条件化方式，以及支撑它的标定。

---

## 1. 中心主张（与 v11 相同）

1. **单变异 cis-MR 的检验统计量恒等于结局 GWAS 的检验统计量**
   （一工具 + 一阶 SE ⇒ `|z| = |β_out|/se_out`）。暴露决定符号、尺度、
   哪些变异有资格被检验；**不决定哪些显著**。
2. **4.96 倍的富集由结局自己已找到的位点承担。** 剔除后 1.98 倍 / P = 0.413
   （条件化背景后 2.07 / 0.399；按共享 lead SNP 合并后见 §2.2）。
3. **换估计量不改变这一点**——七个口径剔除后无一显著。

---

## 2. 对 v11 七项意见的逐条处置

### 2.1 ✅ 第 1 项：两处逻辑过推已撤回

Discussion 曾写 "the attribution **must** recur" 与 "Colocalisation **must** be poor"。
**两句都改了。** 现在明确区分：代数必然的只有两条（信号必落在结局有信号处；
profile 特异性无法出现在 P 值里），其余全部写为"恒等式使之**可能**、不**要求**"。
S42 §5 有同样毛病，一并改。

### 2.2 ✅ 第 2 项：两条条件化都算了，其中一条比评审知道的更严重

| | 显著 | 背景 | fold | P |
|---|---|---|---:|---:|
| 全部 | 4/8 | 66/655 | 4.96 | 0.0048 |
| 剔 GWS，背景不变（原报法）| 1/5 | 66/655 | 1.98 | 0.413 |
| **剔 GWS，背景同步剔除** | 1/5 | 63/652 | **2.07** | **0.399** |
| **按共享 lead SNP 合并** | **3/7** | 52/641 | **5.28** | **0.0140** |

⚠ **两个 MC1R 窗口打在完全相同的七个 Landi lead SNP 上**——不是"可能不独立"，
**是确实在重复计同一个已发表区域**。那三个全基因组显著位点**只是两个不同的区域**。
**方向不变，但证据比未合并的计数弱**（P 0.0048 → 0.0140）。

⚠ 按**间距**合并被否决：250 kb 就把背景从 655 压到 84，等于把 `fixed_centre`
当初要避免的单连锁串联请回来。

### 2.3 ✅ 第 3 项：摘要改了，且原本还多一个数目错

评审指出"Holm 显著 ≠ 通过验证"，**但没注意到数目也错了**：有可用名单的一共**五个**
疾病（含 melanoma），不是"另外五个"。现在写：五个自身富集全部经 Holm 存活，
**但只有 melanoma 与 lung 达到完整 `supportive` 判据**；colorectal `inconclusive`，
breast 与 prostate `void`。

### 2.4 ✅ 第 4 项：诊断的 I 类错误、功效、void 率（S46 / `149a`）

| 名单 | 背景 known | **I 类错误** | g=1.25 | g=1.50 | g=2.00 |
|---|---:|---:|---:|---:|---:|
| lung | 10.4% | **0.0048** | 0.754 | 1.000 | 1.000 |
| HCC | 6.6% | **0.0177** | 0.452 | 0.974 | 1.000 |
| colorectal | 31.8% | **0.0105** | 0.947 | 1.000 | 1.000 |
| breast | 29.0% | **0.0272** | 0.919 | 1.000 | 1.000 |
| melanoma | 10.1% | **0.0357** | 0.529 | 0.995 | 1.000 |
| prostate | 40.3% | **0.0394** | 0.970 | 1.000 | 1.000 |

**三项判据全过**：无一超过名义 0.05；g = 1.5 时功效 ≥ 0.974；void 率 0.0177。

⚠ **这条结果推翻了我们自己事先写下的预测**。S46 §1 写于运行之前：
"C1 用 1 Mb 窗口 + `any_record`，在名单密的疾病上**很可能超额拒绝**"。
结果相反，**密名单反而更保守**。
**⇒ 由此排除了一个替代解释**：S41 里 breast/prostate 错配对照全线富集
**不是**诊断超额拒绝，是真实的功效饱和。

零假设用**染色体内环形平移**，不是从背景里重抽 k 个位点——后者是 Fisher 自己
假设的超几何零分布，按构造必然校准，只能查实现 bug。

### 2.5 ✅ 第 5 项：语料已公开

`148a` 152 篇 / 93 种期刊 / 2023–2026 / **全部 PMID**，三轮打分并列；
`148b` **92 条双编码判断 / 46 篇**，含两位编码者各自的值、证据段落、裁定值；
`148c` 编码规则**逐字**，标明写于读任何全文之前。

### 2.6 ⚠ 第 6 项：部分完成，三项需作者

**已做**：`150a_environment.lock` —— **226 个包的确切版本**（19 直接调用 + 207 递归依赖），
`renv.lock` 的 JSON 结构，`renv::restore()` 可读；加 `sessionInfo()` 全文。
它**不是 renv 产出的**，记录的是"现在装着什么"，而本轮 132–150 全部结果表
都在这个环境下跑过。

**仍缺，需作者/机构**：容器镜像、仓库 DOI、`DESCRIPTION` 维护者身份。
⚠ **容器比包清单更要紧**：本轮 S45 已证明**与 BLAS 无关的随机性也能改变结果**，
而锁文件锁不住操作系统、BLAS 与编译器。

### 2.7 ⚠ 第 7 项：压了 1,100 词，但不够，剩下的是作者的取舍

- **Part 1 的结构现在可见**：原本 375 行只有一个标题，加了七个四级标题，**一字未改**；
- **Part 3 从 1,543 词压到 435 词**：六个小节**逐字**移入 S47，正文留框架 + 摘要。

**16,951 → 15,893 词。** ⚠ **这不够**：正文（不含 Methods 与文献）仍约 10,600 词，
而 Genome Biology / Cell Genomics 一般 5,000–8,000。
**剩下的候选全在 Part 1**（双轴交叉 1,180 词、作废的 RA 格 519、错配名单选择 498）——
再砍等于决定把多少"诚实装置"移出读者视线，**这个取舍留给作者**。

---

## 3. 本轮新增的补充材料

| | 内容 |
|---|---|
| **S42** | 恒等式、数值核验、分解 **＋ §7 两条条件化** |
| **S43** | 匹配功效的跨疾病比较 ＋ 机会校正量 A |
| **S44** | 七口径正面基准 |
| **S45** | 精细定位版分解 ＋ 代理 LD 的两种失效模式 |
| **S46** | 诊断自身的 I 类错误、功效、void 率 |
| **S47** | 工作示例（原 Part 3）全文 |
| 模块 H | `cqtna_decomposition()`，`R CMD check` OK，**271 项断言** |

---

## 4. ⚠ 仍然请评审重点看的地方（我们自认最脆弱）

1. **确证性主张仍压在一张小表上**，且合并后更小：**7 个区域、3 个已知**。
   leave-one-out 去掉最强位点 → P 从 0.0048 到 0.0255。
2. **恒等式只在 5 个格子里的 2 个上做过数值核验**（其余三张表未存 outcome 侧列）。
3. **S44 的 IVW 对比混着工具集差异**，现有数据无法剥离；
   **coloc/SMR 是下游过滤器**（MR p 最大 0.0498），继承而非检验恒等式。
4. **S45 不是模块 H 的独立证据**：亚阈值位点给 0 个 credible set 是功效使然。
5. **S46 的标定管不到"信号强度与局部 LD 相关"这类偏差**——环形平移保留不了
   z 与该变异自身 MAF/LD 的关系。
6. **外部验证结构上不可得**：门槛 R ≥ 4（合格覆盖率下限 R ≥ 5），
   现存表型合格的最好资源 R = 0.43，缺口 9–13 倍。
7. **摘要 364 词**：v11 评审同时要求"更精确"（第 3 项）与"更短"（R3），
   **这两条在此处直接冲突**，我们选了准确优先。

---

## 5. ⚠ 本轮我们自己发现并修正的错误（续 v11 §4）

| | 缺陷 | 修正 |
|---|---|---|
| 9 | 我把"占上限百分比"当密度归一化报出，**它恒等于 p_sig，完全不校正背景** | 换成机会校正量 A；S43 与正文都已撤回 |
| 10 | **`susie_rss` 在 KANSL1 上不可重复**：无种子三次给 0/1/0，每次都 `converged=TRUE`、零警告 | 定位到 `susie_get_cs` 的 purity 随机抽样；加种子 + 跨 5 种子敏感性 |
| 11 | 报告 credible set **只报个数不报大小**：PARP1 的 35 变异 / 68 kb 与 KANSL1 的 1,851 / 650 kb 无法区分 | 大小与跨度入表 |
| 12 | 模块 H 的**新测试断言错了**（"A 对密度不变"，实际不是） | 改为断言真正成立的不变性 |
| 13 | 清单脚本续行写坏，产生三个字面量 `n` 条目 | 修；57 文件零缺失 |

---

## 6. 我们最想要的三个回答（与 v11 相同，第三个已更新）

1. **重构后的中心主张站得住吗？** 恒等式是初等代数，**我们不主张它是新数学**。
2. **§2 与 §5 的修正有没有哪一处修得不对、或还有同类未被发现？**
3. **v11 评审给的三分法**（NC 级问题、Genome Biology/Cell Genomics 级证据、
   专业方法期刊级完成度）**在完成 1–7 之后是否移动？**
   我们自评：证据级别因 S44/S46 上移一档，完成度仍卡在容器/DOI/正文长度。

---

## 7. 阅读顺序

1. 本文件
2. `manuscript/MANUSCRIPT_GB.md` 的 Abstract 与 Part 1 第一节
3. `manuscript/SUPP_estimator_identity.md`（S42，含新增 §7）
4. `SUPP_typeI_calibration_spec.md`（S46）——**含事前预测被推翻的记录**
5. `SUPP_method_benchmark_spec.md` ＋ `145a`（S44）
6. `ASSESSMENT_gap_to_ten.md`——我们自己的不客气自评

⚠ **数字对账**：`python step127_audit_manuscript_numbers.py <repo>`，当前 **CLEAN**。
它只查数字，查不了措辞——v11 §4 的第 2、3 条正是它抓不到的那类。
