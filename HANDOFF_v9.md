# 项目交接文档 v9

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-20（工作会话跨 08-19 / 08-20 两日；v8 的 §三-A1/A2/A4/A6/A7 已完成）
**取代**：`HANDOFF_v8.md`（其 §五技术坑、§六方法论纪律仍有效，本文件继承并扩充）
**工作树**：干净。本轮 commit `c9b7968` → `ed6ffc7`，共 **10 个**

> **新会话阅读顺序**
> 1. 本文件，尤其 **§三（还剩什么）**
> 2. `manuscript/NUMBER_MIGRATION_fixed_anchor.md`（**本轮改了哪些数字的唯一清单**）
> 3. `manuscript/PREREG_locus_partition.md`（S37，冻结）与 `PREREG_permutation_estimand.md`（S38，已定稿）
> 4. `CONVENTION_REVIEW.md` §"更正记录" 与 §八（逐位点错配诊断）
> 5. `manuscript/MANUSCRIPT_GB.md`（投稿母稿，**已按新网格更新**）

---

## 一、当前定位

**v8 的阻塞项已全部清掉。正文、图 1/2、方法、讨论、限制段都已在同一个统计单位上。**

主分析口径（冻结，S37）：非递归固定锚定分区、最大跨度 1000 kb、归属口径 C1 `any_record`。
500 kb 为**预注册**敏感性；1 Mb 单连锁 + C2 仅作历史复现，不承担推断。
置换估计目标（S38，2026-08-19 定稿）：容差 1.00 + 随文报整张扫描表。

结论形状变了，且**变得更保守也更干净**：

> **六格中一格作废（RA × eQTLGen，错配对照自身显著）；余五格方向全部 > 1，四格 P < 0.05。**
> **癌症侧四格的错配对照在扫过的每一个窗口下都干净。**
> **非癌泛化只由 RA × CD4 一格支持。该格的单名单错配对照在七个窗口设置里只有一个
> （预注册主设置）干净；换成四份不相关名单后它不作废，但裕度 1.28 为全网格最窄。
> 最终措辞：**"强于四份不相关名单中的任何一份，但不具特异性"**，
> **不写成"泛化到非癌症成立"。**

⚠ 最后一条是本轮**最重要的新发现**，本文件初稿写的"500 kb 下不存活"低估了它，见 §二-I。

---

## 二、本窗口做了什么

### A. ★ 修掉了 v8 §二-C 那个审计链错误

`step124_full_grid.R` 把 MHC 排除版当主格并注释成 "pre-registered (S33)"，
已改回：**主格 = 全基因组**（S33 §4 定 N1 为主格、§5 的主检验无任何 MHC 排除），
MHC 排除两行改名 "(MHC excluded)" 并标 `posthoc_sensitivity`。

**legacy 复现逐位命中 S33 §8 登记表**：3.46（P=1.65e-5）/ 3.57（P=7.7e-14）/
错配 1.60（P=0.161）/ 排除 MHC 3.03 / 3.32，五行全中。

⚠ **一个数都没变**：改前改后 30 行数值载荷逐字节比对，18 行非 RA 行完全相同，
12 行 RA 行只是互换标签。**这次更正改的是"哪一行有资格叫主分析"，不是任何计算。**

### B. 网格加了两列，且比 v8 说的多一个取值

`region_filter` = `all_genome` | `MHC_excluded`
`analysis_status` = `prereg_primary` | **`prereg_sensitivity`** | `posthoc_sensitivity` | `legacy_reproduction`

v8 §三-A2 只提了三个取值。加第四个是因为 **500 kb 与 HCC-low 是在 S37 里冻结的**，
写在看到结果之前；把它们并进 `posthoc_sensitivity` 同样是错的标签。

### C. 逐位点错配诊断（`step125_mismatch_loci.R` → `125a`/`125b`）

距离判定**独立重写**（排序 + 二分查找，不调包内部函数），每格用 `stopifnot`
与 `cqtna_attribution()` 对账，两个计数逐格一致。

**⚠ 顺带更正了 v8 §三-A4 的数字。** v8 写的"19 个错配已知里 5 个纯 C1 扩散"
算的是**MHC 排除那一格**，即当时被错标成主格的那一格。
**预注册主格的正确数字是 24 个错配已知、其中 4 个纯 C1 扩散**
（chr5:34.74–35.64、chr20:34.56–35.56、chr21:42.22–42.91、chr22:21.46–22.42 Mb）。

**结论：错配对照失效的主因不是口径。** 24 个里 20 个真的靠近黑色素瘤 lead。

### D. ★ 更正之后才看得见的两件事

**1. RA × CD4 在预注册的 500 kb 敏感性下也过不了错配对照**（1.99×，P=0.039）。
之前这格显示干净（1.10×，P=0.522），那是 MHC 排除版在冒充主格。
**且 8 个错配已知位点无一来自 C1 扩散**——是实打实的 RA–melanoma 区域重叠，
不能归因于口径或块宽。
⚠ **这只是冰山一角**：后来的 S36 全窗口扫描（§二-H）发现它在**七个设置里六个失效**，
正文措辞据此降级得比"对分区尺度敏感"更狠。

**2. 那个事后口径在主窗口下根本没救回 eQTLGen**：MHC 排除版 RA × eQTLGen
在 1000 kb 下**仍然失效**（1.83×，P=0.0052），只在 500 kb 下才显得干净。
**换口径也没换来它想要的结论。**

### E. 把所有非网格的位点级数字也搬到同一单位

位点级统计量全部依赖分区，所以全部要搬，否则又是混用两个单位。

- **`step99` 加了 per-record 落盘**（`99d_r13_records.tsv.gz`，9 张表）。
  99a/99b/99c 重跑后**逐字节相同**——加落盘没动任何已发表数字。
  `.gitignore` 已加 `!99d_r13_records.tsv.gz` 例外（又踩了一次 v8 坑 #42）。
- **`step126_recompute_on_fixed_anchor.R`** → `126a`（迁移检验、三个额外 CD4 资源）、
  `126b`（容差扫描）、`126c`（容差 1.00 的可引用行）。
- **`step85e` 改造**：分区由 `LOCUS_METHOD` 环境变量选，两个分区各出一份表。
  ⚠ 顺带修了一个真 bug——**HCC 分支此前复用 `85a` 里存好的 `locus` 列**（单连锁写的），
  换分区时它不会跟着变。已改为每次从坐标重算。
  另把 eQTLGen 作为第四个数据集加进去（`92e` 在树里没有生成脚本）。

**关键新数**（详见迁移表）：

| | 旧（单连锁）| 新（固定锚定）|
|---|---|---|
| R13 迁移 | 2/2，9.6×，P=0.011 | **4/4，10.02×，P=9.1e-5** |
| 第三个资源 Schmiedel | 3.36×；错配 6.58×，P=0.033 → 失效 | 3.41×；错配 5.79×，P=0.042 → **仍然失效** |
| 密度匹配 melanoma×CD4 | 3.44×，P=0.041（分位分层，另一个检验）| **4.95×，P=0.0042** |
| 密度匹配 RA×CD4 | **没做过** | **4.50×，P=1e-4，全部合格容差下不变** |
| 匹配背景 melanoma | 4.36×，P=0.023 | **6.66×，P=0.0016** |

### F. 置换估计目标定稿（S38）

选 **B**：容差 1.00 + 强制随文报整张扫描表。解冻前提（S38 §5.3 要求分区问题先解决）已满足。

**判读结果**：melanoma 两格、HCC-high×eQTLGen、HCC-low 两格、RA 两格在**全部合格容差**下 P<0.05；
**HCC-high × CD4 在全部合格容差下 P ≥ 0.05**（1.00 时 P=0.105），
按 S38 §3 该格归属主张**降级为描述性**——它同时也是 Fisher 下唯一未达显著的一格，两个检验一致。

⚠ 容差 0.10 在四个格子上覆盖率不足 100%（43.9%–97.1%），按 §1.7 返回 NA。
**正文与补充材料都不得引用容差 0.10 的任何数字。**

### G. 正文、图、映射表全部更新

- `MANUSCRIPT_GB.md`：摘要、主结果段、迁移段、第二疾病段、第二资源段、
  第三资源段、**RA 段（整段重写）**、双轴交叉段、**置换段（整段重写）**、
  讨论、限制段、方法（分区规则写清了，并说明单连锁只作复现）、S26 单位敏感性段。
- 新增 `manuscript/NUMBER_MIGRATION_fixed_anchor.md`：**逐条列出改了什么、依据哪张表**，
  末尾带核销清单。
- 图 1：locus 划分由单连锁改为固定锚定。**独立实现，与 R 包结果一致**
  （meta 4/8 = 4.96×，背景 66/655 = 10.1%，与 `123d` 逐位相同）。
- 图 2：改读 `123d` + `126a` 一张表，作废格画成 VOID 斜纹格并写明错配倍数；
  panel c 加了作废那一行。九图全部重建通过。
- `FIGURES_GB_mapping.md` 12 处更新。
- S 编号：⚠ **S36 早已被 `SUPP_window_sensitivity.md` 占用（2026-08-17）**，本轮初次分配撞号，已改为
  `PREREG_locus_partition.md` = **S37**、`PREREG_permutation_estimand.md` = **S38**；
  正文的 `S9–S35` 范围同步改为 `S9–S38`。**加新补充材料前先扫一遍已占用的编号。**

### H. ★★ S36 窗口扫描：本轮最重要的新发现

`step128_window_sensitivity_fixed_anchor.R` → `128a`/`128b`/`128c`。
旧的 S36 建在单连锁上、且跳过 RA 两格（step108 未落盘）。重算后**八格全覆盖**，
并且**第一次跨窗口报了错配对照**——旧版没报，所以看不见下面这件事。

**RA × CD4 的错配对照，在七个互不相同的设置里只有一个干净，就是预注册的主设置。**

| KNOWN_KB | 100 | 250 | 500 | **1000（主）** |
|---|---|---|---|---|
| RA × CD4 错配倍数 | **4.17 ❌** | **3.03 ❌** | **2.38 ❌** | **1.71 ✅** |
| melanoma × CD4 错配倍数 | 0.00 ✅ | 0.00 ✅ | 0.00 ✅ | 0.00 ✅ |

LOCUS_KB 扫描同向（2.48 ❌ / 2.07 ❌ / 1.99 ❌ / 1.71 ✅）。

**三点判读**：

1. **单调**——窗口越紧，错误名单富集越强。说明"1 Mb 下干净"不是因为归属特异，
   而是因为 1 Mb 太粗，两份名单都开始命中大量位点，比值被稀释到 1 附近。
   **这一格是在对照分辨力最低的那个设置上通过的。**
2. **对比在同一资源内成立**——melanoma × CD4 与 RA × CD4 用同一套 Soskic 工具、
   同一分区、同一口径，前者八个设置错配倍数全是 0.00。**差别在疾病，不在方法。**
   ⚠ 但**不能说"癌症格都没事"**：`melanoma × eQTLGen` 的错配倍数轨迹与 RA × CD4
   **几乎重合**（4.11/3.44/2.55/1.59 对 4.17/3.03/2.38/1.71），只是它只在最紧的
   100 kb 失效。**"窄窗口 + 密集资源"是一般性的脆弱点，RA 只是最严重的那个。**
   初稿的 S36 §3 与正文讨论段都写成了"癌症格全都干净"，**已更正**——
   这是画图时才发现的（见 §四-51）。
3. **距离分布也一致**——RA 的 41 个命中只有 6 个在 50 kb 以内，中间段填了 10 个；
   癌症格是双峰的（要么几 kb，要么几 Mb）。RA 的命中正落在"窗口一收紧就掉出去"的距离带。

**处置**：按字面规则该格不作废（判据在预注册主检验上执行），
但已在正文、摘要、讨论、限制段一律降级为**描述性归属**，
并强制随文给出整张扫描表。决定与时间记录在 S37 §7.4。

### I. ★★ 多名单阴性对照（S39）——§三-B 那条"最该补的"已做

`step130_multilist_control.R` → `130a`/`130b`/`130c`，预注册见
`manuscript/PREREG_multilist_control.md`（**S39，先写后跑**）。

规则：树里**所有 ≥30 条 GRCh38 lead SNP、且不是该格自身结局**的名单，每格 4 份。
纳入 colorectal(642)/melanoma(157)/RA(83)/HCC(73)/prostate(35)；
breast(6)、lung(0) 因低于门槛排除（**排除在打分之前**）。**本轮不新取数据**——
免得"跑完不好看再多加几份名单"。

**★ 最重要的一条**：RA × CD4 在主设置下对四份名单的表现是

| 名单 | melanoma | **HCC** | colorectal | prostate |
|---|---|---|---|---|
| 倍数 | 1.71 | **3.05** | 1.77 | 1.04 |
| P | 0.102 干净 | **0.0028 富集** | 0.0045 富集 | 0.586 干净 |

**本研究当初指定的错配名单（melanoma）恰好是四份里对自己最有利的一份。
若当初指定 HCC，这一格会在预注册主设置下直接作废。**
⚠ 这是**单名单阴性对照设计本身**的缺陷，不是 RA 的局部问题，已写进正文与讨论。

**判读（S39 §5）**：八格**无一落入 C**，每格自身名单都强于任何不相关名单。
但裕度分层极清楚：

- 四个癌症格 **k = 0**（不相关名单一个都不命中）；
- melanoma×eQTLGen 2.99、HCC-low×eQTLGen 4.24；
- **RA 两格 1.28 / 1.32，全网格最窄**；
- 64 个扫描设置里**唯一反转的**是 RA × CD4 在 `locus_kb = 100 kb`（3.70 < 4.24）。

**按 §7/§8.5 部分上调 RA**：从"纯描述性"上调为
**"强于四份不相关名单中的任何一份，但不具特异性，裕度全网格最窄"**。
⚠ **不得**上调为"非癌症泛化成立"。

⚠ 另记：S39 §3 的**规则**与它下面那张**枚举表**原先不一致（表漏了 RA 名单，
少算一份对照）。脚本执行的是规则，表已更正并在文件里留了记录——
**注意那个错误的方向是让结果更好看**（melanoma×eQTLGen 会由 B 变 A）。

### J. 复现性核对

- `123d` 重跑 **md5 相同**（`2cb294f8…`）
- `126b`/`126c` 重跑数值相同（seed 1 下确定）
- `step85e` 在 `LOCUS_METHOD=single_linkage` 下**逐值复现**已发表的五行
- `step124_cells.R` 抽出为共享定义，`step124`/`step125`/`step128`/`step130` 同源；抽出后 `123d` 未变
- `step130` 的名单纳入是**按规则执行**的（读文件、数行数、比门槛），不是手写清单，
  所以门槛与结果在同一次运行里可见

---

## 三、★ 还剩什么

### A. 投稿前

1. ~~摘要与讨论再读一遍~~ **已做**。通读中查出两处把话说过头：
   讨论段"四个癌症格在每个窗口下都干净"（实为三格全程 0、melanoma × eQTLGen 最紧窗口失效），
   以及 RA 段"富集在每个设置下都稳定"（实为跨位点宽度稳定 3.70–3.96，
   跨已知窗口则升到 7.30）。**两处都已改。**
2. ~~S36 的表做成图~~ **已做**：`figures/FigS_window_control.pdf`
   （`make_supp_fig_window_control.py`）。两面板 = 两个"1 Mb"约定；
   实心点 = 该设置下对照失效。画图本身就查出了第 1 条里的错。
3. ~~`REVIEW_BRIEF_external.md` 未检查~~ **已做**：两处旧数已更新，
   并在顶部加了 §0-b 状态块（该文件**不在 `step127` 自动对账范围内**，是手工同步的，
   已在文件里写明"与 GB 冲突时以 GB 为准，并请把冲突报回"）。
4. **`92e` 未在新分区上重算**。它是 C2 口径，已由 `85e_matched_background_fixed_anchor.tsv`
   取代，正文已不引用它。若审稿人追问，说明口径差别即可（见 §四-49）。

### B. 应该做但不紧急

- ~~用多个预先选定的不相关疾病名单替代单一错配名单~~ **已做，见 §二-I（S39）。**
  仍欠一步：四份对照名单**全是癌症**。对 RA 而言确实不相关，
  但"四种癌症"不是"四种随机疾病"。**取非癌非免疫的名单（身高、T2D 等）需要新取数据**，
  本轮按 S39 §3 明确不做；若要做，须另起一份预注册。
- LD 区块分区（`locus_method = "blocks"`）作为关键敏感性，待样本内 LD 后升为主分析（S37 §1）；
- References 的 7 条未引条目与 DepMap release（见 v7 §三-A2/A3）；
- R 包发布：GitHub + Zenodo，占位符 `OWNER` 待替换，见 `cqtna_r/RELEASE.md`；
- R 包 README：确认没有任何单连锁分区上的置换数（S38 §7.3 那条警告）。

### C. 明确不做

- CRAN、百万级性能、X/Y 支持——投稿后再说。

---

## 四、技术坑（v8 第五节之外的新增）

46. **★ Bash heredoc 会把 `\t`/`\n` 折叠成真实字符——这是真的，本轮实测确认。**
    `python - <<'PYEOF'` 里写 `'sep="\\t"'`，python 收到的是**真实 tab**，长度 7 不是 8。
    结果是字符串匹配静默失败。**改字符串里含转义序列的代码，一律用 Write 工具写补丁脚本，
    并在脚本里用 `chr(92)` 拼反斜杠。**
47. **一个脚本"支持换分区"不等于它每个分支都换。** `step85e` 的 melanoma 分支调
    `assign_loci()`，HCC 分支却复用 `85a` 里**存好的 `locus` 列**。
    改了函数、加了 `partition` 列，HCC 行的数字纹丝不动——因为它根本没走那个函数。
    **加了口径开关之后，必须验证每个分支的数字都真的变了；不变的那个才是可疑的。**
48. **中间表里存着的派生列会把旧口径偷渡进新分析。** 同上。
    `locus`、`known` 这类列是**某个口径的产物**，不是数据。
49. **同名不同物**：`92e` 的 4.14/4.48 与新算的 6.49/5.66 不是同一检验的两个版本——
    92e 用 C2（分子只看显著记录），新的用冻结的 C1。**引用时必须同时写口径与分区。**
51. **★ 把表画成图，是发现自己把话说过头的最快方法。**
    S36 §3 与讨论段都写着"癌症格没有这个问题"。一画折线图，
    `melanoma × eQTLGen` 那条线**贴着 RA 那条一起往上走**，一眼就看出来了。
    表格按格子分行，读者（和作者）会逐行确认"这格 clean"；
    折线图把同一个量画在同一坐标里，**趋势相同但判定不同的两条线立刻并排**。
    **凡是要写"某一类全都如何"的句子，先把那一类画出来。**
50. **R 4.5.0 里没装 `cqtna`，装在 4.4.1。** `Rscript` 不在 PATH，
    用 `D:\R\R-4.4.1\bin`。PowerShell 里 `Rscript -e` 会被反引号/`\n` 咬，**写临时 .R 文件跑**。

---

## 五、方法论纪律（v8 第六节之外的新增）

42. **★ 判断某口径是不是预注册的，去读预注册文件的"设计"节，不要读"结果登记"节。**
    （v8 坑 #41 的升级版：本轮又验证一次——那个错误的诊断表也是在错的那格上算的，
    **一个错标会顺着传播到它下游的每一张表**。）
43. **修正一个标签之后，必须重跑它下游的每一个诊断。** 我差点把 v8 的 5/19 直接写进正文；
    它算的是被错标的那一格，正确答案是 4/24。
44. **"换个更宽松的口径能救活这一格"要先验证它真能救活。**
    MHC 排除版在主窗口下同样失效。**追结果的口径改动，往往连结果都追不到。**
45. **阴性对照不是正交的，就要在正文里说它不正交。**
    RA 与 melanoma 共享免疫区域，8 个错配位点无一来自口径扩散——
    这不是"对照不够干净"，这是**对照测的根本不是那个东西**。
46. **一个让结论变强的重算，和一个让结论变弱的重算，要用同样的力气去查。**
    本轮 HCC-high × eQTLGen 从 P=0.0515 变 0.0015（变强），
    RA × eQTLGen 作废（变弱）——两者都来自同一次分区更换，都必须写进正文。

---

## 六、文件地图（本窗口新增）

**决策**：`manuscript/PREREG_locus_partition.md`（**S37，冻结**）·
`manuscript/PREREG_permutation_estimand.md`（**S38，已定稿**）·
`manuscript/NUMBER_MIGRATION_fixed_anchor.md`（**改稿唯一清单**）

**脚本**：`step124_cells.R`（共享格子定义）· `step124_full_grid.R` ·
`step125_mismatch_loci.R` · `step126_recompute_on_fixed_anchor.R` ·
`step127_audit_manuscript_numbers.py`（正文↔表格对账，改完必跑）·
`step128_window_sensitivity_fixed_anchor.R`（S36 重建）·
`step129_selfcheck_on_fixed_anchor.R`（96a/100a 是否随分区变）·
`step130_multilist_control.R`（多名单阴性对照，S39）·
`step85e_matched_background.py`（`LOCUS_METHOD` 可选）· `step99_r13_transfer.py`（加落盘）

**表**：`123d`（全网格，含两个新列）· `125a`/`125b`（逐位点错配诊断）·
`126a`（非网格格子）· `126b`（容差扫描）· `126c`（可引用行）·
`128a`/`128b`（KNOWN_KB / LOCUS_KB 扫描，**含跨窗口错配对照**）· `128c`（连续距离）·
`129a`（自检表两分区对照）· `85e_matched_background_fixed_anchor.tsv` ·
`99d_r13_records.tsv.gz`（per-record）

**补充材料**：S30（加废止提示 + §10 归宿）· **S36**（`SUPP_window_sensitivity.md`，全面重算）·
**S37**（分区决策，加 §7 结果登记）· **S38**（置换估计目标，加 §7 扫描表）

**图**：`figures/Fig1`–`Fig9`（全部重建）· `figures/make_gb_all.py` ·
**`figures/FigS_window_control`**（S36 跨窗口错配对照，本轮新增，`make_supp_fig_window_control.py`）

---

## 七、下一轮建议顺序

1. **决定 S39 要不要配图**。多名单结果目前只有表；
   一张"每格 × 每名单"的点阵（自身名单一种颜色、不相关名单另一种，
   横轴倍数）会把"癌症格拉开、RA 挤在一起"这件事一眼说清
2. **考虑把 `FigS_window_control` 提进正文**——它和 S39 是本轮最强的两块证据，
   但正文已贴 8,000 词上限
3. 取非癌非免疫的对照名单（须另起预注册）
4. 然后才谈冻结投稿版本

⚠ **正文数字是自洽的，`manuscript/NUMBER_MIGRATION_fixed_anchor.md` 是它的账本。
改任何一个数之前，先在那张表上找到它的来源文件；改完跑 `step127`。**
⚠ **`step127` 只查数字。"某一类全都如何"这种句子它查不出来——见 §四-51。**
