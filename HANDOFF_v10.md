# 项目交接文档 v10

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-20（四轮第三方复核全部处理完毕；外部验证方案已采纳为 S40）
**取代**：`HANDOFF_v9.md`（其 §四技术坑、§五方法论纪律仍有效，本文件继承并扩充）
**工作树**：干净。本轮 commit `c9b7968` → `e8a7aee` 之后另有若干，共 **20 个**

> **新会话阅读顺序**
> 1. 本文件，尤其 **§三（还剩什么）** 与 **§五（方法论纪律）**
> 2. `review_packet_v9/README_审核要点.md` §0（四轮复核的逐条处置）
> 3. `manuscript/MANUSCRIPT_GB.md`（投稿母稿）
> 4. `manuscript/NUMBER_MIGRATION_fixed_anchor.md`（数字来源账本）

---

## 一、当前定位

**四轮复核后的裁决：内部统计证据基本通过，小修即可投；真正限制档次的是缺少独立外部验证。**

主分析口径（冻结，S37）：非递归固定锚定分区、最大跨度 1000 kb、归属口径 C1 `any_record`。

| | 结论 |
|---|---|
| **确证性主张** | **仅 melanoma 与 HCC 两个瘤种**——不是"所有肿瘤"，不是"通用规律" |
| 六格网格 | 一格作废（RA × eQTLGen，错配对照自身显著）；余五格方向全部 > 1，**四格达名义单侧 Fisher P < 0.05**（BH 后不变，但格子非独立复制）|
| 唯一未达显著 | HCC-high × CD4：Fisher 0.123、密度置换 0.107、窗口扫描依赖 ≥500 kb——**三个检验一致，描述性报告** |
| **RA × CD4** | 3.91×，P = 2.3×10⁻⁷，**探索性**。七个窗口设置里只有预注册主设置通过错配对照；多名单裕度 1.28 全网格最窄。**不进入正式证据链** |
| 密度置换 | randomized-greedy matching，行序不变，八格 10,000/10,000 有效抽样 |
| 历史口径 | 1 Mb 单连锁 + C2 仅复现已发表数字，不承担推断 |

---

## 二、四轮复核做了什么（压缩版）

**第一轮**（概念）：RA × CD4 降为探索性；S39 由"预注册"改为事后诊断；
两处我的事实错误更正（"melanoma 是最有利名单"→ 实为 prostate；"无限裕度"→ 全部有限）。

**第二轮**（技术）：**置换存在顺序依赖**——仅反转输入行序即改变经验 P。
已修（规范排序 + 每次置换随机化处理次序），八格判定无一改变。
**关键做法：规则+实现+测试在 `1bfb870`，结果在 `ea8bcad`，先后可由 git 核验。**

**第三轮**（算术）：我把"头号格子的移动 0.08"写成了"最大移动"，实为 **0.37**
（HCC-low × eQTLGen），六格下移。四处更正。

**第四轮**（文档）：banner、README、运行时长、126b 字段表述——
其中 126b 那句**是同一处第二次说过头**。

**外部验证**：复核者起草方案，**本项目已采纳为 S40**（见 §三-A1）。

---

## 三、★ 还剩什么

### A. 投稿前

1. **★★ S40 的三项前提，只完成了一项。**
   `manuscript/PREREG_external_validation.md` 已记录采纳；但
   - **合格外部队列：没有**（须与 FinnGen R12/R13、Rashkin GCST90011809、
     Landi 参照名单的全部贡献队列无 participant 重叠 + 保管方签署谱系表）；
   - **公开注册取得 DOI：没有**；
   - 锁定容器与 SHA-256 清单：没有。
   ⚠ **在拿到 DOI 之前，任何材料都不得称该方案为 pre-registered**，
   只能说 "adopted and frozen"。正文已按此措辞。
2. **★ 两个占位符，只有作者能填**（`make_review_packet.sh` 每次构建都会列出）：
   - `cqtna_r/DESCRIPTION` 的 `MR audit project` / `noreply@example.com`；
   - 正文的 `⟨repository DOI⟩`。
3. **正文聚焦**（第三轮 Reviewer 3）：读者容易把患者单细胞分析、TPI1 功能叙事
   与 outcome-architecture 审计当成同一条验证链。**外部验证只验中心那一条**，
   正文主次分层还可以再收。
4. `REVIEW_BRIEF_external.md` 是手工同步的，不在 `step127` 对账范围内。

### B. 应该做但不紧急

- 因果基因命名基准仅十个 melanoma 位点，覆盖不足以给出稳定错误率；
- 非癌非免疫的对照名单（身高、T2D 等），需新取数据 + 另起预注册；
- LD 区块分区作为关键敏感性（S37 §1），待样本内 LD。

### C. 明确不做

- CRAN、百万级性能、X/Y 支持。

---

## 四、技术坑（v9 §四之外的新增）

53. **★★ 随机化算法里，"循环顺序"是隐藏参数。** `for (L in sig)` 看似无害，
    但各候选池大小不同，**顺序决定同一条随机数流怎么切**。反转输入行序就能改 P。
    **任何按元素循环消耗 RNG 的实现，都要写一条行序不变性测试。**
54. **`tapply`/`split` 的分组顺序按字符串排序，受 locale 影响。**
    跨机器复现时这是第三个自由度。位点键要解析成数值再排。
55. **roxygen 块与函数之间不能插东西。** 在 `#'` 块和函数定义之间插入辅助函数，
    会让那个块去文档化辅助函数，原函数的 `.Rd` 被删。插在块**之前**。
56. **控制台字符串超过 ~110 字符会被终端折断**，可能把句号丢到下一行。
    **每行控制在 80 字符内，并渲染一次再确认**，不要目测。
57. **`Authors@R` 在 `R CMD check` 源目录模式下不展开**，会报缺 Author/Maintainer。
    **先 `R CMD build` 再 check tarball。**

---

## 五、方法论纪律（v9 §五之外的新增）

50. **★★ "先写规则后跑"如果不能被 git 证明，就等于没写。**
    S39 因文件/脚本/结果同提交被判为事后设计——**判得对**。
    S38-A1 改成两次提交：先规则+实现+测试，后结果。
    **可核验的顺序不是形式主义，它是这句话唯一的证据。**
51. **★ 补完一个缺口之后，要重新量一遍再写句子。**
    126b 那处连续两次说过头：先是"两张表都保存了"（实际缺四个字段），
    补完后又写"40 行全部带值"（实际 34 行有值、6 行按规则为 NA）。
    **"我修好了"不是描述，是意图。**
52. **★ 修复一个自由度之后，要看它把结论推向哪边，并主动说出来。**
    本轮修复让 melanoma × CD4 的 P 从 0.0042 变 0.0050——更接近 0.05。
53. **★ 采纳一份别人写的方案，不会让它变成"事前"的。**
    S40 与第三方原文分成两份文件：**方案是收到的（他人、事后），
    采纳是我们做的（本项目、有日期）**。就地改写会制造第三次同类错误。
54. **错的方向值得单独看一眼。** 本轮两处算术错误——"最大移动 0.08"、
    "40 行全部带值"——**方向都恰好让自己的工作显得更好**。
    这不必然是动机问题，但**它是一个该主动检查的模式**。
55. **把表画成图，是发现自己把话说过头的最快方法。**（v9 §四-51 的重申，本轮又验证一次。）

---

## 六、文件地图

**决策与预注册**
`PREREG_locus_partition.md`（**S37**，冻结，含 §7 结果登记）·
`PREREG_permutation_estimand.md`（**S38**，设计冻结、容差为分析选择）·
`AMENDMENT_S38_permutation_order.md`（**S38-A1**，顺序依赖的缺陷/修复/事前判读/前后对照）·
`PREREG_multilist_control.md`（**S39**，**事后探索性**，不得用于升级）·
`PREREG_external_validation.md`（**S40**，外部验证采纳记录）·
`EXTERNAL_VALIDATION_PROTOCOL_v9_3.md`（第三方起草原文，**未改写**）·
`SUPP_window_sensitivity.md`（**S36**）· `CONVENTION_REVIEW.md` ·
`NUMBER_MIGRATION_fixed_anchor.md`（**改稿账本**）

**脚本**
`step124_cells.R`（共享格子定义，其余脚本 source 它）· `step124`（主网格）·
`step125`（逐位点错配诊断）· `step126`（非网格 + 置换）· `step127`（正文↔表格对账）·
`step128`（窗口扫描）· `step129`（自检表两分区对照）· `step130`（多名单对照）·
`step131`（从 126b 渲染 S38 §7.1）· `make_review_packet.sh`（组包 + 完整性 + 占位符闸门）

⚠ 全部脚本解析自身目录，支持 `Rscript x.R <dir>` 或 `CQTNA_DIR`。

**R 包**：`cqtna_r/` 0.3.0，`R CMD check` **OK**，**268 项断言**
（含 25 项行序不变性/诊断/门槛测试）

**第三方材料**：`review_in/`（复核者自己写的六个验证脚本，标了来源）

---

## 七、下一轮建议顺序

1. **填两个占位符**（维护者信息、deposit DOI）——最小、最快、且是投稿阻断项
2. **推进 S40 的第一项前提**：找合格外部队列。
   ⚠ 判据是**队列谱系**不是发表年份；FinnGen DF13 不合格（同一参与者延长随访）
3. 公开注册（OSF / Registered Report），拿到 DOI 后正文措辞才可改为 pre-registered
4. 正文主次分层再收一轮（§三-A3）
5. 然后才谈冻结投稿版本

⚠ **改任何一个数之前，先在 `NUMBER_MIGRATION_fixed_anchor.md` 找到它的来源文件；
改完跑 `step127`。`step127` 只查数字，查不了措辞——
"全部/每一个/无一"这类句子它查不出来（§五-55）。**
