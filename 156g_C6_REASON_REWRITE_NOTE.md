# 致两位 C6 编码者：请重写 13 条裁定理由

**日期**：2026-09-07
**文件**：`156d_C6_disagreements.tsv`（13 行）

---

## 1. 发生了什么——错在我们，不在你们

你们裁定那 13 篇时，**逐篇写了理由**。我们看到过，它们确实在文件里。

然后我们自己的脚本把它们删了。

`step156_estimator_provenance_coding.py` 的评分环节每次运行都会重写
`156d_C6_disagreements.tsv`：它把 `adjudicated`（码）读回来保留，
**却把 `reason` 列写成空字符串**。你们交回文件之后，我们又跑了两次评分，
于是 13 条理由被覆盖了两遍。

**git 里也没有**——涉及这个文件的两个提交，`reason` 列都已经是空的。
不存在可以恢复的副本。

**这是我们的缺陷，不是你们的疏漏。** 脚本已经修好：现在同时保留两列，
**任何会把非空内容写成空的写入一律拒绝**，并对没有理由的裁定行逐条告警。

## 2. 没丢的东西

| | 状态 |
|---|---|
| 你们的 13 个裁定**码** | **完好** |
| 你们各自的 A/B 编码文件（码、引文、出处） | **完好** |
| W 10 / M 28 / X 7 / U 1，22.2% | **不受影响** |
| κ = 0.534、一致率 71.7% | **不受影响**（裁定前算的，来自你们两份原始文件）|

丢的只有**裁定那一步的书面理由**——也就是证据链。

## 3. 请你们做什么

**不是重新裁定。** 是把你们当时已经做出的决定的理由重新写下来。

`156d_C6_disagreements.tsv` 每行都摆着你们当时看到的全部材料：

| 列 | 内容 |
|---|---|
| `coderA` / `A_quote` / `A_where` | A 的码、依据原句、出处 |
| `coderB` / `B_quote` / `B_where` | B 的码、依据原句、出处 |
| `adjudicated` | **你们共同定下的码（已保留，请勿改动，除非见第 4 条）** |
| `reason` | **← 请填这一列** |

理由写一句即可，说清**为什么那个码胜出**。例如当时那种写法：
"补充表是该基因的实际估计量，方法学那句是通用描述。"

⚠ **两人共同写**，与当初裁定同一规格，不要一个人补完。

## 4. 如果重写时你们认为原来的码错了

**可以改，但必须留痕**：把 `adjudicated` 改掉，并在 `reason` 里
**明写这是一次改动**以及理由。

⚠ **不要为了让理由好写而去改码。** 我们要的是当时那个决定的理由；
只有当你们现在确信当时判错了，才改。

## 5. 两条边界

1. ⚠ **不要打开 `156f_C6_bounds.tsv`。** 那是我们在你们裁定前就算好的
   敏感性分析（13 篇不同裁法会让最终比例在 17.4%–45.7% 间移动）。
   它当时对你们保密，现在同样应当保密——尤其在第 4 条允许改码的前提下。
2. ⚠ **不要看 `151a_estimator_usage.tsv`**（自动词组扫描），理由同当初。

## 6. 我们会怎么标注这批理由

**如实标注为"原始记录遗失后于 2026-09-07 重写"**，而不是"裁定时记录"。

这两者不是一回事：当时的理由是与决定同时产生的，重写的理由是在
知道结果之后回溯的。我们不会把后者说成前者。

论文与 S51 现在写的是"裁定理由被我们自己脚本的缺陷销毁、正在重新记录"，
拿到你们重写的版本后会改成上面那句标注。

## 7. 交回方式

把填好 `reason` 列的 `156d_C6_disagreements.tsv` 交回即可。我们会跑：

```
python step156_estimator_provenance_coding.py . score
```

它现在会检查每一条裁定是否都带理由，并在缺失时逐条点名。

抱歉给你们添了这次返工。
