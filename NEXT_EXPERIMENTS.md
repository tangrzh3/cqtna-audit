# 进一步实验规划

**日期**：2026-08-23
**依据**：S42（恒等式与分解）、S44（七口径基准）、`BENCHMARK_vs_NC_reference.md`
（对照 Zhang et al. 2026 *Nat Commun*）

---

## 0. 现在的证据状态，先说清楚

S44 之后，论文的核心主张已经不是"melanoma 上观察到 4.96 倍"，而是：

> **单变异 Wald cis-MR 的检验统计量恒等于结局 GWAS 的检验统计量；
> 由此提名名单是结局 GWAS 在更宽阈值下的重读。剔除结局自身已达 5×10⁻⁸ 的位点后，
> 七个常用口径（Wald / IVW / IVW-MRE / weighted median / coloc×2 / SMR-HEIDI）
> 无一还剩下显著的位点层归属。**

**这条主张已经足够硬，缺的是"所以应该怎么做"。** 下面按此排序。

---

## 1. ★★ 建设性贡献：把分解做成包里的第九项检查（**最高优先，成本最低**）

**做什么**：`cqtna_decomposition()` —— 把提名名单按"该位点在结局 GWAS 中是否已达
全基因组显著"分层，报两套归属（全部 / 剔除后）。

**为什么值**：论文目前是纯破坏性的。对照的 NC 论文之所以是 NC，一半在于它给了
可用的东西。这个检查**从我们自己的发现里直接长出来**，任何人跑完 MR 都能算，
零额外数据。

**输入**：候选表 + 结局 GWAS 的 P（后者本来就在手，因为 MR 就是用它算的）。
**工作量**：一个函数 + 文档 + 测试，半天。
**风险**：低。

---

## 2. ★★ I 类错误标定（对照论文做了，我们没有）

**做什么**：在暴露与结局无关的零假设下，C1 归属的单侧 Fisher 检验拒绝率是多少？

**怎么做**：保持位点划分与已知名单不变，**置换结局侧的 z**（按染色体块置换以保留
LD 结构），重跑归属，统计 10,000 次的拒绝率。

**为什么值**：任何人提出一个诊断，第一个被问的就是它的 I 类错误。
`step132/133` 标定的是 S40 闸门的**功效**，从未标定过诊断本身的**大小**。

⚠ **预期结果可能不好看**：C1 用 1 Mb 窗口 + `any_record`，在名单密的疾病上
背景 known 占比高达 40%，**很可能超额拒绝**。若真如此，那也是必须报的——
而且它正好解释 S41 里 breast/prostate 为什么全线富集。

**工作量**：一到两天。**风险**：中（结果可能对我们不利）。

---

## 3. ★ 位点特异背景（借自对照论文的核心洞见）

对照论文：*"the locus-specific background is much higher than genome-wide
background, suggesting a one-size-fits-all cutoff might not be appropriate."*

**做什么**：把归属的背景从**全局** known 占比换成**位点特异**的——
例如按染色体、按基因密度、或按该位点区域内的已报告位点密度分层。

**为什么值**：直接回应我们自己列出的"估计量依赖约定"这条限制，
也是 S43 里"fold 上限由名单密度决定"那个问题的根治办法。

**工作量**：两到三天。**风险**：中（需要新的约定，必须事前冻结）。

---

## 4. ★ 阈值扫描：重读与新增的分界在哪

**做什么**：把 BH 阈值从 0.05 扫到更严，看"剔除 GWS 后仍显著"这件事
在什么阈值下会出现（如果会）。现在 BH 0.05 对应结局 P ≤ 2.1×10⁻⁴。

**为什么值**：把 S42 的分解从一个点变成一条曲线，回答
"那用更严的阈值呢"——这也是审稿人会问的。

**工作量**：半天（`step141` 已有机器）。**风险**：低。

---

## 5. 我**不建议**做的

| | 为什么 |
|---|---|
| 虚拟扰动 / 通路富集 补"验证" | `findings.md` 早写过：继承训练数据的相关性，解决不了归属问题，只会被读成补软证据 |
| 再加第三个暴露资源 | 恒等式说明暴露侧不决定显著性，**加暴露资源不改变任何结论** |
| 追 S40 的外部验证 | 已证明结构上不可得（缺口 9–13 倍），不是资源问题 |
| 为 TPI1 补湿实验 | TPI1 已被自己的六层证据判为不可升级；补实验是在救一个我们自己不主张的东西 |

---

## 6. 需要作者/机构，我做不了

1. **容器镜像与 `renv.lock`**（`S40_LOCK.md` §2）；
2. **两个占位符**：`cqtna_r/DESCRIPTION` 的维护者信息、正文的 `⟨repository DOI⟩`；
3. **目标刊决定**，摘要要不要从 344 词再砍（NC 150 / Genome Biol 250）；
4. **正文引文是否从 47 条扩充**——若要扩，我可以逐条核实着加，但不凭印象加。

---

## 7. 建议顺序

**第 1 项 → 第 4 项 → 第 2 项 → 第 3 项。**

理由：第 1 项把论文从纯破坏变成有交付物，最便宜；第 4 项半天且巩固核心主张；
第 2 项风险中但**不做会被审稿人问倒**；第 3 项最贵，且需要新的事前冻结。

⚠ **第 2 项可能给出对我们不利的结果**（诊断超额拒绝）。
**那种情况下仍然要报**——本文全部价值就建立在"报告对自己不利的东西"上，
而且它能解释 S41 的 breast/prostate。
