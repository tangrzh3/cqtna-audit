# 位点归属口径：结论、证据、以及你怎么复核

> **⚠ 本文件的开头结论已被后续工作推翻。先读这一段再读下文（2026-08-20 加注）。**
>
> | | 当时（2026-08-18）| **现在（已冻结，S37）** |
> |---|---|---|
> | 归属口径 | 不采纳 C1，维持已发表的 C2 | **采纳 C1 `any_record`** |
> | 位点分区 | 1 Mb 单连锁 | **非递归固定锚定，最大跨度 1000 kb** |
>
> **为什么翻转**：当时判 C1 不可用，是因为在**单连锁**分区上 C1 让阴性对照失效。
> 后来查明**两个问题同源**——单连锁把染色体臂串成 30.8 Mb 的块，
> 而 C1 会把这种宽块内的 known 状态扩散开。换成有界分区后，
> 三种口径在不串联的位点上**结果一致**，对照也恢复干净。
> 即：**当时看到的是分区的病，误诊成了口径的病。**
>
> 下文完整保留，因为它记录了那次误诊的推理与证据；
> **但其开头的"结论"行不再有效**，以 `manuscript/PREREG_locus_partition.md`（S37）为准。

**日期**：2026-08-18 · ~~**结论：不采纳评审建议的 C1 口径，论文数字维持已发表值。**~~
~~**理由：C1 让阴性对照失效——而阴性对照是本研究自己预注册的裁判。**~~
（↑ 已推翻，见上方加注。）

---

## 一、事情的经过

外部评审（P0-1）指出模块 A 混用了三套口径：

- 分母：某位点只要**任一记录**靠近已知 lead SNP 就算 known；
- 分子：只看该位点的**显著记录**；
- 基因标签：按**单条记录**判，于是一个基因可能被标成 novel 而它所在的位点被算作 known。

**这三条里，第三条是无争议的 bug，已修**（基因标签一律继承位点状态，同一位点内必然一致）。

前两条不一致也是真的，但"改成一致"有三种改法，而它们给出不同答案。

---

## 二、三种口径

| 代号 | 分子（显著位点算不算 known） | 分母 | 是否自洽 |
|---|---|---|---|
| **C1 any-record** | 位点内**任一记录**靠近 | 同 | ✅ 自洽（评审建议的） |
| **C2 sig-record** | 位点内**显著记录**靠近 | 任一记录 | ❌ 不自洽（**已发表的**） |
| **C3 lead-variant** | 位点的**最显著记录**靠近 | 同 | ✅ 自洽 |

---

## 三、★ 决定性证据：阴性对照

预注册 §5/§6-E 的原话：**用错配疾病的已知位点名单打分，若也给出显著富集，该格作废。**

用它当裁判，三种口径的表现是：

### melanoma × eQTLGen 全血（密集资源）

| 口径 | 匹配名单 | **错配名单（HCC 名单打 melanoma）** | 判定 |
|---|---|---|---|
| C1 any-record | 4.88×，P = 1.3e-13 | **3.90×，P = 1.7e-4** | ❌ **对照失效 → 该格作废** |
| **C2 sig-record（已发表）** | 4.44×，P = 3.7e-11 | **1.30×，P = 0.41** | ✅ **干净** |
| C3 lead-variant | 6.32×，P = 1.6e-14 | 2.66×，P = 0.096 | ⚠ 擦边 |

RA 那格同样：错配对照从 **1.60×（P = 0.16，干净）**变成 **2.40×（P = 0.0078，失效）**。

### melanoma × CD4 时程（稀疏资源）

| 口径 | 匹配 | 错配 |
|---|---|---|
| C1 | 4.09×，P = 0.028 | 0.00×，P = 1 |
| C2 | 4.09×，P = 0.028 | 0.00×，P = 1 |
| C3 | 4.66×，P = 0.020 | 0.00×，P = 1 |

**CD4 那格在三种口径下全部稳健**，匹配值几乎不动、错配一律 0.00。

---

## 四、机制：单连锁把染色体臂串成了一个"位点"

| 格子 | 位点跨度中位数 | 最大 | >10 Mb 的位点 |
|---|---|---|---|
| melanoma × Soskic CD4 | **62 kb** | 4.6 Mb | **0** |
| melanoma × **eQTLGen** | **1,400 kb** | **30,778 kb** | **30** |
| HCC-high × Soskic | 63 kb | 8.7 Mb | 0 |

eQTLGen 的 30 个显著"位点"跨度从 445 kb 到 **30.8 Mb（588 条记录）**。
一块 10–31 Mb 的区域里，**几乎任何疾病**的已知 lead SNP 都能找到一个。
所以 C1 的"任一记录靠近"在这种资源上等于问"这个区块有多宽"，不是问信号在哪。

改变状态的那个位点是 `1_20`，**chr1:107.1–120.0 Mb，跨度 12,954 kb，105 条记录**，
其中没有任何一条显著记录靠近已知 SNP。

**这就是为什么 20/30 → 22/30：串联，不是归属变强。**

---

## 五、结论

1. **论文数字维持已发表值**（4.09× / 4.44× / 8.85× / 17.62× / 3.46× / 3.57×）。
2. **C2 的不自洽要在正文写明，但方向是保守的**：分子比分母严格，
   所以报出的倍数是**低估**而非高估。这与本文一贯的保守取向一致，
   且与窗口敏感性（收紧窗口倍数上升）指向同一结论。
3. **基因标签的 bug 已修**（无论选哪种口径都必须修）。
4. **单连锁串联必须写进 Limitations**：在密集暴露资源上，
   1 Mb 单连锁产生的"位点"可达 31 Mb，位点级计数在那里计的是区块。
   ⚠ 这条本身就是本文主题的又一个实例——一个约定在悄悄干活。

---

## 5b. ★ 已补做：§5 第 4 条检验的结果（2026-08-18）

我在 §5 里说"最直接的下一步检验"是**在不产生串联的位点分区上重跑三种口径**。
补做了，结果**推翻了口径之争本身**：

**eQTLGen × melanoma，随位点窗口收紧：**

| locus_kb | 最大位点跨度 | C1 匹配/错配 | C2 匹配/错配 | C3 匹配/错配 |
|---|---|---|---|---|
| **100** | **1,182 kb**（仍有 **5** 个显著位点超窗口 5 倍） | **8.02 / 1.47，P = 0.334** | **8.02 / 1.47，P = 0.334** | 8.23 / 1.54，P = 0.308 |
| 250 | 5,347 kb | 6.98 / 3.10，**P = 0.038** | 6.98 / 2.32，P = 0.137 | 7.62 / 2.71，P = 0.096 |
| 500 | 13,291 kb | 6.74 / 4.04，**P = 0.0027** | 6.74 / 2.02，P = 0.182 | 8.46 / 2.91，P = 0.080 |
| 1000 | 30,778 kb | 4.88 / 3.90，**P = 0.00017** | 4.44 / 1.30，P = 0.411 | 6.32 / 2.66，P = 0.096 |

**三条结论：**

1. ⚠ **更正（2026-08-18，第三方复核指出）：100 kb 下并非"不串联"。**
   最大显著位点跨度仍有 **1,182 kb**，且有 **5 个显著位点超过窗口的 5 倍**。
   正确的说法是：**100 kb 下仍有残余串联，但在这份数据上它没有造成
   C1/C2 对显著位点的分类差异**（两者都给 8.02 / 1.47）。
   **C1 与 C2 相同不能证明"没有串联"**，只能说明串联在此处未跨过判定边界。
   逐窗口的 `sig>5x win` 计数见 `python review_convention.py`，它在
   **任何**窗口下都不为零——所以没有哪个窗口可以被称作"干净分区"。
2. **C1 的阴性对照随串联加剧而单调恶化**（0.334 → 0.038 → 0.0027 → 0.00017），
   C2 的始终干净。这坐实了机制解释，也回答了 §4.1 的反方质疑：
   不是"C1 的对照终于开始工作"，而是 **C1 在串联下把区块宽度读成了归属**。
3. ⚠ **100 kb 下倍数更高（8.02 vs 4.44）且对照更干净。**
   即论文在 1 Mb 报的 4.44 相对于 100 kb 的 8.02 是**保守**的。

**因此本判断的正确形式是 §8 的 C，不是 A：**
维持 C2 只是"在坏分区上挑了个不出错的口径"，
**真正该做的是承认 1 Mb 单连锁在密集资源上产生退化分区**，
并把位点窗口敏感性从补充材料提到与主结果并列的位置。

复现：`python review_convention.py`（脚本已含该扫描逻辑，见 §6.2）。

---

## 六、★ 你怎么复核（三条路，从快到慢）

### 1. 三行代码，看阴性对照怎么说（5 分钟）

```r
library(cqtna)
mr <- as_cqtna_mr(cqtna_demo("mr"), build = "GRCh38")
kn <- as_cqtna_known(cqtna_demo("known"),    build = "GRCh38")
mm <- as_cqtna_known(cqtna_demo("mismatch"), build = "GRCh38")

for (k in c("significant_records", "any_record", "lead_variant"))
  cat(k,
      "matched",    cqtna_attribution(mr, kn, known_from = k)$fold,
      "mismatched", cqtna_attribution(mr, mm, known_from = k)$fold, "\n")
```

包里现在把口径做成了显式参数，并且**错配对照显著时 `cqtna_audit()` 会报警**
（`negative_control_failed`）。判据不是我的意见，是预注册写死的。

### 2. 逐个位点看，而不是看汇总数字（20 分钟）

```bash
python review_convention.py eQTLGen
```

把**每一个在两种口径下状态不同的位点**摊开：坐标、跨度、该位点上全部工具变量、
各自到最近已知 lead SNP 的距离、以及是否显著。你要判断的是一道具体的题——
**"chr1:107.1–120.0 Mb 这 13 Mb、105 个基因，算不算一个'已知位点'？"**
不需要相信我的结论。

### 3. 全格对照表（1 分钟）

```bash
python step122_locus_convention_recheck.py     # 旧→新逐格对照，落 122a
```

### 复核时该盯住的三个问题

1. **阴性对照该不该当裁判？** 若认为不该，那预注册 §5/§6-E 就得一并推翻。
2. **13 Mb 的"位点"还算位点吗？** 若算，C1 成立；若不算，问题在位点定义而非口径。
3. **为什么所有改法都让数字变大？** 因为分母（背景）在 C1/C2 下都用"任一记录"，
   而分子越松倍数越高。真正中立的比较是 C3——它两端同一规则，
   结果是倍数**更高**且错配对照擦边，说明这个分区上位点级统计本身就不稳。

---

## 七、包里因此增加的东西

- `known_from = c("significant_records", "any_record", "lead_variant")`
  贯穿模块 A 与 G，默认复现已发表口径，三种都能跑；
- `cqtna_locus_spans()`：位点跨度诊断，显著位点跨度超过窗口 5 倍即**报警**；
- `cqtna_audit()` 在错配对照自身显著时**报警并置 `negative_control_failed`**；
- 合成测试 12、13 分别锁住"阴性对照失效必须报警"与"串联必须被检出"。

---

## ⚠ 更正记录（2026-08-18，第三方复核指出）

**我把一个事后稳健性分析升成了"预注册主口径"。**

`step124_full_grid.R` 曾在**看到 RA×eQTLGen 作废之后**，把 RA 的输入换成 MHC 排除版
作为主格，并把它注释为 "pre-registered (S33)"。核对 `PREREG_noncancer_outcome.md`：

- §4 只规定 **N1 = Soskic CD4 × RA 为主格**，N2 = eQTLGen 为次；
- §5 的主检验定义里**没有任何 MHC 排除**；
- MHC 排除行只出现在**结果登记表**，即看过 RA 结果之后加的稳健性分析。

**这与本文用来批评他人的失败模式是同一种。** 处置见 `HANDOFF_v8.md` §三-A1：
主格改回 MHC 包含，MHC 排除标为 post-hoc sensitivity，
legacy 复现改用 MHC 包含输入恢复 3.46 / 3.57。

⚠ 同时记下：**RA×eQTLGen 在 1000 kb 下作废，不得用 500 kb 挽救**——
冻结记录 §4 明写主分析失败即作废。

### 已执行（2026-08-19）

`step124_full_grid.R` 已改，网格已重跑。

**1. 主格改回 MHC 包含。** 两个 RA 主格现在读 `123b` 的 `N1 Soskic x RA` 与
`N2 eQTLGen x RA`（全基因组），MHC 排除的两行改名为 "(MHC excluded)"，
状态标为 `posthoc_sensitivity`。

**2. legacy 复现已恢复，且逐位命中 S33 §8 的登记表**：

| 行 | 本次 legacy | S33 §8 登记 |
|---|---|---|
| N1 Soskic × RA | 57/546，36 个显著、13 已知，**3.46×，P=1.65×10⁻⁵** | 同 |
| N2 eQTLGen × RA | 66/559，76 个显著、32 已知，**3.57×，P=7.7×10⁻¹⁴** | 同 |
| NC 错配（melanoma 名单）| **1.60×，P=0.161** | 同 |
| N1 排除 MHC | **3.03×，P=1.31×10⁻³** | 同 |
| N2 排除 MHC | **3.32×，P=2.07×10⁻¹¹** | 同 |

**3. 新增两列**：`region_filter`（`all_genome` / `MHC_excluded`）与
`analysis_status`（`prereg_primary` / `prereg_sensitivity` /
`posthoc_sensitivity` / `legacy_reproduction`）。
⚠ 比 `HANDOFF_v8.md` §三-A2 提的三个取值多一个 `prereg_sensitivity`：
500 kb 与 HCC-low 两格是在 `PREREG_locus_partition.md` 里冻结的，
写在看到结果之前，把它们并进 `posthoc_sensitivity` 同样是错的标签。

**4. 一个数都没变。** 改前改后 30 行的数值载荷逐字节比对：18 行非 RA 行完全相同，
12 行 RA 行只是**互换了标签**——同一批数字换了地位。
这次更正改的是"哪一行有资格被称为主分析"，不是任何计算。

### ★ 更正之后才看得见的两件事

**A. RA × CD4 在 500 kb 下也过不了错配对照（1.99×，P=0.039）。**
之前这一格显示为干净（1.10×，P=0.522），那是 MHC 排除版在冒充主格。
于是在预注册的 500 kb 敏感性下，**非癌结局一格不剩**。
按冻结记录 §4 第三行（"主分析与 500 kb 敏感性结论不一致 → 报两者，
措辞降级为'对分区尺度敏感'"），正文关于 RA 的措辞必须按此降级：
1000 kb 主分析下 RA × CD4 成立（3.91×，P=2.3×10⁻⁷，对照 1.71× P=0.102），
**但该格不在 500 kb 下存活**。

**B. 那个事后口径在主窗口下根本没救回 eQTLGen。**
MHC 排除版的 RA × eQTLGen 在 1000 kb 下**仍然失效**（1.83×，P=0.0052），
只在 500 kb 下才显得干净（1.48×，P=0.076）。
原注释"排除 MHC 把对照降到 1.03 倍"只对 CD4 那一格成立。
**即换口径也没换来它想要的结论——这恰恰说明它是在追结果，而不是在改进方法。**

---

## 八、逐位点错配诊断（`step125_mismatch_loci.R`，2026-08-19）

汇总的错配倍数说不出**哪些位点**在扛它，而两种机制留下的痕迹完全不同：

- **共享生物学／共享密度**：我们某条**显著**记录确实落在错误疾病 lead 的 1 Mb 内；
- **C1 状态扩散**：该位点**没有任何显著记录**靠近错误名单，
  它被算作 known 纯粹是因为同一块里某条**非显著**记录靠近。
  这是口径的性质，不是基因组的性质，而且在汇总数字里完全看不见。

脚本里的距离判定是**独立重写**的（排序 + 二分查找，不调包内部函数），
每格都用 `stopifnot` 与 `cqtna_attribution()` 对账，两个计数逐格一致。

### 主分析（1000 kb）

| 格子 | 对照 | 显著位点 | 错配已知 | 其中纯 C1 扩散 |
|---|---|---:|---:|---:|
| melanoma × eQTLGen | clean | 34 | 3 | **0** |
| RA × CD4 | clean | 41 | 7 | **0** |
| **RA × eQTLGen** | **FAILED** | 109 | **24** | **4（16.7%）** |
| RA × eQTLGen（MHC 排除，事后）| FAILED | 98 | 19 | 5（26.3%）|
| HCC-low × eQTLGen | clean | 4 | 1 | 1 |

其余各格错配已知数为 0。

### ⚠ 这里更正 `HANDOFF_v8.md` §三-A4 的一个数字

v8 写的"**19 个错配已知里 5 个纯 C1 扩散**"，以及那串位点
（chr2:202.0–202.9、chr5:34.7–35.6、chr20:34.6–35.6、chr22:21.5–22.4），
**算的是 MHC 排除那一格**——也就是当时被错标成主格的那一格。

**预注册主格的正确数字是 24 个错配已知、其中 4 个纯 C1 扩散**：
chr5:34.74–35.64、chr20:34.56–35.56、chr21:42.22–42.91、chr22:21.46–22.42 Mb。
（chr2:202.03–202.90 只在 MHC 排除格里出现，主格里不出现。）
正文与补充材料请用 **4/24**，不要用 5/19。

### 结论：错配对照失效的主因不是口径

**24 个里 20 个是真的靠近黑色素瘤 lead**（1000 kb 主格）。
C1 扩散只解释四分之一，且这四个块里最近的显著记录距离错配 lead
仍有 1,063–1,680 kb——**离得很远，纯粹是块宽把状态传过去的**。

所以两件事都要写进正文，且不能只写其中一件：

1. **RA 与 melanoma 确实共享免疫区域**，错配名单不是正交的阴性对照（§三-A5 的措辞成立）；
2. **另有一小部分是 C1 在宽块内的状态扩散**，与单连锁串联无关（这里最宽的块也只有 999 kb），
   也与 MHC 无关（四个块全在 MHC 之外）。

### 500 kb 下的对照

| 格子 | 对照 | 显著位点 | 错配已知 | 纯 C1 扩散 |
|---|---|---:|---:|---:|
| **RA × CD4** | **FAILED**（1.99×，P=0.039）| 43 | 8 | **0** |
| **RA × eQTLGen** | **FAILED**（1.87×，P=0.0024）| 123 | 22 | 1 |

⚠ **RA × CD4 在 500 kb 下失效，8 个错配已知位点无一来自 C1 扩散。**
即这一格的失效**不能**归因于口径或块宽，是实打实的 RA–melanoma 区域重叠。
把窗口收窄没有让它变干净，只是把它推过了显著线。
