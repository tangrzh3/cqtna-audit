# HANDOFF v13 — 2026-09-11

**仓库**：`D:\R_ex\MR`（git，`main`，**未 push**，约 255 个提交）
**上一份**：`HANDOFF_v12.md`

---

## 0. 下一个窗口第一件事

**把 `step159` 第 7 组的范围从编号区间改为全仓库 `step*`，重跑一次验收。**

```python
# step159_container_acceptance.py 当前：
GROUP7_GLOBS = ["step1[2-5][0-9]_*.py", "step1[2-5][0-9]_*.R"]
```

改成匹配全部 `step*.py` / `step*.R`。⚠ **务必保留现有 `GROUP7_SKIP`**，
它挡着三个会造成实际损害的脚本：

| 必须继续排除 | 为什么 |
|---|---|
| `step150_environment_lock.R` | **重新生成 `150a_environment.lock`**，会用已漂移的当前机器覆盖作者机记录 |
| `step143_list_provenance.py` | 按设计查询 GWAS Catalog **实时接口**，两周的目录增长会被误报成容器差异 |
| `step140/141/147` | 必跑清单已跑过，重复执行（`step147` 单独就 9 分钟） |

扩大范围后**预期会有脚本失败或产出差异**——那正是应该被发现的东西，不是应该被绕过的。
`run()` 已有"失败步骤自动撤销其改动"的保护。

重跑命令（Docker 需先启动，见 §4）：

```
docker run --rm -v "D:\R_ex\MR:/repo" -w /repo cqtna-audit:0.3.0 \
    python3 step159_container_acceptance.py /repo --reuse-baseline
```

⚠ **必须带 `--reuse-baseline`**。工作树里现在是容器产出，重新快照会把比对
变成"容器 vs 容器"、报出零差异。基线 `159d_baseline/` 保存的是首次运行前的
原始 Windows 表，已提交。

---

## 1. ⚠ 悬而未决：§8 的止损线可能已触发

**这是本轮最重要、也最未完成的一件事。**

第三方评审指出 S54 §9.6 的覆盖率主张有误。核实结果：

| | 数量 |
|---|---:|
| A 正文引用的数值（`step160`） | **191** |
| B 其中 `step127` 已对账 | 174 |
| **C 其中源表由容器重算（`step161`）** | **82** |

**C / A = 42.9%**，而 §8 写死：「可重跑覆盖率低于正文所引数字的一半 →
容器不足以作准绳，**退回 (a)**」。

### 我错在哪（两处，第二处更隐蔽）

1. §9.6 表格写"分母为**正文所引数字**：41/41 = 100%"。**41 是 `step127`
   的审计集，不是正文的 191 个。** 这是 §10.2 已更正过一次的分母替换，
   第二次犯，还贴了一个更权威的标签。
2. **把"已审计"当成了"已重算"。** `step127` 拿一个数去对一张表，
   而那张表**可能是从作者机挂载进来的**。两者是不同的量。

### ⚠ 但 42.9% 目前还不是关于可复现性的结论

**第 7 组的 glob 是 `step1[2-5][0-9]`，编号低于 120 的脚本从未被尝试过——
不是不能跑。** 已实测三个：

| 脚本 | 输入 | 结果 |
|---|---|---|
| `step53_power_stability.py` | 3 个，**一个不缺** | 跑通，`53b` 逐字节相同 |
| `step55_recovery_by_locus_class.py` | 1 个，不缺 | 跑通，`55a` 逐字节相同 |
| `step104_litaudit_validation.py` | 2 个，不缺 | 跑通，`104c` 逐字节相同 |

这三张表贡献正文 **15 个**数值。计入后 97/191 = 50.8%。

⚠ **不要据此宣布门槛通过**，两个理由：
- 这是在看到"42.9% 不达标"之后才去扩大范围的。方向上是收紧（更多东西被
  真正重算），但时序必须如实登记，同 §10.2 的处理方式。
- **还没查完**：`08`/`09`/`12`/`16` 的产出脚本尚未找到，
  不知道还有多少是同类误判。

**真正跑不了的**（依赖未分发的 GSE 系列原始数据）：
`step6`(coloc)、`step19b`(空间)、`step43`、`step44`、`step47`(染色质)、`step68`(单细胞)。

### 全仓库重跑之后要做的判断

跑完用 `step161` 重算 C/A，然后：

- **若 ≥ 50%**：§9.6 的 (b) 裁定成立，但**必须把 42.9% 与扩大范围的时序
  一并写进 §9.6**，不得只留最终数字。
- **若 < 50%**：按 §4 第二种终局**退回 (a)**——`CONTAINER.md` §5 改回 (a) 措辞，
  正文写明退回理由。

⚠ §8 的"可重跑覆盖率"措辞本身有歧义（是"正文数字的重算比例"还是"必跑清单
的执行比例"）。**在看到结果之后挑一个能保住 (b) 的读法，正是 S54 存在的意义
所要防止的。** 若要采用第二种读法，必须由作者署名裁定并注明该判断作于
观察结果之后。

---

## 2. 第三方评审的四条：已处理三条，第一条见 §1

| # | 意见 | 状态 |
|---|---|---|
| 1 | 41/41 不能证明全文覆盖率 | ⚠ **未完成**，见 §1 |
| 2 | "无印出数字变动"需明确基准 | **待改**：`6.489→6.546` 在印出精度上就是 `6.49→6.55`。应改为"修正规则并登记迁移后，最终跨环境验收未发现**新增**的印出精度差异" |
| 3 | 两包漏锁属"尚未修复"而非"不可修复" | **待改**：`DEPOSIT_GAPS.md` §1 现在的理由只支持"不应重生成 `150a`"，不支持"无法修复"。可另建补充版本清单、钉死安装版本、加运行时检查 |
| 4 | `CONTAINER.md` 指引冲突 | **部分完成**。§4 表格与 §5 措辞已更新；**L13"尚未构建"、L112"验收用精简镜像"仍在**，须标为已失效并指向当前方法（精简镜像现在会被自己的闸门拒绝） |

**已修完的两处**（本轮）：
- Discussion 八条检查中**四条是断句**（"counting."、"alongside."、"as well as."、
  (viii) 无动词）——上一轮压缩从句子中间截断所致，已从 S49 恢复完整表述
- 可用性声明称"能复现正文每一个数"且"钉住两个外部二进制"——**两者皆不实**，
  已按实情改写，列明四处缺口

---

## 3. 本轮完成的事

### 3.1 审计覆盖率 12% → 100%（可对账部分 174/174）

`step160` 度量覆盖率并**区分"不可对账"与"未对账"**（21 项不可对账均附理由）。
`step127` 从 41 个数扩到 174 个，新增 §1e–§2v 共 22 个块。

**查出正文错误 2 处**（已改并登记 `NUMBER_MIGRATION_fixed_anchor.md`）：

| 位置 | 错误 | 正确 |
|---|---|---|
| L421 | `13.53` | `12.09` —— 一句列举**密度匹配置换**的话里，有一个数取自**未匹配的网格表** |
| L1050 | `0.185` | `0.184` —— Steiger 有界 R² 下界实为 0.184479 |

⚠ **`step127` §1 当时正在为 13.53 背书**：该值在正文唯一的出现就是那处错误，
所以审计报 OK，改正后反而报 MISSING。**一个被错误满足的审计比没有审计更糟。**

**不准确声明 1 处**：TwoSampleMR 列在已安装软件清单并标版本号。
证据：无 `library()`、不在 `DESCRIPTION`、**git 全史无使用**、锁里没有、容器未装。
论文真正用到的是它的**公式**（自行实现），该处引用（L1048）保留。

### 3.2 容器：全量镜像与 §9.6 裁定

- `cqtna-audit:0.3.0`（`container/Dockerfile`）**闭包 226/226**，0 不符 0 缺失
- 七次构建查出**两条 deposit 缺陷**：`150a` 的 `Repositories` 只记 CRAN 却含
  40+ Bioc 包、`cqtna` 被记为来自仓库 —— **该锁文件按原样无法还原**，
  **只有真正构建全量镜像才会暴露**
- 09-07/08 精简镜像上的两次运行（闭包 42/226、11 处版本不符）按 §8 属
  **带伤重跑，结论已作废**（S54 §9.9）
- §9.6 已填 **(b) 容器为准绳** —— ⚠ **但见 §1，该裁定的依据之一有误**

### 3.3 我自己修掉的 7 处审计缺陷

**全部是"检查被文档别处的巧合满足"**：`nan` 通过、`"0.00"` 通过、
`"0.05"`/`"5.2"`/`"0.125"` 撞上别处、方向写反、P 值绑错行。
**七次全部靠读输出发现，没有一次是退出码告诉我的。**

另修：`step160` 从不读压缩表（12 张、1.8 GB）、两遍扫描优化从写下起就是死代码、
三个类别重叠导致覆盖率虚高 99% 实为 96%（已加断言强制划分完备）。

### 3.4 压缩：实测后放弃，作者接受超出

主文 **8,517 词**（Word 口径），目标 7,000–8,000。
⚠ **此前评审简报报的 8,752 是错的**。

先试"只收紧句子不删内容"：**三处改写只省 2 个词**。原因：按空白切分计数，
`3.85-fold` 与 `3.85` **都是一个 token**；且这些句子长是因为装了很多信息，
不是话多。外推 54 句全改约省 25–30 词，覆盖缺口 5%。

**作者决定按 8,517 投稿，超 6.5%。** 理由与备选压缩段落记于
`COMPRESSION_PLAN.md` §5（该文件的 §1 清单已过期，§5.1 已更正）。

---

## 4. 环境与操作

### Docker

镜像库已迁至 **E 盘**：`E:\DockerData\DockerDesktopWSL\disk\docker_data.vhdx`。
迁移过程踩过两个坑，**下次遇到直接照此处理**：

1. **Docker Desktop 的 robocopy 退出码 bug**：`/MOV` 复制成功返回 1，
   而 Docker 把非零当失败并回滚设置。**再点多少次 Apply 都一样。**
   绕法：先让旧磁盘不存在（改名），Docker 就不走 robocopy。
2. **AF_UNIX 套接字孤儿**：非正常退出后 `sailor-ingest.sock`、
   `docker-secrets-engine\engine.sock` 变成打不开、删不掉、改不了名的文件，
   挡住下次启动。绕法：**整个目录改名**，Docker 会重建。两处都要清，
   只清一处不成。

⚠ Docker 弹错误对话框时**不要点 "Reset to factory defaults"** ——
会抹掉 15 GB 镜像。要点就点 "Quit"。

遗留可清理：`C:\...\Docker\wsl\disk\docker_data.vhdx.bak-*`、
`E:\DockerBackup\*.stale-*`、`E:\DockerDesktopWSL`（空目录）、
`C:\...\Docker\run.broken-*`。共约 23 GB。

### 常用命令

```
python step127_audit_manuscript_numbers.py .   # 正文数字 ↔ 表（174 个）
python step153_prereg_inventory.py .           # 预注册计数（十七份）
python step154_crossref_audit.py .             # 交叉引用/图注/文献
python step155_renumber_references.py .        # 文献顺序（只报告）
python step160_audit_coverage.py .             # 审计覆盖率（约 17 秒）
python step161_container_number_coverage.py .  # 容器重算了哪些正文数字
python step152_set_identity.py                 # 残留占位符（20 处）
```

前五个当前全绿。

---

## 5. 提交前仍未完成

1. **§8 分母问题**（见 §1）—— **最高优先级**
2. 评审第 2、3、4 条（见 §2）
3. **作者身份 20 处占位符**：姓名/邮箱/ORCID/GitHub、Zenodo DOI、四条声明
4. **DOCX 过期**：`manuscript/MANUSCRIPT_GB.docx` 停在 09-07，
   **不含 `13.53→12.09`、`0.185→0.184`、`6.49→6.55`、TwoSampleMR 移除**。
   ⚠ **不要发出**。且它不在 git 跟踪范围内
5. **DOCX/PDF 逐页视觉检查**（Reviewer 3 上一轮的硬门槛，仍未做）
6. `REVIEW_BRIEF_v14.md` 中"主文 8,752 词"须改为 8,517
7. **push、冻结、存档**（版本号与哈希）；255 个提交全部未 push

---

## 6. 给下一个窗口的两条提醒

**① 先找产出脚本，再下结论。** 本轮我有**三次**把"我查错了表"报成了
"论文可能有问题"，还有**两次**把"我不会查"报成了"需要作者说明"——
而答案一直写在仓库里（一次在画同一张图的脚本里，一次在 findings 文档里
连条数都写明了）。**推给作者看起来谨慎，实则是把工作转移给一个
还得重新推导仓库已有记录的人。**

**② 用已发布的实现，不要重写。** MC1R 三个坐标卡了很久，
出路不是"更小心地重实现分区"，而是**导入 `step85e` 的 `assign_loci`**
（其文档明写与 `cqtna:::cq_assign_loci` 一致）。风险随之消失。
