# S46：归属诊断自身的 I 类错误、功效与 void 率标定——口径先定死

**编号**：S46　**日期**：2026-08-23
**脚本**：`step149_typeI_calibration.R`　**输出**：`149a_calibration.tsv`
⚠ **本文件写于看到任何标定结果之前。**
⚠ 事后分析（第三方评审 2026-08-23 提出），不改动任何注册终点。

---

## 1. 要回答的问题

本文提出一套诊断，却从未标定过它自己的**大小**。
`step132/133` 标定的是 S40 闸门的**功效**，不是这个检验的 I 类错误。

> 在结局信号与已知位点位置无关的零假设下，
> C1 归属的单侧 Fisher 检验以多大概率拒绝？

⚠ **事先声明：这一项可能给出对本文不利的结果。**
C1 用 1 Mb 窗口 + `any_record`，在名单密的疾病上背景 known 占比高达 40%，
**很可能超额拒绝**。若真如此仍须照报——而且它正好能解释 S41 里
breast 与 prostate 为什么错配对照全线富集。

---

## 2. 零假设怎么造（**关键设计选择，先定死**）

**不能**简单地在背景位点里随机抽 k 个当显著集：那正是 Fisher 检验假设的超几何零分布，
**按构造必然校准**，只能检出实现 bug，检不出估计量本身的毛病。

采用**染色体内环形平移**：把每条染色体上的 outcome z 向量整体循环移动一个随机偏移量。

- **保留**：z 的空间自相关（LD 造成的）、位点大小分布、变异密度、记录数；
- **打断**：信号位置与已知位点位置之间的对应关系。

这是基因组学里的标准零假设构造。**每次重复对每条染色体独立抽偏移量。**

⚠ **它保留不了的**：z 与该变异自身 MAF/LD 特征的关系。
平移后的 z 会落在一个 LD 结构不同的变异上。**这条界限必须与结果一同陈述。**

---

## 3. 三个量，三种设定（**全部事前固定**）

| 量 | 设定 | 判据 |
|---|---|---|
| **I 类错误** | 零假设：染色体内环形平移 | 单侧 Fisher P < 0.05 的比例。**名义值 5%** |
| **功效** | 备择：把 z 按 §4 的方式移向已知位点 | 同上，越高越好 |
| **void 率** | 零假设下，**错配名单**（HCC）也富集的比例 | 越低越好 |

每格 **10,000 次重复**，`set.seed(1)`。

## 4. 备择假设怎么造

在零假设的平移基础上，把**落在已知位点 1 Mb 内**的记录的 |z| 乘以放大因子
`g ∈ {1.25, 1.5, 2.0}`，其余不动，然后重算。
`g = 1` 即回到零假设，作为内部一致性核对。

## 5. 在哪些名单上做（**全部，不得挑选**）

melanoma（p_bg = 0.101）、lung（0.104）、breast（0.290）、
colorectal（0.318）、prostate（0.403）。

**选这五个是因为它们的背景密度跨越 4 倍**，而 S43 已经证明 fold 的上限由密度决定。
若 I 类错误随密度上升，那正是需要报告的东西。

## 6. 同时报告机会校正量 A

对每个设定报 fold 与 A 的分布（中位数与 95% 区间），
因为 S43 已证明 fold 不可跨名单比较。

## 7. 判读（事前）

- I 类错误在 **[0.03, 0.07]** 内 → 该名单上检验校准良好；
- **> 0.07** → **超额拒绝，必须在正文限制段写明，并给出受影响的疾病**；
- < 0.03 → 保守。

⚠ **无论结果如何都照报**，且不得只报对本文有利的名单。

---

## 8. 结果（`149a_calibration.tsv`，10,000 次/格，seed = 1）

| 名单 | 背景 known | **I 类错误** | g = 1.25 | g = 1.50 | g = 2.00 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| lung | 10.4% | **0.0048** | 0.754 | 1.000 | 1.000 | 保守 |
| HCC | 6.6% | **0.0177** | 0.452 | 0.974 | 1.000 | 保守 |
| breast | 29.0% | **0.0272** | 0.919 | 1.000 | 1.000 | 保守 |
| colorectal | 31.8% | **0.0105** | 0.947 | 1.000 | 1.000 | 保守 |
| melanoma | 10.1% | **0.0357** | 0.529 | 0.995 | 1.000 | 校准良好 |
| prostate | 40.3% | **0.0394** | 0.970 | 1.000 | 1.000 | 校准良好 |

**没有一份名单超额拒绝**，但"全部通过校准"是过强的说法：

1. **I 类错误**：六份名单**无一超过名义 0.05**，最高 0.0394。
   但按本文件 §7 自己的判据，**只有两份**（melanoma 0.0357、prostate 0.0394）
   落在 [0.03, 0.07] 的校准区间内；**另外四份明显保守**
   （lung 0.0048、colorectal 0.0105、HCC 0.0177、breast 0.0272）。
   ⇒ 正确的表述是：**无膨胀；四份保守，两份落在预设区间内。**
2. **功效**：见 §8.2，**是构造性情景下的功效，不是一般保证**。
3. **void 率**：零假设下错配名单（HCC）富集的比例为 **0.0177**。

### 8.1 ⚠ 事先的担忧没有兑现，而且方向是反的

S46 §1 事先写明："C1 用 1 Mb 窗口 + `any_record`，在名单密的疾病上
**很可能超额拒绝**"。**结果相反**：密度最高的三份名单
（breast 29.0%、colorectal 31.8%、prostate 40.3%）的 I 类错误分别是
0.0272 / 0.0105 / 0.0394，**没有一个超过名义值**。

⚠ **但由此能推出什么，必须按 §8.4 收窄。**
本标定只在 **melanoma 的 z 场**上做，因此它证明的是：
**在 melanoma 的检验位置与 z 空间结构下，名单几何本身不制造超额拒绝。**

**⇒ 对 S41 里 breast 与 prostate 错配对照全线富集这件事，
这条结果削弱了"诊断失准"这个替代解释，但没有排除它**——
要排除它，需要在 breast 与 prostate **各自的结局 z 场**上重做标定，本文未做。
先前版本写成"已排除"，那是过推。

### 8.2 功效随名单密度上升，这是构造使然

密名单（0.92–0.97 @ g = 1.25）比稀名单（HCC 0.45、melanoma 0.53）功效高，
因为"落在已知位点 1 Mb 内"的记录更多，备择假设动到的记录也更多。
**这不是密名单更好，只是同一个 g 在密名单上是一个更大的扰动。**

### 8.3 ⚠ 功效数字是构造性情景，不是一般保证

备择假设把**落在已知位点 1 Mb 内**的记录 |z| 整体乘以 g。
这会把显著位点中位数从约 10 抬到 25–81，是一个**强且人为构造**的扰动。

**因此 0.974–1.000 不能读作"该诊断在一般真实效应下接近满功效"**，
只能读作"在这个构造出来的备择下功效很高"。
真实效应的大小与空间分布都不由本设计决定。

### 8.4 ⚠ 只标定了一套 z 场

`step149` 只读 `13_meta_locus_annotation.tsv`——**melanoma 的结局 z**——
然后在同一批 655 个背景位点上平移它，**只更换六份已知位点名单**。

**所以它标定的是"名单几何在 melanoma 基准下的表现"，
不是六种疾病各自的诊断表现。** breast、prostate、lung 自己的结局 z 结构
（信号数目、强度分布、与 LD 的关系）从未进入这次标定。

**要标定它们，须以各自的结局 sumstats 重跑本流程。** 本文未做，
故不得声称"S46 已标定六种疾病中的诊断表现"。

### 8.5 这个标定还管不到的地方

⚠ 环形平移**保留不了** z 与该变异自身 MAF / LD 特征的关系——
平移后的 z 会落在一个 LD 结构不同的变异上（§2 事先写明）。
因此本标定说明的是**位置层面**的零假设下检验是校准的，
**不**说明它对"信号强度与局部 LD 结构相关"这类偏差免疫。
