# 事后探索性敏感性分析：用多个不相关疾病名单替代单一错配名单

**写于 2026-08-20。**
（⚠ 初稿手写为 2026-08-19，且声称"写作先于运行这一点可由 git 核对"——
**两句都已更正**：本文件实际提交于 2026-08-20 01:03 +0800，工作会话跨过午夜；
而"顺序可由 git 核对"是错的，因为本文件与脚本、结果在**同一个提交**里，
git 恰恰核对不了。见下方"地位"节。）
**★ 地位（2026-08-20 第三方复核后改写）：事后（post-hoc）探索性敏感性分析。
不是可审计的预注册，不得用于把任何格子的证据等级上调。**

三位复核者一致指出同一件事，我接受：

> 本文件、`step130_multilist_control.R`、结果表 `130a/b/c` 与正文解释
> **首次出现在同一个提交（`821de44`）里**。
> 因此 git **无法证明**规则先于运行确定。
> 我确实是先写规则后跑脚本，但**这句话不可核验**，
> 而本项目的全部纪律就建立在"只主张可核验的东西"上。

**由此产生的三条约束**：

1. 本文件**不得**被称为 pre-registered，正文亦不得称本检验为 "stronger test"；
2. 本文件**不能**把 RA × CD4 从探索性/描述性重新上调（原 §7、§8.5 的上调条款**作废**，见 §9）；
3. 本文件的价值是**诊断性**的——它显示单名单对照的判定对名单选择敏感，
   这一点不依赖它是否预注册。

⚠ 与 S37 的区别在此说明白：S37 首次入库于 2026-08-18（`b003dab`），
**早于它所约束的四个格子被计算**，那条时间线可核验，所以 S37 仍是冻结的预注册。
**S39 没有这个性质。**

**编号**：S39（补充材料：多名单阴性对照）

---

## 0. 登记：查了什么、没查什么

**已查看**：仓库里现存的已知位点名单文件名、行数与列格式，即

| 名单 | GRCh38 行数 | 来源 |
|---|---:|---|
| colorectal | **642** | `step94b`（GWAS Catalog，p < 5×10⁻⁸，每 rsID 一条）|
| melanoma (Landi 2020) | **157** | 论文表 |
| HCC | **73** | `step84`（论文表 + Catalog，已去循环）|
| prostate | **35** | `step94b` |
| breast | 6 | `step94b` |
| lung | **0** | `step94b`（GRCh38 定位全部失败）|

**未查看**：**任何名单对任何格子的打分结果**——倍数、P、背景已知率，一个都没算过。
本文件写完之前不运行 `step130`。

---

## 1. 为什么需要这份文件

S36 §3 的结论是：**单一错配名单对 RA 不是正交的阴性对照**。
RA × CD4 的错配对照在七个窗口设置里六个失效，且失效强度随窗口收紧单调上升，
原因是 RA 与黑色素瘤**真的共享免疫区域**。

这意味着现有设计里的阴性对照存在一个结构性缺陷：

> **"错误疾病"是我们选的，而且只选了一个。**
> 若恰好选中一个与结局共享结构的疾病，对照会失败；
> 若恰好选中一个完全无关的疾病，对照会通过。
> **单个名单的通过或失败，无法区分"归属特异"与"名单选得巧"。**

多名单对照把这个自由度收掉：**同一格用多个事先选定的不相关名单各打一次分**，
报告**有多少个名单也富集**，而不是报告我们挑的那一个的结论。

---

## 2. 命题

> **命题 M**：某格的 FDR<0.05 提名落在**结局自身**已知位点上的富集，
> 不能由"该格提名落在任何一份足够大的疾病位点名单上"解释。

⚠ 注意命题 M 比原来的错配对照**弱**：它不要求任何一个不相关名单都不富集，
只要求结局自身名单的富集强于不相关名单。
**放宽判据 + 事后设计 = 本检验只能作诊断，不能作确证。**
原稿把"更诚实"写成了它的优点，复核者指出这是把**放宽**说成了**改进**，已删。

---

## 3. 名单选择规则（跑前定死）

**纳入**：仓库中已有的、**GRCh38 可定位 lead SNP ≥ 30 条**的疾病名单，
且**不是**该格结局自身的名单。

- **≥ 30 条**这个门槛沿用 S34 的"30 个可评价位点"下限，不是为本次新设的；
- **不新取数据**。只用已在树里的名单，避免"跑完发现不显著再去多取几个名单"；
- 因此 **breast（6 条）与 lung（0 条）被排除**，排除理由是**名单大小**，
  与任何打分结果无关（§0 已登记：分数尚未计算）。

**由此，对照名单集合固定为**：

| 格子的结局 | 不相关名单（对照集） |
|---|---|
| melanoma | HCC · colorectal · prostate · **RA** |
| HCC | melanoma · colorectal · prostate · **RA** |
| RA | melanoma · HCC · colorectal · prostate |

> **⚠ 本表的一处内部不一致，2026-08-20 记录并更正。**
> 本节开头的**规则**是"树里所有 ≥30 条、且不是该格自身结局的名单"。
> RA 名单（83 条）满足该规则，因此对 melanoma 与 HCC 两类格子**也是对照名单**。
> 但我最初把这张表写成了每行三份，**漏掉了 RA**——
> 表是规则的枚举，枚举漏了一项，规则本身没错。
> `step130` 执行的是**规则**，所以癌症格实际各有 4 份对照，与更新后的表一致。
> 此处如实记录：**改的是表，不是判据，也不是结果**；
> 若反过来按错的表只用三份对照，`melanoma × eQTLGen` 会由 B 变成 A
> （它唯一命中的那份对照正是 RA），**即那个错误会让结果更好看**，
> 更没有理由保留它。

⚠ 四份对照名单**全部是癌症**。这是现有数据的限制，**必须写进正文**：
对 RA 而言这是四份真正不相关的名单；
对 melanoma / HCC 而言，"另一种癌症"未必与"随机疾病"等价。
**本检验因此对 RA 的信息量最大，对癌症格较弱。**

---

## 4. 计算口径（跑前定死，全部继承已冻结的决定）

- 分区：**非递归固定锚定，最大跨度 1000 kb**（S37）
- 归属口径：**C1 `any_record`**（S37）
- 已知窗口：**1000 kb**
- 检验：单侧 Fisher，对该格全部工具位点构成的背景
- **每一份名单都在同一分区上重算背景**，不得跨名单引用背景数

**主设置** = 上述（即预注册主分析）。
**次要（同样跑前定死）**：`known_kb` 与 `locus_kb` 各扫 100/250/500/1000 kb，
按 S36 建立的报告标准，整张扫描表随文给出。
⚠ 扫描是**次要**的；判读按主设置执行，扫描表只用于展示稳健性，
**不得**在看过扫描后改判主设置的结论。

---

## 5. 主检验与判读表（在看结果前写死）

对每一格，定义：

- **F_own** = 该格对**结局自身**名单的富集倍数
- **F_max** = 该格对**对照集中任意一份**名单的最大富集倍数（只计 P<0.05 且倍数>1 者；若无，记 0）
- **k** = 对照集中达到 P<0.05 且倍数>1 的名单数

| 情形 | 判读 | 正文怎么写 |
|---|---|---|
| **A** k = 0 | **对照干净**。归属特异于结局自身遗传 | 可写"多名单阴性对照全部为阴性" |
| **B** k ≥ 1 且 **F_own > F_max** | **对照不正交，但方向仍分离** | 必须同时报 k 与 F_own/F_max；归属可写为"强于任一不相关名单"，**不得**写成"特异" |
| **C** k ≥ 1 且 **F_own ≤ F_max** | **归属主张不成立** | 该格的归属**必须降级为描述性**，并在摘要说明 |
| **D** 某格显著位点 < 2 | **无功效**，不判读 | 如实报"做不成" |

**A、B、C、D 都可能，事先接受。**

⚠ 出现 C 时**不得**：换名单、抬高名单大小门槛、只报主设置里表现好的那些格、
或改用 §4 的次要扫描里对自己有利的那一档。

### 5b. 为什么判据是 F_own vs F_max，而不是"任何对照都不许显著"

因为**位点密度是真实存在的**：colorectal 有 642 个 lead SNP，
在 1 Mb 窗口下会覆盖基因组相当大的一部分，
**任何**一组显著位点都可能落在其中若干个上。
要求"任何不相关名单都不得显著"会把一个密度事实误判为归属失败。

判据因此定在**相对强度**上。这比原来的单名单二分判据弱，
**这一点必须在正文里写明，而不是悄悄换掉判据**。

---

## 6. 这次回答不了的

- 四份对照名单**全是癌症**，不构成"对所有疾病的随机抽样"；
- 名单大小差异巨大（35 至 642），**倍数跨名单不可比**，只在格内比较；
- Catalog 名单的完整度随疾病与年份变化，与 S33 §7 记录的偏保守方向同理；
- 不改变任何格子的**结局自身**归属数字——本检验只增加对照，不重算主结果。

## 7. 与 S36 的关系

S36 证明了**单名单对照对 RA 不正交**。本文件不重复那个结论，
而是回答它引出的下一个问题：**换成多个名单之后，RA 那一格还剩什么。**

若本检验落在 A 或 B，RA 的非癌症泛化**可以**从"描述性"上调；
若落在 C，则维持描述性，且 S36 的降级判断得到独立确认。
**两种结果都有信息量，事先接受。**

> **⚠ 本节的上调条款已于 2026-08-20 作废**，见 §"地位"与 §8.5。
> 原文保留不改，因为它记录了当时的想法；**但它不再是有效的判读规则**。
> 作废理由与结果无关，而与本文件的**地位**有关：
> 一个无法证明"规则先于结果"的检验，不能承担升级功能。

---

## 8. 结果登记区（跑完填入；设计与判据不得改动）

脚本 `step130_multilist_control.R` → `130a` / `130b` / `130c`。
名单纳入规则按 §3 执行：colorectal(642)、melanoma(157)、RA(83)、HCC(73)、prostate(35) 纳入；
**breast(6) 与 lung(0) 因低于 30 条门槛排除**（排除在打分之前执行）。
每格 **4 份**不相关对照名单——含 RA 名单用于癌症格，见 §3 那条更正记录。

### 8.1 主设置（固定锚定 1000 kb，C1）

| 格子 | 对照名单数 | k（也富集的） | F_own | F_max | F_own/F_max | 判读 |
|---|---:|---:|---:|---:|---:|:--:|
| melanoma x Soskic_CD4 | 4 | **0** | 4.96 | — | **∞** | A |
| melanoma x eQTLGen_blood | 4 | **1** | 6.31 | 2.11 | **2.99** | B |
| HCC_high x Soskic_CD4 | 4 | **0** | 7.88 | — | **∞** | A |
| HCC_high x eQTLGen_blood | 4 | **0** | 10.87 | — | **∞** | A |
| RA x Soskic_CD4 | 4 | **2** | 3.91 | 3.05 | **1.28** | B |
| RA x eQTLGen_blood | 4 | **3** | 3.94 | 2.98 | **1.32** | B |
| HCC_low x Soskic_CD4 | 4 | **0** | 15.49 | — | **∞** | A |
| HCC_low x eQTLGen_blood | 4 | **1** | 13.53 | 3.19 | **4.24** | B |

**★ 没有任何一格落入 C。** 每一格的结局自身名单富集都**强于任何一份不相关名单**。

### 8.1b ⚠ 裕度改用"对全部对照"的比值（2026-08-20，复核者指出）

原稿的 `F_max` 只取**达到 P<0.05 的**对照，于是四个癌症格 `F_max = 0`，
比值被写成"无穷大 / 无对手"。**这在数学上不成立**：对照不显著不等于对照不存在，
例如 melanoma × CD4 的 prostate 点估计仍有 **2.73×**。

改用 **`F_max_all`（全部四份对照里的最大倍数，不论是否显著）**，比值全部有限：

| 格子 | F_own | F_max_all（谁）| **margin** |
|---|---:|---|---:|
| melanoma × CD4 | 4.96 | 2.73（prostate）| **1.82** |
| melanoma × eQTLGen | 6.31 | 2.24（prostate）| **2.82** |
| HCC-high × CD4 | 7.88 | 2.08（colorectal）| **3.79** |
| HCC-high × eQTLGen | 10.87 | 5.21（prostate）| **2.09** |
| HCC-low × CD4 | 15.49 | 2.03（colorectal）| **7.63** |
| HCC-low × eQTLGen | 13.53 | 3.19（colorectal）| **4.24** |
| **RA × CD4** | 3.91 | 3.05（HCC）| **1.28** |
| **RA × eQTLGen** | 3.94 | 2.98（HCC）| **1.32** |

**结论方向不变**：癌症六格 1.82–7.63，RA 两格 1.28 / 1.32，仍是全网格最窄。
但"无限裕度"的说法已删，`130c` 现在同时输出 `F_max_sig` 与 `F_max_all`。

配图：`figures/FigS_multilist_control.pdf`（`make_supp_fig_multilist.py`）——
每格一行，实心菱形为结局自身名单，空心圈为四份不相关名单，
实心蓝圈为"该不相关名单也富集"，横线跨度即裕度。

### 8.2 ★ 最重要的一条：单名单对照的结论取决于挑了哪一份名单

RA × CD4 在主设置下对四份不相关名单的表现：

| 名单 | 倍数 | P | 判定 |
|---|---:|---:|---|
| **melanoma** | 1.71 | 0.102 | 干净 |
| **HCC** | **3.05** | **0.00277** | **富集** |
| **colorectal** | 1.77 | 0.00447 | 富集 |
| prostate | 1.04 | 0.586 | 干净 |

⚠ **2026-08-20 更正一处事实错误。** 原稿写"melanoma 恰好是四份里对我们最有利的一份"，
**这是错的**：最有利的是 **prostate（1.04×，P = 0.586）**，melanoma 排第二。
复核者查表指出，我核对后确认。正确表述是：

> **melanoma 是四份里通过的两份之一；另外两份（HCC 与 colorectal）会让这一格作废。**
> **即该格的作废与否，取决于当初指派了哪一份名单。**

结论方向不变（判定对名单选择敏感），但**"选了最有利的那份"这个更重的说法不成立**，
已从本文件、正文、图注与交接文档中全部删除。

这正是 §1 写下的那个自由度被实测出来的样子：
**单个名单的通过或失败，测的是名单的选择，不只是归属的特异性。**
⚠ 这一条必须进正文，且不能只当作 RA 的局部问题——它是**单名单阴性对照设计本身**的缺陷。

### 8.3 §4 的次要扫描（八格 × 两种约定 × 四档，共 64 个设置）

**63 个设置为 A 或 B；唯一落入 C 的是 `RA × CD4` 在 `locus_kb = 100 kb`**
（F_own 3.70 < F_max 4.24）。

- 癌症六格：F_max 多数为 0.00，即不相关名单**一个都不命中**；
- **RA 两格的 F_own/F_max 比值全程在 1.25–1.32 之间**，是全网格最窄的裕度；
  癌症格要么无穷大（F_max = 0），要么 ≥ 2.4。

**这与 S36 的结论一致而非冲突**：S36 说 RA 的单名单对照随窗口收紧而失效；
本检验说即便换成四份名单，RA 的裕度也是全网格最薄的一格。

### 8.4 按 §5 判读表的处置

- 六个癌症格：**A 或 B**，其中 melanoma × CD4、HCC-high 两格、HCC-low × CD4
  在主设置下 **k = 0**，是全网格最干净的四格；
- **RA × CD4 = B**：k = 2/4，F_own 3.91 > F_max 3.05。
  按 §5 的 B 行，正文**可写"强于任一不相关名单"，不得写"特异"**，
  且**必须同时报出 k 与两个倍数**；
- **RA × eQTLGen = B**，但该格已按 S33 §5 因错配对照作废，本检验不改变作废判定。

### 8.5 ⚠ 原"按 §7 上调"条款作废（2026-08-20）

原稿据 §7 把 RA × CD4 从"纯描述性"部分上调为"强于四份不相关名单中的任何一份"。
**该上调撤回**，理由是 §"地位"节：本文件是事后设计且判据被放宽，
**一个事后放宽的检验不能给任何结论升级**。

现在的处置（与复核意见一致）：

> **RA × CD4 作为探索性 / 描述性结果保留**，完整数值与窗口扫描照常给出，
> 但**不进入正式证据链**，摘要不得让它与稳健癌症格处于同一层级。

本文件对 RA 的贡献只剩**诊断性**的一条：**该格的作废与否取决于指派了哪一份错配名单**
（§8.2）。这一条不需要预注册地位就成立。

### 8.6 这次仍然回答不了的

四份对照名单全是癌症（§6 已登记）。对 RA 而言它们确实不相关，
但**"四种癌症"不是"四种随机疾病"**。若日后取得非癌、非免疫的名单
（如身高、2 型糖尿病），本检验应当重跑——**而那需要新取数据，本轮明确不做**（§3）。


---

## 9. 复核后的处置汇总（2026-08-20）

| 复核意见 | 处置 |
|---|---|
| 本文件与脚本、结果同提交，无法证明先后 | **接受**。改称事后探索性敏感性分析，不再自称预注册 |
| 判据由"任一对照显著即废"放宽为"自身 > 最强对照" | **接受**为事实描述；不再把放宽说成"更诚实"或"更强的检验" |
| 不能据本文件上调 RA × CD4 | **接受**，§8.5 撤回上调 |
| "melanoma 是最有利名单"是事实错误 | **接受**，最有利者为 prostate，已改（§8.2）|
| "无限裕度"不成立 | **接受**，改用 `F_max_all`，全部有限（§8.1b）|

**仍然成立、且不依赖本文件地位的一条**：
四份名单里两份会让 RA × CD4 作废、两份不会——
**单名单阴性对照的判定对名单选择敏感**。这是本文件唯一进入正文的结论。
