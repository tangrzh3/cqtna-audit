# 预注册：FinnGen R13 转移测试 —— 换掉结局数据集后，这套审计还成立吗

**写于 2026-08-13，在读入任何 R13 关联结果之前。**
**地位**：与 S9、S18、S20、S21、S22、S24 同规格（第七份）。跑完只允许填 §10。
**编号**：S25

---

## 0. 这一份检验的是什么，以及**它不是什么**

### 0.1 它不是第六个功效点

`PREREG_power_trajectory.md`（S9）的第 0 步闸门写死了一条：
**只有 endpoint 定义逐字可比的 release 才能进入功效轨迹**。
R6 正是被这条挡掉的（143 例 vs R8 的 2,705 例，说明尚未接入癌症登记链接）。

R13 **不满足这条闸门**：

| | R12（全文所用）| R13 |
|---|---|---|
| endpoint | `C3_MELANOMA_SKIN_EXALLC` | `C3_MELANOMA_SKIN_WIDE` |
| 病例 / 对照 | 5,753 / 378,749 | 6,226 / 372,159 |
| 疾病编码 | C43 · 172 · C44 + melanoma morphology | **相同** |
| controls 排除 | C3_CANCER + C3_CANCER_WIDE_EXALLC | C3_CANCER + **C3_CANCER_WIDE** |

**病例定义相同，controls 排除规则不同。** 故 R13 **不得**作为 R8→R12 轨迹的第六点，
**不得**进入 `59a_release_trajectory.tsv` 的符号检验或与 `58a` 登记预测的区间比对。

⚠ 上表的 endpoint 定义**不是从 manifest 的描述行推断的**。
R13 manifest 把该 endpoint 的 phenotype 字段写作 "Malignant melanoma of skin, **including Hilmo**"
（Hilmo = 芬兰住院登记），照字面读会得出"R13 新纳入了住院登记病例"的结论。
**那是错的**：Risteys 的逐项编码核对显示 R12 的 `_EXALLC` 本来就含住院编码。
（Step 98 曾按 manifest 字面推断出"病例口径变了"，已更正；见纪律第 17 条。）

**不得改用裸 `C3_MELANOMA_SKIN`**：R13 把它列为更窄的相似 endpoint，
且已发布的 manifest 里根本没有该 endpoint。本地核实：`_EXALLC` 后缀在整个 R13 manifest
出现 **0 次**，即 R12 所用的 endpoint 在 R13 已不存在，没有"同名续跑"这条路。

### 0.2 它是什么

**转移测试**：一位读者今天拿到本文的方法与工具变量，用 FinnGen **当前**发布的
黑色素瘤 endpoint 重跑一遍，会不会得到同一个答案？

这是本文所有主张里最容易被读者当场证伪的一条，也是投稿后最可能被人真的去跑的一条。
所以**必须我们自己先跑，并且事先写死判读**。

⚠ **R13 与 R12 是嵌套的**（R13 的参与者包含 R12 的全部）。
本测试**不是独立复现**，也不得被写成独立复现。它回答的是
"换成当前数据集后结论是否改变"，不是"在另一群人身上是否成立"。

---

## 1. ★ 功效与定义两者同时变了 —— 事先写死的处理

这是本预注册最关键的一节。R13 相对 R12 有**两处**变化，公开数据**无法把它们分开**，
本测试也**不声称**能分开。但两者的**方向**可以事先判定：

| 变化 | 量 | 对"信号更强/更多"的方向 |
|---|---|---|
| 病例数 | 5,753 → 6,226（**+473，+8.2%**）| **↑**（无歧义）|
| 对照数 | 378,749 → 372,159（**−6,590，−1.7%**）| **≈0**。N_eff 由病例数主导：R13 的 `1/n_controls` 项只占 `1/n_cases + 1/n_controls` 的 **1.6%** |
| controls 排除规则 | `C3_CANCER_WIDE_EXALLC` → `C3_CANCER_WIDE` | **↑ 或 ≈0**。队列总量在增长而对照池反而缩小 → 排除规则**变宽**了 → 对照中残留的癌症患者更少 → 结局错分更少 → 效应估计的衰减更小 |

→ **三项要么把结果推向"更强/更多"，要么可忽略；没有一项把结果推向"更弱/更少"。**

**由此得到一条不对称的事先判读（本节的核心）**：

- **若转移成功**（结论不变或略增强）：**部分被混杂**。不得写成"功效增长导致"，
  也不得写成"定义变化导致"，只能写"在同资源的当前发布上结论不变"。
- **若转移失败**（结论方向反转或消失）：**两处变化都解释不了它**，因为两者都指向反方向。
  失败是**真信息**，必须原样写入正文与摘要。

⚠ **纪律第 9 条在此明确适用**：若结果与 R8–R12 轨迹的外推不符，
**不允许**归因于"endpoint 定义变了"作为开脱。上表已事先说明为什么这个开脱不成立。

---

## 2. 命题

> **命题 T**：本文的位点归属结论（FDR<0.05 的 MR 提名系统性落在该疾病自身的已知位点上）
> 与候选名单的构成，在把结局换成同一资源的**当前发布**后不改变。

---

## 3. 固定不变的东西（唯一允许变的是结局文件）

暴露数据（Soskic CD4⁺T 动态 eQTL；eQTLGen 全血）、工具变量选择（p<5e-8）、
harmonise 与等位匹配规则、Wald ratio、BH-FDR 的检验家族定义、
已知位点参照系（Landi 157 位点 / HCC 73 位点，1 Mb 窗口）、
位点合并规则（1 Mb 单连锁）、四分类标注 —— **全部与 `step59b` / `step94c` / `step94d` 逐位一致**。

**变异覆盖的处理（事先指定）**：主分析限定在 **R12 与 R13 共有**的工具变量上，
并报告该限制丢弃了多少条；次分析用各自的完整覆盖，两者并列报告。
（与 S9 §2.1 同一处理。）

---

## 4. 要跑的格子（跑前定死，不得增删）

| # | 暴露 | 结局 | 地位 |
|---|---|---|---|
| **C1** | Soskic CD4 | **R13 melanoma** | **主格** |
| C2 | eQTLGen 全血 | R13 melanoma | 次（第二个暴露资源上的同一转移测试）|
| C3 | Soskic CD4 | R13 HCC (`C3_HEPATOCELLU_CARC_WIDE`, 1,070/372,159) | 次 |
| C4 | eQTLGen 全血 | R13 HCC | 次 |
| **R12 对照行** | 同上四格 | R12 对应 endpoint | **重现校验，见 §6-PC1** |

R13 HCC 相对 R12 HCC（`C3_HEPATOCELLU_CARC_EXALLC`，947 / 378,749）
病例 +123（+13.0%），controls 排除规则的变化与黑色素瘤一致，§1 的方向判定同样适用。

---

## 5. ★ 登记的定量预测（**在读入任何 R13 结果之前写下，此后不得修改**）

预测的唯一来源是**已在手的 R8–R12 观测轨迹**（`59a`/`59b`）与 R13 的公开元数据
（病例/对照数）。**未查看任何 R13 关联统计量。**

已观测轨迹（主分析 = 共有变异子集）：

| release | 病例 | N_eff | FDR<0.05 记录 | 基因 | 独立位点 | 新位点基因 |
|---|---|---|---|---|---|---|
| R8 | 2,705 | 10,708 | 7 | 4 | 1 | **0** |
| R9 | 2,993 | 11,848 | 8 | 5 | 1 | **0** |
| R10 | 3,194 | 12,647 | 8 | 5 | 1 | **0** |
| R11 | 3,932 | 15,551 | 8 | 5 | 1 | **0** |
| R12 | 5,753 | 22,668 | 10 | 6 | 2 | **0** |
| **R13** | **6,226** | **≈24,494**（1.081× R12）| ⟨预测见下⟩ | | | |

**登记的预测（点值 + 区间）**：

| 项 | 点预测 | 登记区间 | 判为"符合"的条件 |
|---|---|---|---|
| **P1** FDR<0.05 记录数 | **11** | **[9, 14]** | 落在区间内 |
| **P2** FDR<0.05 基因数 | **6** | **[5, 8]** | 落在区间内 |
| **P3** R12 那 6 个基因的恢复数<br>（CDK10 · CHMP1A · CTU2 · PARP1 · SPATA33 · VPS9D1-AS1）| **6/6** | **≥5/6** | 达标 |
| **P4** FDR<0.05 中的**新位点**基因数 | **0** | **≤1** | 达标 |
| **P5** 位点归属倍数（C1）| **>1** | **fold > 1** | 达标 |
| **P6** 独立位点数 | **2** | **[2, 4]** | 落在区间内 |

**区间是怎么定的（写下来以防事后说成"当然会中"）**：
R13 的 N_eff 只比 R12 高 8.1%，而 R8→R12 全程（N_eff 2.12 倍跨度）记录数只从 7 走到 10。
线性外推给出 10.4；取整为 11，区间在两侧各留一档以容纳 §1 的定义差异与抽样噪声。
**下界 9 允许比 R12 少一条**——即使 §1 判定三项变化都指向"更多"，仍留了下行余地；
**上界 14 是 R12 的 1.4 倍**，若真的跳到 15 条以上，说明发生了轨迹之外的事，须当作偏离报告。

⚠ **P4 是本预注册最有信息量的一条。** R8–R12 五个嵌套功效上新位点基因恒为 0。
若 R13 出现 ≥2 个新位点基因达 FDR<0.05，那就是**在真实纵向数据上第一次观测到名单更替**
——这是 S9 §8.5 明写"没有观察到、不可以写"的那件事。
它会**加强**④a，但必须作为**与登记预测不符**如实登记，且必须先排除是 §1 的定义差异所致
（无法排除时，两个解释并列报告，不选一个）。

---

## 6. 过程阳性对照（不通过则该格作废，不解释结果）

| | 判据 | 不通过怎么办 |
|---|---|---|
| **PC1 重现校验** | 用本次新写的脚本跑 **R12** 那一轮，必须逐位重现 **10 条记录 / 6 基因 / 2 位点**，且基因名单与 `59b` 完全一致 | **脚本有 bug**。修好重跑，**不解释任何 R13 数字**。这不是结果 |
| **PC2 已知大效应位点** | MC1R 簇（VPS9D1-AS1 / CDK10 / SPATA33 / CHMP1A）必须在 R13 的 FDR<0.05 名单中出现 | 提取或等位匹配坏了，不是发现 |
| **PC3 覆盖可比性** | R13 中匹配到的工具变量数 ≥ R12 的 **95%** | 该格记为"覆盖不可比"，**不报倍数**，只报覆盖差异本身 |

## 7. 阴性对照

**NC**：用 **HCC 的已知位点名单**给 C1（Soskic × R13 melanoma）打分。
若该错配组合也给出显著富集，说明归属打分是位点密度的假象，
**本次全部格子作废，且必须回头检查 S24 的六格网格是否受同一假象影响**。
（与 S24 §5-NC 同一逻辑与同一判据。）

---

## 8. 判读表（跑之前写死）

| 结果 | 结论 | 正文怎么写 |
|---|---|---|
| **A** P1–P6 全部符合 + PC 全过 + NC 阴性 | **转移成功** | "该审计在 FinnGen 当前发布（R13）上重跑得到同一结论；R13 的病例定义与 R12 相同，controls 排除规则不同。"**仍不得**写成第六个功效点，不得写成独立复现 |
| **B** P5 达标（归属仍成立）但名单构成偏离（P3 <5/6 或 P4 ≥2）| **主线存活，名单稳定性被限定** | 归属主张不变；名单部分改写为"在同资源的下一次发布上即发生更替"。⚠ **不得**把偏离归因于定义差异（§1 已事先说明该开脱不成立），两个解释并列，明写无法分离 |
| **C** P5 不达标（fold ≤ 1）或富集方向反转 | **主张在转移测试上失败** | 必须进摘要与 Discussion。**不得**改用 `C3_MELANOMA_WIDE` 或任何其他 endpoint 再试一次以求得到 A（纪律：阴性之后不得另起炉灶找阳性）|
| **D** PC1 未通过 | 脚本 bug，**不是结果** | 不写 |
| **E** PC3 未通过 | 该格"覆盖不可比" | 如实写为何不可比，不报倍数 |
| **F** NC 阳性 | 归属打分作废 | 本次全废 + 回查 S24 |

**B 与 C 都可能，且事先接受。**
出现 C 时**不得**：删掉不利的格子、改窗口、改 FDR 阈值、改 endpoint、
或只报 eQTLGen 那一列而不报主格。

---

## 9. 这次回答不了的

- **不能**把功效增长与 controls 定义变化分开（§1，公开数据做不到）
- **不能**当作独立复现（R13 ⊃ R12，嵌套）
- **不能**外推到非欧洲人群或非 FinnGen 资源
- **不涉及** coloc / SMR / 精细定位；TPI1 的任何边界不因本测试改变
- **不回答**新位点候选里有几个是真的

---

## 10. 结果登记区（跑完填入，设计与判据不得改动）

⟨待填⟩

脚本 `step99_r13_transfer.py` → 输出 `99a_r13_trajectory.tsv`、`99b_r13_lists.tsv`、
`99c_r13_attribution.tsv`。

### 10.1 过程对照

| | 判据 | 结果 |
|---|---|---|
| PC1 重现校验 | R12 = 10 条 / 6 基因 / 2 位点 | ⟨待填⟩ |
| PC2 MC1R 簇 | 须在 R13 FDR<0.05 中 | ⟨待填⟩ |
| PC3 覆盖 | ≥95% | ⟨待填⟩ |
| NC 错配名单 | 须为阴性 | ⟨待填⟩ |

### 10.2 观测 vs 登记预测

| 项 | 登记预测 | 观测 | 符合？ |
|---|---|---|---|
| P1 记录数 | 11 [9,14] | ⟨待填⟩ | |
| P2 基因数 | 6 [5,8] | ⟨待填⟩ | |
| P3 恢复 6 基因 | ≥5/6 | ⟨待填⟩ | |
| P4 新位点基因 | 0（≤1）| ⟨待填⟩ | |
| P5 归属倍数 | >1 | ⟨待填⟩ | |
| P6 独立位点 | 2 [2,4] | ⟨待填⟩ | |

### 10.3 判读表落在哪一格

⟨待填⟩

---

## 11. 修改记录

| 日期 | 改动 | 是否已看过任何 R13 关联结果 | 理由 |
|---|---|---|---|
| 2026-08-13 | 定稿 | **否**。仅查看了 R13 manifest 的 endpoint 名称与病例/对照数（元数据）、以及已在手的 R8–R12 结果 | — |
