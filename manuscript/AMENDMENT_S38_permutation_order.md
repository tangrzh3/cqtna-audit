# S38 修订记录 1：修复密度匹配置换的顺序依赖

**编号**：S38-A1（对 `PREREG_permutation_estimand.md` 的修订，不修改原文件 §0–§7）
**写于 2026-08-20，在跑修复后的置换之前。**

> ⚠ **本文件与包的修复、单元测试在同一个提交里；结果表在下一个提交。**
> 上一轮复核指出 S39"文件、脚本、结果同提交，无法证明先后"。
> 这次把顺序做成**可核验的**：先提交"规则 + 实现 + 测试"，
> 再提交"跑出来的数"。`git log --diff-filter=A` 可查。

---

## 1. 缺陷

第三方复核（Reviewer 1）指出密度匹配置换存在**顺序依赖**：仅反转输入行序，
经验 P 就会改变。我已独立复现（`cqtna` 0.3.0，R 4.4.1，seed 1，10,000 次置换，
melanoma × Soskic_CD4，容差 1.00）：

| 输入行序 | 倍数 | 经验 P |
|---|---:|---:|
| 原序 | 4.951 | 0.0042 |
| 完全反序 | 4.805 | 0.0044 |
| 随机打乱 | 4.856 | 0.0061 |

**这三行应当完全相同——行序不是数据。**

### 1.1 机制（两个，都要修）

```r
sig  <- unique(as.character(mr$locus[mr$fdr < fdr]))   # 首次出现顺序
...
for (L in sig) {
  avail <- setdiff(pool[[L]], drawn)                   # 不放回
  drawn <- c(drawn, avail[sample.int(length(avail), 1L)])
}
```

**(a) RNG 流对齐**。`sig` 的顺序决定了 `sample.int` 的调用次序，
而各位点候选池大小不同，于是**同一条随机数流被切成不同的片段**。
这一项纯粹是假象，**没有任何统计含义**。本例中 0 次抽样耗尽池，
所以观察到的差异几乎全部来自这一项。

**(b) 贪心优先权**。不放回抽样下，**排在前面的位点从更大的池中抽**，
后面的位点池被前面抽走的元素缩小。这是一个真实但小的系统性偏差，
且"谁排在前面"由输入行序这个与数据无关的东西决定。

⚠ 另有一个潜在的第三项：候选池成员顺序来自 `tapply` 的分组排序，
**字符串排序受 locale 影响**。本机 `Chinese (Simplified)_China.utf8` 下未观察到差异，
但跨机器复现时它会变成第三个自由度。一并修掉。

---

## 2. 修复（在看到修复后的任何数字之前写死）

| # | 改动 | 针对 |
|---|---|---|
| **2.1** | `sig` 与候选池成员一律按**规范顺序**排序（`chr` 数值、`start`、`end`），不再依赖输入行序或 locale | (a)(c) |
| **2.2** | **每一次置换内部，随机化位点的处理次序**。仍为不放回、池耗尽即作废该次抽样（S38 §1.6 不变） | (b) |
| **2.3** | 结果表保存 `n_draws_used`、`n_draws_exhausted`、各池大小（最小/中位/最大）、`min_pool_size`、`effective_draw_fraction` | R2.3 |
| **2.4** | 新增**最低有效抽样比例** `min_draw_fraction = 0.95`：有效抽样少于 95% 即返回 NA 与原因，不出 P | R2.3 |
| **2.5** | 记录 `RNGkind()`、`R.version.string`、`Sys.getlocale("LC_COLLATE")` 到 spec | R2 环境冻结 |

**2.2 为什么是随机化而不是固定某一个次序**：固定次序（哪怕是规范排序）仍然让
某个位点系统性地享有最大池。把次序放进随机化里，贪心偏差在 10,000 次抽样上被平均掉，
**且结果不再依赖任何与数据无关的排列**。

⚠ **这是对实现缺陷的修复，不是对估计目标的改动。**
S38 §1.5（池排除显著位点）、§1.6（不放回、耗尽作废）、§1.7（100% 匹配覆盖率）
**一字未改**。2.4 新增的是一道**更严**的门槛，不是放宽。

---

## 3. 判读表（在看修复后的结果之前写死）

设修复后 melanoma × CD4 在容差 1.00 下的经验 P 为 `P*`。

| 情形 | 处置 |
|---|---|
| **A** 八格的判读（S38 §7.2）与修复前**一致** | 更新数字，正文结论不变，并在 S38 §8 记录修复前后对照 |
| **B** 某格由显著变不显著，或反之 | **以修复后为准**。正文相应格子的密度置换结论改写；若 melanoma 两格中任一格变不显著，**密度置换从推断链撤下**，仅作探索性诊断 |
| **C** 修复后仍存在行序依赖（单元测试不过） | **密度置换整体撤下**，正文删除全部经验 P，只保留"该检验未能给出与实现无关的结果"这一句 |
| **D** 有效抽样比例 < 95% 的格子 | 该格返回 NA，如实报"做不成"，不得降低门槛 |

**A、B、C、D 都可能，事先接受。**

⚠ 出现 B 或 C 时**不得**：调 seed、调置换次数、改容差、只报修复前的数、
或声称"修复前的数更接近真值"。

---

## 4. 验收条件

单元测试必须锁死以下三条，否则修复不算完成：

1. **行序不变性**：同一份数据在原序、反序、随机打乱三种输入下，
   `empirical_p` 与 `fold_vs_null` **完全相同**；
2. **重叠池**：构造一组候选池高度重叠的合成数据，验证
   `n_draws_exhausted` 被正确计数，且低于 `min_draw_fraction` 时返回 NA；
3. **诊断字段**：`n_draws_used`、`n_draws_exhausted`、`min_pool_size`、
   `effective_draw_fraction` 全部随结果返回且非 NULL。

---

## 5. 与正文的关系

在本修订完成并重跑之前，**正文中的全部密度置换经验 P 视为未经验证**。
若落入 §3 的 C，正文相应段落按 C 改写。

⚠ 另单独记一条与本修订无关、但复核者一并指出的逻辑错误（已改）：
**密度置换不能解除 RA 对错配名单对照的依赖**——两者检验的是不同问题，
前者问"是否由位点密度造成"，后者问"是否特异于该结局自身遗传"。
正文原有暗示此意的句子已删。

---

## 6. 结果登记区（跑完填入；§1–§5 不得改动）

### 6.1 修复前后对照（容差 1.00，seed 1，10,000 次置换）

| 格子 | 修复前倍数 | 修复前 P | **修复后倍数** | **修复后 P** | 判读 |
|---|---:|---:|---:|---:|:--:|
| melanoma × CD4 | 4.95 | 0.0042 | **4.87** | **0.0050** | 不变 |
| melanoma × eQTLGen | 6.16 | ≤1e-4 | **6.14** | **≤1e-4** | 不变 |
| HCC-high × CD4 | 9.38 | 0.1046 | **9.18** | **0.1071** | 不变（仍不显著）|
| HCC-high × eQTLGen | 8.47 | 0.0041 | **8.24** | **0.0045** | 不变 |
| HCC-low × CD4 | 12.95 | 0.0049 | **12.85** | **0.0065** | 不变 |
| HCC-low × eQTLGen | 12.46 | 0.0010 | **12.09** | **0.0012** | 不变 |
| RA × CD4 | 4.50 | ≤1e-4 | **4.52** | **≤1e-4** | 不变 |
| RA × eQTLGen（已作废）| 4.11 | ≤1e-4 | **4.14** | **≤1e-4** | 不变 |

### 6.2 按 §3 判读表：落在 **A**

**八格无一改变显著性判定。**

⚠ **2026-08-20 更正一处我自己的算术错误（复核者指出，已重算确认）。**
原稿写"最大移动是 melanoma × CD4 的 4.95 → 4.87，即 0.08 倍"。
**0.08 是头号格子的移动，不是最大移动。** 逐格绝对移动：

| 格子 | 修复前 | 修复后 | 移动 |
|---|---:|---:|---:|
| **HCC-low × eQTLGen** | 12.46 | 12.09 | **−0.37** |
| HCC-high × eQTLGen | 8.47 | 8.24 | −0.22 |
| HCC-high × CD4 | 9.38 | 9.18 | −0.20 |
| HCC-low × CD4 | 12.95 | 12.85 | −0.10 |
| melanoma × CD4 | 4.95 | 4.87 | −0.08 |
| RA × eQTLGen | 4.11 | 4.14 | +0.03 |
| melanoma × eQTLGen | 6.16 | 6.14 | −0.02 |
| RA × CD4 | 4.50 | 4.52 | +0.01 |

**最大移动 0.37 倍**（HCC-low × eQTLGen），**八格中六格下移、两格上移**。
原稿写的"有升有降"字面成立但暗示了对称，实际以下移为主。

**移动不改变任何显著性判定**——这是本节的结论，不受该更正影响；
但"最大 0.08"这个数是错的，且它比真值小四倍多，
**方向恰好是让修复显得更无害**，所以必须更正。

**处置**：更新正文与 S38 §7 的数字，结论不变。
⚠ 特别记下：**HCC-high × CD4 仍然不显著（0.1071）**。
若它翻转过来，按 §3-B 我本该改写该格的结论；它没有。

### 6.2b 方法应当怎么称呼（复核者 R1 指出）

当前实现是**规范排序后、每次置换随机化目标顺序的贪心无放回匹配**
（randomized-greedy matching）。

⚠ 它**不是**"从所有可行的完整匹配中均匀抽样"。
作为一个事先定义、可复现、且不依赖任何与数据无关排列的随机化零分布，它可用；
但正文与方法段**必须按其真实名称称呼它**，不得暗示它是均匀完整匹配抽样。已改。

### 6.3 行序不变性已验证

修复后同一份数据在原序 / 反序 / 随机打乱三种输入下：
**倍数 4.872、P = 0.00500，三者完全相同**（修复前为 4.951/0.0042、4.805/0.0044、4.856/0.0061）。
单元测试 `test-permutation-order.R` 锁死这一条，共 25 项断言。

### 6.4 匹配诊断（§2.3）

`126c`（可引用行）保存 `n_draws_used`、`n_draws_exhausted`、
`effective_draw_fraction`、`min/median/max_pool_size`，以及
`RNGkind`、`R.version.string`、`LC_COLLATE`。

`126b`（容差扫描）同样保存这一整套，逐容差一行。

⚠ **2026-08-20 更正记录**：本段原写"`126b`/`126c` 现在保存……"，
而复核者查出**当时 `126b` 缺 `max_pool_size` 与三个环境字段**——**声明说过头了**。
已改代码使两张表都写全并重跑，主结果一个数未变。

⚠⚠ **然后我又把这句话说过头了一次。** 上一版写"40 行全部带 `max_pool_size`"，
复核者查出实际是：**列存在于全部 40 行，但只有 34 个可引用行有值；
其余 6 行（覆盖率不足、按 §1.7 返回 NA 的那六个）为 NA**。

具体是容差 0.10 上的 melanoma 两格、HCC-low × CD4、RA 两格，
以及容差 0.25 上的 RA × CD4。**这正是应有的行为**——
这些格根本没有产生可引用的零分布，写上池大小反而会误导。

**同一个地方连续说过头两次。** 记在这里，
因为"我说保存了"、"确实保存了"和"保存在哪些行上"是三件事。

**八格在容差 1.00 下全部 10,000/10,000 次抽样完成，无一次池耗尽**，
故 §2.4 新增的 95% 门槛在主设置下未触发——但它现在存在，
且在合成的重叠池数据上按预期返回 NA（单元测试第 4 条）。

### 6.5 我要说清楚的一点

修复让 melanoma × CD4 的 P 从 0.0042 变成 0.0050，**更接近 0.05 而不是更远**。
这不是我挑的方向，也没有第二次机会去挑——
规则与实现在结果之前提交（`1bfb870`），结果在其后的提交里。
**这正是上一轮 S39 没能做到、而复核者有权不信的那件事。**
