# 预注册：把容器定为准绳——重跑规则、迁移规则与判定翻转的处理

**编号**：S54
**写于 2026-09-07**，**在容器被构建、在任何一个数字被重算之前**。
**地位**：与 S9/S18/S20/S21/S22/S24/S25/S28/S29/S30/S32/S33/S34/S35/S52/S53 同规格（**第十七份**）。
跑完只允许填 §9。§1–§8 一字不改。

---

## 0. 这一份是干什么的

`CONTAINER.md` §5 把选择摆成互斥的两条：

- **(a) 容器是文档**——论文数字保持 Windows 产出，容器供读者复跑；
- **(b) 容器是准绳**——在容器里重跑，**用容器的数字作为论文的数字**。

**作者已选 (b)（2026-09-07）。**

(b) 更相称于一篇以复现审计为核心的论文，但它有真实代价：
**任何一个数动了，论文就得改，而已经读过前一版的评审会看到一份不同的稿子。**
本文件在构建镜像之前把这件事的规则写死，**代价是事先接受的**。

⚠ 本项目纪律 §五-56：§7 是事前预期，写在看任何重算结果之前。

## 1. 为什么必须事前写

论文当前的数产自 **Windows 11 / x86_64-w64-mingw32 / R 参考 BLAS /
`Chinese (Simplified)_China.utf8` 且 `LC_NUMERIC=C`**（`150b_session.txt`）。
容器是 **Linux / OpenBLAS / `C.UTF-8`**。

已知会造成差异的来源，事前列明，**不是事后用来解释某个不合意的数**：

1. **浮点**：BLAS、编译器、libm 实现不同，末位可变；
2. **排序**：locale 不同 → 按字符键排序再取第一行的步骤可能取到别的行；
3. **包解析**：`150a_environment.lock` 锁版本，但 Bioconductor/Posit 快照的
   依赖解析仍可能不同；
4. **Python 补丁版本**：记录为 3.12.4，容器给的是发行版的 3.12.x；
5. **外部二进制**：SMR / PLINK 的 Linux 构建与 Windows 构建。

⚠ **随机性不在此列**：R 与 numpy 的种子化 RNG 跨平台确定。
若种子化结果仍变，那是 **1–3 之一在起作用**，不得记为"随机波动"。

## 2. 接受判据（两层，跑前定死）

### 层一：正文引用的每一个数

判据 = **在正文印出的精度上四舍五入后相同**。
正文印 `4.96`，容器就必须在两位小数上给出 `4.96`。

**执行方式**：容器内重跑分析步骤 → 重跑
`step127_audit_manuscript_numbers.py`。它逐个把正文的数与表对账，
**任何一个 `MISSING` 就是一个动了的数**。同理跑 `step153` / `step154` / `step155`。

⚠ 这正是 `step127` 存在的理由，本文件不另造判据。

### 层二：每一个**判定**（categorical）

以下必须逐字相同，**层二的翻转比层一的位移严重得多**：

- 每个格子的显著/不显著、阴性对照通过/失效、cell void 与否；
- `147a_finemap_decomposition.tsv` 的 `seed_stable`（T/F）与 `verdict`；
- `101b_matched.tsv` 的 `criterion_met`；
- `130c_multilist_verdict.tsv` 的判定列；
- `cqtna` 包 `testthat` 的 271 项断言全过；
- `step101` 自带的 PC（"重现 85.8% / 22.8%"）必须仍然通过。

## 3. 必跑清单（按"最便宜且最有信息"排序）

| 顺序 | 内容 | 为什么 |
|---:|---|---|
| 1 | `step127` / `step153` / `step154` / `step155` | 四个审计一次性覆盖正文全部对账关系 |
| 2 | `Rscript -e 'testthat::test_local("cqtna_r")'` | 271 项断言 |
| 3 | `step140` / `step141` | **中心主张**：恒等式与分解 |
| 4 | `step101` | 自带 bitwise PC，最灵敏的浮点探针 |
| 5 | `step147` | **最可能动的一个**（susie 纯度抽样，S45 已记录） |
| 6 | `step151` / `step156 score` / `step158` | 纯文本与列联，应当完全不动 |
| 7 | 其余输入齐备的 `step*` | 有多少跑多少 |

## 4. ⚠ 全有或全无条款（本文件最要害的一条）

**不得**在容器与 Windows 之间挑选：
一致处采用容器值、不一致处保留 Windows 值——**这是选择性报告**。

只有两种合法终局：

- **容器成为准绳**：所有**可重跑**的数一律采用容器值，
  连同任何翻转了的判定一起采用并报告；
- **容器不作准绳**：全部退回 (a)，`CONTAINER.md` §5 改回 (a) 的措辞，
  并在正文写明退回的理由。

## 5. 数字动了怎么办（迁移规则）

1. 在 `manuscript/NUMBER_MIGRATION_fixed_anchor.md` 里登记：旧值、新值、来源表、步骤；
2. 正文、补充材料、**图注**、`step127` 期望值一并改；
   ⚠ 图注里的数字 `step154` **查不到**，必须手工核（`step127` §1a–1d 才查数）；
3. 重跑四个审计直到全绿；
4. 在 §9 逐条列出**每一个**动过的数——**不得只报"多数一致"**。

## 6. 判定翻转怎么办

**按"翻了"报告，不得静默采用。**

1. 在正文新增或扩写一句，说明该判定在两个环境下不同；
2. 在 §9 记录两个环境各自的值与差异来源（若可判定）；
3. ⚠ **若翻转发生在 `seed_stable` 上**：那不是构建问题，
   而是**关于方法的结果**——S45 的结论应据此加强，
   即该量既依赖种子也依赖环境。

## 7. 事前预期（写在跑之前）

- **绝大多数数不会在正文印出的精度上变**。倍数、计数、两位有效数字的 P 值应当稳定。
- **最可能动的是 `step147`**（susie 纯度按 100 个变异随机抽样估计，
  S45 已记录无种子时 0/1/0 的行为）。**我预期 `seed_stable` 至少有一格翻转。**
- **计数类（位点数、记录数、基因数）应当完全相同**；若不同，
  优先怀疑 §1 第 2 条（locale 排序），而不是浮点。
- **`step101` 的 PC 我预期仍然通过**；若不过，说明浮点差异已大到影响下采样重现，
  那本身是重要发现。
- **`step151` / `step156` / `step158` 应当完全不动**（纯文本、纯列联）。

⚠ **若结果与上述相反**——例如 `step147` 稳定而计数类反而变了——
必须在 §9 写明"§7 未预见"，**不得**事后编一个机制。

## 8. 停止规则与作废条件

- **镜像构建失败或依赖解析与 `150a` 不符** → 记录差异，**先修镜像**，不得带伤重跑；
- **必跑清单第 1–2 项就失败**（审计红或包测试挂） → 判为环境未就绪，
  **不得**继续跑 3–7 并解释单个数字；
- **可重跑覆盖率低于正文所引数字的一半** → 容器不足以作准绳，**退回 (a)**，
  按 §4 的第二种终局处理；
- 重跑只做一次。不得在看到结果后更换镜像基底、包快照或容差。

## 9. 结果登记区（跑完才填；§1–§8 不得改动）

**执行**：2026-09-07/08，`step159_container_acceptance.py`，
镜像 `cqtna-acceptance:0.3.0`（`container/Dockerfile.acceptance`，见 §9.1）。
输出 `159a_container_acceptance.tsv`（逐步骤日志）、`159b_console.log`（完整控制台）、
`159d_baseline/`（阶段 0 快照）、`159e_table_diff.tsv`（逐格 diff）。

⚠ **执行过程中发现并修正了两处 `step159` 自身的缺陷，均在容器镜像存在之前 /
未观察到任何计算结果之前不适用——这两处是**运行本身**的缺陷，
按 §10.1 的同一原则处理：如实登记，不追溯掩盖：

1. **执行顺序缺陷**（§10.1 已记录）：阶段 1 只是挂载完好性检查，
   真正的验收判据在阶段 3。
2. **`step158` 因缺外部数据失败时，覆盖了 `158c_console.log` 里此前一次
   成功运行的完整记录**（33 行分析输出被替换成 1 行失败信息）。
   git 历史中内容未丢失，已用 `git checkout` 恢复；`step159` 已修：
   现在任何失败步骤触碰过的已跟踪文件都会被自动撤销。

### 9.1 镜像

`container/Dockerfile.acceptance`——**精简验收镜像**，非全量镜像
（`container/Dockerfile`，S54 §3 全部 226 个包的那个）。
基于 `rocker/r-ver:4.4.1`（Ubuntu 22.04 jammy，已用 `cat /etc/os-release` 核实）。

构建中发现并修正的三处（均记入镜像自身的注释与本项目 git 历史，非本文件重述）：

- jammy 官方源顶多到 Python 3.10，而 `requirements.txt` 锁的 `scipy==1.18.0`
  需要 ≥3.12——通过 deadsnakes PPA 装 Python 3.12；
- `add-apt-repository` 自带的 keyserver 拉取在本机网络下失败（`gpg` 退出码 2）——
  改用 HTTPS 直接从 `keyserver.ubuntu.com` 取同一把公钥；
- `rocker/r-ver:4.4.1` 内置的 Posit 包管理器仓库是**冻结在 2024-10-30 的快照**，
  而 `irlba 2.3.7`（`150a` 锁定的版本，经核实是 CRAN **当前**版本）发布在那之后，
  导致 `remotes::install_version` 报"版本无效"——**`150a` 的版本号本身没有问题**，
  是基础镜像内置仓库比它旧；改用滚动的 `.../latest` 仓库解决。

镜像最终验证：`cqtna 0.3.0`、`data.table 1.16.0`、`susieR 0.14.2`、
`testthat 3.3.2`，与 `150a_environment.lock` 一致（`testthat` 不在 `150a` 里，
是测试期依赖，参 S54 §3 备注）。

### 9.2 层一：动了的数（逐条）

**在正文或任何补充材料印出的精度上，无一个数变动。** 阶段 3 的四个审计
（`step127`/`153`/`154`/`155`）全部通过，即它们对着**容器产生的表**
重新核对正文引用的每一个数字，结果与核对 Windows 产出的表时相同。

阶段 4 逐格比对发现 **3 张表**存在真实的浮点级差异（另有 6 张表在字节层面
不同、但内容零差异，是换行符规范化的副产品，见 9.2 末尾说明）：

| 表 | 格 | 基线 | 容器 | 相对差异 |
|---|---|---|---|---:|
| `141a_enrichment_decomposition.tsv` | row2/3 col7 (`fisher_p`) | 0.413019883209858 | 0.413019883209857 | ~2×10⁻¹⁵ |
| `141b_reviewer_conditioning.tsv` | row1 col7 (`fisher_p`) | 0.413019883209858 | 0.413019883209857 | ~2×10⁻¹⁵ |
| `147a_finemap_decomposition.tsv` | 14 行 col16 (`estimate_s`) | 见 `159e_table_diff.tsv` | 同上 | 1×10⁻¹⁰–1×10⁻⁸ |

**逐一核对了这两个量在全文任何地方被印出的每一处**（不是靠 `step127`
通过来推断）：

- `fisher_p`：正文三处印为 `P = 0.41`（行 24、141、689），
  S42 印为 `0.413`（三位小数）——**两个版本在三位小数上都四舍五入为 0.413**；
- `estimate_s`：仅 S45 以三位小数印为"0.216 / 0.333"
  （`SUPP_finemap_decomposition.md` 第 62 行）——**两个版本在三位小数上完全相同**。

**这正是 §1 第 1 条事先列明的浮点差异**（BLAS/编译器/libm 实现不同，
末位可变），量级与预期一致，且在本文任何印出精度上不可见。

⚠ **另有 6 张表**（`140a`、`140d`、`101a`–`101d`）在阶段 4 的**字节级**比对中
被判定为"不同"，但逐格比对显示 **0 个格子有内容差异**。核实原因：
基线快照来自加 `.gitattributes` **之前**未被规范化的 Windows CRLF 版本，
容器写的是 LF；`git add` 时 git 自身的规范化逻辑确认这些文件与已存的
LF 版本完全一致（`git status` 未把它们列为改动）。**这不是差异，
是 `.gitattributes` 修复生效的副作用**，已在提交信息中如实注明，
不计入"动了的数"。

### 9.3 层二：翻转的判定（逐条）

**无。** 逐项核对：

- `147a` 的 `seed_stable`、`verdict`、`converged_L10`、`converged_L20`、
  `n_cs_across_seeds`、`stable_in_L` 六列，**14 行全部字节相同**——
  `159e_table_diff.tsv` 里这些列一次都没出现；
- `101b_matched.tsv` 的 `criterion_met` 未变（该表整体字节比对判定为"不同"，
  但如 9.2 所述是纯换行符问题，内容零差异）；
- `130c_multilist_verdict.tsv`、`123d_fixed_anchor_full_grid.tsv`、
  `126a_offgrid_attribution.tsv`、`85e_matched_background_fixed_anchor.tsv`
  四张判定表**根本不在"9 张不同"名单里**——阶段 4 判定它们与基线完全一致。

### 9.4 无法在容器内重跑的部分，及其在论文中的标注方式

**必跑清单（§3 第 1–6 组）覆盖率：6/7 项执行，1 项因外部数据缺失未能重跑**——

- `step158`（S53 可见性重叠）：需要 S53 预注册里明确说明的外部下载文件
  （GSE314342 的 bulk RNA-seq），按 `DEPOSIT.md` 的既定政策**不随仓库分发**。
  这**不是容器缺陷**，是该分析从一开始就依赖仓库之外的数据；
  `step159` 正确地把它标记为 `not rerunnable` 而非 `FAIL`。

**Group 7（清单之外，能跑多少跑多少）覆盖率**：18 个额外脚本尝试重跑，
15 个成功、3 个因外部数据缺失（`step120`/`121`/`122`，指向仅存在于作者机的
绝对路径与未随仓库分发的 `landi2020_known_loci_grch38.csv`）标记为
`not rerunnable`。另有 `step143`（S39 名单出处稽核）在**第一次**运行中
成功执行但产出了 68 行"差异"——核实后确认是该脚本**按设计**查询的
GWAS Catalog 实时接口在两周间自然增长所致，与容器无关（详见 `step159`
本轮 commit 说明），现已从 `step159` 的重跑清单中排除，不再计入本记录。

⚠⚠ **更正（2026-09-08）——上面这两段用错了分母，见 §10.2。**

此处原写"仓库自带全部输入的必跑步骤，**重跑成功率 100%**……远高于 §8 停止规则"。
**100% 是按"必跑清单上的脚本数"算的，而 §8 的止损线用的分母是"正文所引数字"。**
按正确分母重算：`step127` 对账 **41 个数**，第一次运行中
**只有 23 个（56%）**出自容器重算的表；其余 18 个所依据的
`123d` / `126a` / `126c` / `130c` / `85e` **从未被容器算过**，
因为第 7 组的实现只匹配 Python 脚本而它们的产出脚本是 R。

**56% 仍在 50% 的退回线之上，但只高 6 个百分点，不是"远高于"。**
原句是一个**对本项目有利方向的错误陈述**，按纪律保留原文并在此更正。
第 7 组已修（§10.2），补跑结果见 §9.7。

### 9.5 §7 预期的对错

| 预期 | 结果 |
|---|---|
| 绝大多数数不会在印出精度上变 | ✅ **完全确认**——无一个印出的数字变动 |
| `step147` 最可能动、预期至少一格 `seed_stable` 翻转 | **一半对**：`estimate_s`（`step147` 的连续统计量）确实是唯一出现两位数以上格子差异的表，方向猜对了；但 `seed_stable` **一格都没翻转**——比预期更稳定 |
| 计数类应完全相同；若不同优先怀疑排序而非浮点 | ✅ 未观察到任何计数类差异，此条未被检验到需要动用 |
| `step101` 的 PC 预期仍通过 | ✅ 通过（`101a`–`101d` 内容零差异，含 PC 依赖的全部数据） |
| `step151`/`156`/`158` 应完全不动 | `step151`、`156` ✅ 确认不动；`step158` 无法重跑，此条**未被检验**而非被驳回 |

**§7 明确写了"若结果与预期相反须记'未预见'"**：`seed_stable` 比预期
更稳定，不是"未预见的方向"，是"预见的量确实动了、但预见的翻转没发生"——
按 §7 的字面要求，这**不需要**记为未预见，但如实记在此处以示区分。

### 9.6 终局：容器作准绳，还是退回 (a)

**⚠ 本节留白，按 §4"全有或全无"条款，由作者（人）判断后填写，
本文件的执行者不得代为决定。**

基于 9.1–9.5 的事实：容器产生的表在正文与全部补充材料的**任何印出精度**上
均未改变任何数字；无任何判定（显著性、阴性对照、void、`seed_stable`）翻转；
仓库自带输入的必跑步骤重跑成功率 100%。若采纳为准绳，
§5 的迁移动作（登记 `NUMBER_MIGRATION_fixed_anchor.md`、改表、重跑四审计）
**理论上不需要改动任何正文数字**——但决定本身、以及是否要为此重写
`CONTAINER.md` §5 与已送出的 `REVIEW_BRIEF_v14.md`，仍需你的判断。

---

## 10. 修订记录（**执行前**，§1–§8 未改一字）

### 10.1 2026-09-07：执行顺序有逻辑缺陷，已更正

**发现者**：第三方复核。**发现时点**：容器尚未构建，**未观察到任何重算结果**。

**缺陷**：§3 的必跑清单把"四个审计"排在第 1 位、把重跑分析排在第 3–7 位。
按这个顺序，`step127` 审计的是**挂载进容器的旧 Windows 表**，
而不是随后由容器产生的表——**它证明不了"容器结果与正文一致"**，
只证明了挂载进来的仓库没坏。

**更正后的执行顺序**（`step159` 已按此重写）：

| 阶段 | 内容 | 性质 |
|---|---|---|
| **0** | 把挂载进来的结果表快照为 `159d_baseline/`，**只读基线** | 参照物 |
| **1**（闸门 A）| 包测试 `testthat` + 四个审计 | **仅证明挂载完好**，不是验收 |
| **2** | 重跑全部可跑的分析（含 §3 第 7 组"输入齐备的其余步骤"） | 产生容器表 |
| **3**（★ 验收）| **再次**跑四个审计——此时对的是**容器产生的表** | **这才是层一判据** |
| **4** | 容器表 ↔ `159d_baseline/` 逐文件逐格比对 | 列出动了的数 |

**§3 清单的内容一字未改**，改的只是**执行顺序**，以及把原先未实现的第 7 组补上。
§2 的两层判据、§4 的全有或全无、§5–§8 全部不变。

⚠ **为什么允许在此时改**：本项目对预注册的约束是"看到结果之后不得改"。
本次更正发生在**镜像构建之前**，无任何结果可看；且更正的方向是**收紧**
（原顺序根本证明不了要证明的事）。按 §五-56 的同类先例，如实登记而非追溯掩盖。

### 10.2 2026-09-08：第 7 组的实现漏掉了全部 R 脚本——⚠ **本条发生在结果已被观察之后**

**发现者**：作者追问 §9.6 该怎么判时，执行者回头核对覆盖率的分母。
**发现时点**：⚠ **第一次验收运行已经跑完，结果已被观察。**
这一点必须写在最前面，本条与 §10.1 性质不同，不得混为一谈。

**缺陷**：§3 第 7 组写的是"其余输入齐备的 `step*`"，
而 `step159` 的实现是 `glob("step1[2-5][0-9]_*.py")`——**只匹配 Python，且只匹配 120–159**。
两处收窄同时生效，后果是：

`step127` 对账正文的 **41 个数**里，有 **18 个**所依据的表**从未被容器重算过**——
它们的产出脚本是 R（`step124_full_grid.R` → `123d`；
`step126_recompute_on_fixed_anchor.R` → `126a`/`126c`；
`step130_multilist_control.R` → `130c`），
或编号落在区间外（`step85e_matched_background.py` → `85e`）。
涉及双轴网格、置换标定、匹配背景对照与整个 S39 多名单对照。

**§9.4 曾据此写"重跑成功率 100%"，那是错的分母。**
100% 是按"必跑清单上的脚本数"算的；而 §8 的止损线用的分母是
"**正文所引数字**"。按正确分母，第一次运行的覆盖率是
**23/41 = 56%**——高于 50% 的退回线，但只高 6 个百分点，
而非先前记录的"远高于"。§9.4 已据此更正。

**更正**：第 7 组改为同时匹配 `.py` 与 `.R`，R 脚本以 `Rscript` 调用，
并按名字补入 `step127` 所读表的产出脚本。同时排除三类：
已在必跑清单中跑过的 `step140`/`141`/`147`（避免重复执行）、
以及 ⚠ `step150_environment_lock.R`——**它会重新生成 `150a_environment.lock`**，
即作者机的包版本记录，跑一次就会被容器版本静默覆盖，
而 §1 的全部比较正是以那份记录为基准。

**基线保护**：阶段 0 原本在 `159d_baseline/` 已存在时直接中止。
补跑必须与**原始 Windows 表**比对，而此刻工作树里已经是容器产出，
重新快照会把比较变成"容器 vs 容器"、报出零差异。
故新增 `--reuse-baseline`：显式、留痕、非默认，复用已提交的原始基线。

⚠ **为什么允许在结果已被观察之后改**：§8 禁止的是
"看到结果后更换**镜像基底、包快照或容差**"——三者一个都没动，
判据、容差、镜像全部原样。本次更正**扩大**了被检验的范围，
只会更难通过，**在结构上无法用于把结果引向"一致"**。
但它终究是事后修正，故如实记于此，**不假装是事前设计**，
且第一次运行的 56% 覆盖率与本次结果一并保留，供复核者自行判断。
