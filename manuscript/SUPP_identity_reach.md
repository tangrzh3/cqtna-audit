# S51：恒等式在被审计文献里覆盖多大范围——一次失败的测量，与替代它的设计

**编号**：S51　**日期**：2026-08-25，**2026-08-26 大幅改写**
**脚本**：`step151_instrument_count_in_literature.py`（词组扫描）·
`step156_estimator_provenance_coding.py`（C6 人工编码）
**输出**：`151a_estimator_usage.tsv`、`151b_console.log`、
`156a`/`156b`（盲编码文件）、`156c`–`156e`（一致性与裁定）
**语料**：与 S23 同一批缓存全文，共 **154** 篇

---

## 0. ⚠ 撤回声明（本节最重要）

本文件 2026-08-25 的第一版写着：

> 71 篇（46%）是恒等式**覆盖面的下界**，130 篇（84%）是**逃脱者的上界**。

**这两句都是错的，现予撤回。** 它们不是"过于自信的表述"，而是**把一个方向未知的
词组命中数说成了有向的界**。第三方复核指出了这一点，核查后问题比复核者说的更重。

---

## 1. 为什么错

`step151` 的 `single` 正则在**全文任意位置**匹配。它不要求该词组：

- 与 `cis-eQTL` 出现在同一句或同一段；
- 描述的是**主要候选名单**；
- 描述的是 **Wald 估计**；
- 属于作者正在做的那一支 cis 提名分析。

**复核者点名的反例成立**：`PMC11187347` 的记录是 `cis=0, single=1`
——一篇**通篇不提 cis-eQTL** 的论文，被计进了"声明单变异 cis 工具"。

### 命中数在最低限度的邻近性要求下崩塌

| 口径 | 篇数 |
|---|---:|
| 词组出现在全文任意位置 | **71** |
| ……其中**从不提及 cis-eQTL** | **23** |
| 词组与 cis 提及相距 500 字符以内 | **13** |
| 词组与 cis 提及**同句** | **10** |

**71 → 10，七倍。** 一个在如此弱的约束下就掉掉七分之六的计数，
不是下界也不是上界，而是**方向未知的词组命中数**：

- 它可能**虚高**——"single-cell"、"single-variant analysis"等无关表述会命中；
- 它也可能**虚低**——每基因工具数只写在补充表里的论文完全看不见。

同理，"130 篇点名多工具估计量"与"其中 65 篇也命中 single"**说明不了**
这些论文的 cis 候选名单由哪种估计量产生。点名 IVW 可以只是因为在 trans 上跑了 IVW。

## 2. ⚠ 现在能说什么

**只有一句**：这个语料里，估计量的**措辞**分布如下（`151a`），
且这组数只作为**语料描述**，不作为任何比例的估计。

| 估计量（全文任意位置点名） | 篇数 / 154 |
|---|---:|
| IVW | 129 |
| MR-Egger | 111 |
| weighted median | 104 |
| mode-based | 73 |
| Wald ratio | 68 |

**恒等式在这个文献里覆盖多大范围，目前是未测量的。**
正文按此措辞，不再声称任何界。

## 3. 替代设计：C6

要回答的量只有一个：

> **这篇论文的主要 cis 提名名单，是由哪种估计量产出的？**

它无法由词组匹配得到，只能逐篇读方法与补充表。设计如下（规则见
`156_C6_CODING_RULES.txt`，**写于任何论文被读之前**）：

| 项 | 取值 |
|---|---|
| 样本 | **与 C1/C3 同一批固定种子 46 篇子样本**（不另抽，便于与审计其余判据对照）|
| 分类 | `W` 单变异 Wald · `M` 多工具估计量 · `X` 混合流程 · `U` 无法确定 |
| 编码者 | 两人**独立**，互不可见，且**都看不到 `step151` 的自动结果** |
| 盲化 | 两份文件用不同种子打乱行序（1561 / 1562）|
| 证据 | 每一条非 `U` 判定**必须**附上所依据的原句与其位置；无引文者在裁定时按 `U` 处理 |
| 报告 | 原始一致率、**裁定前** Cohen's κ、裁定后分布、`W` 在可确定集（W+M+X）中的 Wilson 区间 |
| `U` | **单独报告，不做再分配**——报告缺失本身就是关于这个文献的发现 |

⚠ **κ 在裁定前计算**，且与原始一致率并列报告：四分类且很可能有一个占优类别，
κ 会因流行度效应塌向 0 而一致率仍很高——C1 已经发生过一次这种情形（S23）。

⚠ **`U` 是真答案不是失败。** 规则明写：不得从期刊、年份、作者或论文"看起来
是否高级"去猜。

### 结果（2026-09-07）

**编码质量**：两份各 46 行全部填满，**非 U 判断无一缺引文或出处**。
独立性经核查成立：引文完全相同的仅 3 篇（7%），**出处字符串零重合**
（A 有 40 种写法，B 有 46 种），引文平均长度亦不同。

**一致性（裁定前，按登记）**

| | |
|---|---:|
| 双编码论文 | 46 |
| 原始一致率 | **71.7%** |
| 期望一致率 | 0.393 |
| Cohen's κ | **0.534** |

κ 与原始一致率并排报，理由同 C1：四分类且有一类占优时，κ 会因流行度而偏低。
**κ 定义在裁定前，裁定不改变它。**

分歧 13 篇，构成为 M/X 4、M/W 4、W/X 2、涉及 U 者 3。
**全部由两位编码者共同裁定**，逐篇写明理由，多数援引补充材料里
带每基因 SNP 数的工具表——正是规则判定顺序里排第一的那类证据。

⚠ 有 1 篇（PMC13448146）被裁成**两位编码者都没提过的第三个码**：
A 判 W、B 判 M，共同裁为 **X**，理由是同一候选名单同时由单 SNP SMR 与
多 SNP SMR-multi 承载、且未指定何者为主。规则要求共同裁定，
**并未限制只能在两人所提的码中选**，故此裁定合规；因其少见而记录在此。

**最终分布（裁定后）**

| 码 | 含义 | 篇数 |
|---|---|---:|
| **W** | 单变异 Wald | **10** |
| **M** | 多工具估计量 | **28** |
| **X** | 混合流程 | **7** |
| **U** | 不可判定 | **1** |

**可判定 45 篇中，W 占 22.2%，Wilson 95% CI [12.5, 36.3]。**
U **单独报告、不重新分配**——论文不写本身就是关于这个领域报告习惯的发现。

### ⚠ 裁定前就钉死的边界（未登记，故意前置）

在两位编码者提交裁定**之前**，我们把 13 篇所有极端裁定下的结果算出并提交入库
（`156f_C6_bounds.tsv`）：W 占比在 **17.4%–45.7%** 之间；
若逐篇采用 A 自己的判断为 24.4%，采用 B 的为 27.3%。

**这不在 `156_C6_CODING_RULES.txt` 的报告清单里，是有意增补的敏感性分析。**
之所以放在裁定之前而不是之后：把两端先钉在记录上，
**裁定就无法被"知道数字该往哪走"牵着走**；而两位编码者在完成裁定前未看到该表。

最终值 22.2% 落在该区间内，且贴近两位编码者各自的解法——这正是这张表存在的意义。

### 这个数意味着什么，以及**不**意味着什么

**意味着**：本文分析的那个设计——单变异 Wald cis-MR——**是当前实践中的少数**。
若把混合流程整体计入（其 cis 名单有一部分由单变异承载），上限为 37.8%。

**不意味着**：恒等式对多工具流程也成立。**我们没有测过**，也不主张
"工具变多时论证会优雅地衰减"。M 类占 28 篇，本文对它们另有一条独立批评
（r² < 0.1 的 clumping 保留相关性而 IVW 假定独立，见 Methods），
**那是另一回事，不得与恒等式混为一谈**。

⚠ 这个结果**比我们此前任何一种含糊说法都对本文不利**。
这正是把它测出来而不是断言它的理由。

## 4. 这一节留给读者的教训

我们对文献提出的其中一条要求，是**别把自动匹配的命中数当成实践的测量**
（S23 §1 早已写死这条边界，并因此对 C1/C3 做了 precision 校正）。
**然后我们在 S51 上违反了自己这条规则**，而且是在论文最关键的一处主张上。

抓到它的是第三方复核，不是我们的任何一个审计脚本——
`step127` 只查数字，而这里的每个数字都对得上表，
**错的是那些数字被称作什么**。

## 5. 复现

```
python step151_instrument_count_in_literature.py <repo>   # 语料描述，约 1 秒
python step156_estimator_provenance_coding.py <repo> prepare
python step156_estimator_provenance_coding.py <repo> score   # 编码填完后
```
