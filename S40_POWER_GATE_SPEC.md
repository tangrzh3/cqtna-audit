# S40 §7 功效闸门：实现说明

**编号**：无（**这不是一份预注册**）
**日期**：2026-08-21
**规则来源**：`EXTERNAL_VALIDATION_PROTOCOL_v9_3.md` §7，已由 S40 全文采纳
**实现**：`step132_external_power_gate.R` → `132a_power_gate_curve.tsv`、`132b_cohort_screen.tsv`

---

## 0. 这份文件是什么

§7 的**判据是别人写的、且已经冻结**：

> 在 0.50 effect-retention 情形下，至少 80% 的模拟产生 ≥8 个显著 bounded locus
> **且**通过主 Fisher 终点，该队列才有资格做确证性运行；否则只能标为 pilot。

本文件只记录**执行那条判据时必须自己定下的实现细节**——§7 没有写、
但不写下来就无法复现的那些。**它不新增任何判据，也不改动任何阈值。**

⚠ 本文件写于任何外部关联统计到手之前，且**不产生"事前"地位**：
真正的事前地位要等公开注册取得 DOI（S40 §3）。

---

## 1. 统计模型

冻结记录族是 melanoma × Soskic_CD4 的 3,556 条记录（`13_meta_locus_annotation.tsv`）。

该表里 `se = se_outcome / |beta_exposure|` **精确成立**（无 delta 法二阶项，
已核验最大偏差 0），因此 Wald 检验的 z **恒等于 outcome 侧的 z**：

    z_MR = b/se = beta_outcome / se_outcome

外部队列只换 outcome，暴露侧工具冻结不变。由 §7 的

    se_ext = se_disc × sqrt(N_eff,disc / N_eff,ext)

得到，令 R = N_eff,ext / N_eff,disc、f 为 effect-retention factor，

    z_ext ~ Normal( f · z_disc · sqrt(R),  1 )

`N_eff,disc = 49,337`（12,530 例 / 789,099 对照，正文 Methods）。

### 1.1 抽样单位是**变异**，不是记录

3,556 条记录只来自 **2,126 个不同变异**：同一变异在不同 gene × profile 下重复出现，
**outcome 侧的 beta/se 完全相同**。因此每次重复**按变异抽一次 z**，再广播回它的全部记录。
按记录独立抽样会凭空造出重复信息，把功效抬高。

### 1.2 未建模的相关性（**方向已知**）

同一位点内不同变异之间存在 LD，其 z 在真实外部队列里是相关的；本实现按变异独立抽样。
独立抽样会让"至少一条记录显著"更容易在多个位点同时发生，
**因此本实现给出的功效是上界，所需 N_eff 是下界。**

---

## 2. 每次重复走的流程

1. **harmonisation coverage**：以概率 `coverage` 独立保留每个变异；
   丢弃的变异连同其全部记录退出该次重复。见 §2.1。
2. 由 z_ext 得 p = 2Φ(−|z|)，广播到记录层。
3. **BH** 跨该次重复保留下来的整个记录族，阈值 0.05。
4. **位点形成**：`fixed_centre`、`locus_kb = 1000`。位置不变，故位点划分恒定。
5. **归属**：C1 `any_record`、`known_kb = 1000`、Landi 参照名单。
   `any_record` 下，位点的 known 状态由**该位点的全部记录**决定，与显著与否无关，
   因此 known 向量恒定；背景恒为 **655 个位点、66 个 known**。
6. **单调计数**：ST = 含 ≥1 条显著记录的位点数，SK = 其中 known 的个数。
7. **单侧 Fisher**（greater），fold = (SK/ST)/(BK/BT)。

### 2.1 覆盖率把主曲线和覆盖率检查分成了两条路径

第 4–7 步在**全覆盖**下可以走一条快速路径：位置不变 ⇒ 位点划分恒定 ⇒
`any_record` 的 known 向量恒定 ⇒ 背景恒为 655/66，每次重复只需重算
"哪些位点含显著记录"。

**但覆盖率 < 1 时这条捷径不成立**：丢掉变异会让 `fixed_centre` 在保留集上
**重新锚定**，位点划分和背景本身随之改变。这一点是被等价性测试抓出来的，
不是事先想到的。因此：

| | coverage | 路径 | 重复数 | 用途 |
|---|---|---|---|---|
| **主曲线** | 1.00 | 快速路径 | 10,000 | R × f 全网格 |
| **覆盖率检查** | 0.95（§2.4 合格下限）| 包的完整路径 | 2,000 | 仅 f = 0.50，仅阈值附近的 R |

⚠ 主曲线取全覆盖，是**反保守**的一侧（不损失任何记录）。
覆盖率检查用来量这一侧有多大；它重复数少，因为每次重复都要重算分区。

### 2.2 两条测试

`step132` 内含两条测试，**不过则脚本停止**：

1. **等价性**：25 次随机 p 向量下，快速路径与
   `as_cqtna_mr()` + `cqtna_attribution()` 的 ST、SK、背景与 Fisher P 必须逐项相同；
2. **行序不变性**（v10 §四-53）：把记录族整体倒序输入，判定必须不变。

## 3. 判定

    pass_rep = (ST >= 8) AND (fold > 1) AND (fisher_p < 0.05)
    qualifies = mean(pass_rep) >= 0.80        # 在 f = 0.50 下

### 3.1 只模拟了主终点，没模拟另外两道闸门

§7 说模拟应包含"all primary gates"。本实现只模拟了 §6 的第 2、3 条
（≥8 个显著位点、单侧 Fisher）。**没有**模拟 §5.1 的四名单 specificity gate、
也没有模拟 §5.2 的 density gate——两者都只会**降低**通过率。
**所以本实现报出的 N_eff 门槛同样是下界。** 这一条必须与结论一起说。

## 4. 扫描网格与重复数

- N_eff 网格：50k 起，按 §7 的 R = N_eff,ext/N_eff,disc 取
  R ∈ {0.5, 1, 1.5, 2, 3, 4, 5, 6, 8, 10, 12, 16}
- f ∈ {0.50（判定用）, 0.75, 1.00}
- 主曲线每格 10,000 次重复；覆盖率检查每格 2,000 次；`set.seed(1)`（与 S38 的 seed 约定一致）
- 抽样顺序：变异按 `chr:pos` 数值序规范排序后再抽（v10 §四-54：
  `tapply`/`split` 的分组顺序随 locale 变，位点键必须解析成数值再排）
