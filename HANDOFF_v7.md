# 项目交接文档 v7

**项目**：CD4⁺T 细胞活化动态 eQTL → 靶点提名的审计
**最后更新**：2026-08-17（Step 121 后，HANDOFF v6 §九 的 1–4 项全部完成）
**取代**：`HANDOFF_v6.md`（其 §四强项、§五技术坑、§六方法论纪律仍有效，本文件继承并扩充）
**本轮 commit**：`46ef2a8` → `cf9e0c5`，共 **12 个**，工作树干净

> **新会话阅读顺序**
> 1. 本文件（尤其 §三：还剩什么）
> 2. `manuscript/FIGURES_GB_mapping.md`（**九图对照表 + 本轮查出的正文问题**）
> 3. `manuscript/MANUSCRIPT_GB.md`（**投稿母稿**）
> 4. 全文源 `manuscript/MANUSCRIPT_v2_dual_thread.md`（**唯一正文源**）
> 5. `NEXT_PROJECT_myeloid.md`（本篇之后的方向，已定稿）

---

## 一、当前定位（不变）

**单变异 eQTL-MR 产出的是"在这份数据条件下通过筛选的候选"，不是稳定靶点；
其可复现的部分由已知结局位点、阈值邻近性与基因/区室错归属决定。**
边界：只在两个暴露资源上成立（Soskic CD4 时程、eQTLGen 全血）。

本轮**没有改变任何结论**，但把结论的图件与数字来源补齐了，并改正了三处正文错误。

---

## 二、本窗口做了什么（Step 119–121 + 九图）

### A. GB 九图**全部建成**，一条命令可重现

```
python figures/make_gb_all.py
```
→ `Fig1`–`Fig9` 各一份 PDF + PNG，结尾自动核对文件是否齐全。
逐图对照、数据源、口径见 `manuscript/FIGURES_GB_mapping.md`。

审计结论：缺口比 v6 估计的大。**不只是"两张图没文件"**——
Fig 1/3/4 各缺一个面板，Fig 6/7/9 要从 `Fig9_part2`、`FigS7` 里拆出来重组，
Fig 8 只需改名。v6 的判断（"图件本身没按九图口径导出"）成立。

⚠ **旧的八图方案（`FigA`–`FigG`、`Fig5_compartment`、`Fig9_part2` 等）全部保留**，
两套并存互不覆盖。Fig 8 是直接复用 `make_fig5_compartment` 的 figure 对象另存，
所以两套编号不会各画各的而悄悄漂移。

### B. 三个补溯源的 step（都属"数字对但没脚本"这一类）

| | 问题 | 结果 |
|---|---|---|
| **step119** | `94d_grid.tsv` 混着预注册判为作废的 lung/colorectal 两行且**无标记**；`94e` 汇总的是旧网格；两个错配对照**无表无脚本** | 重建为 `119a`（带 `status` 列：main/sensitivity/**void_bad_coords**+作废理由）、`119b`、`119c`。两个错配对照**逐位重现**（1.30× P=0.4109、0.00× P=1.0） |
| **step120** | `make_fig9_part2.py` 面板 a 的八组 β/SE/P 是**硬编码**，注称 "Step 62" 但仓库里**没有 step62 脚本也没有表** | 从八个 profile 的 parquet 重算 → `120a`，**八个全部逐位重现** |
| **step121** | v5/v6 都欠的 1 Mb 窗口敏感性 | `121a`/`121b`/`121c`，见 §二-D |

### C. ★ 查出并改掉的三处正文错误（都不是审稿人提的）

1. **"十一个生物学模块无一超过组成匹配零模型"——假的。**
   `43c` 说 11 个里**有 2 个超过**：OXPHOS（|SMD| 0.2125）与 Proliferation（0.1180），
   阈值 = 50 个随机模块 |SMD| 的 95 分位 **0.0720**（按 `43b` 重算逐行一致）。
   而且 **Proliferation 是 `step43` 里预先写死的阳性对照**——脚本明写
   "若阳性对照不超过零模型，则该检验功效不足、零模型无信息"，
   **它超过是检验成立的必要条件**。
   `make_fig9_part2.py` 面板 d 的注释错得更露骨，渲染出的字面是
   "all 11 biological modules within the matched null (**9/11**)"。
   → GB 正文、v2 图注、图脚本**均已改**。改对后**比原句更强**：
   贴在零模型上的是 **Activation 模块自己**（SMD = −0.003），这才是
   "该轴不是活化强度换个说法"的直接证据；超过的两个是阳性对照与 OXPHOS，
   后者与"合成代谢状态"一致而非相左。

2. **`+0.56 versus +0.74` 是两个 signature 混用。**
   同病种队列 `79a`：`locked16` = 0.5548、`locked15_noTPI1` = 0.5648；
   发现队列 `74a` 的 +0.74 是 `locked16`。而
   `PREREG_pozniak_replication.md` **H1（主）明写是锁定 16 基因**。
   → v2 与 NC 已统一到主口径：**+0.55 对 +0.74**（含 "+0.56 到 +0.65" 那处改为 "+0.55 到 +0.64"）。

3. **Okada 2014 被引用三次却不在编号表里。**
   RA 是本文唯一的非癌结局，其已知位点名单的来源必须可引。
   按 ORCS 同一套纪律核实（GWAS Catalog REST `GCST002318` → PMID 24390342 →
   NCBI esummary）：*Nature* 2014;506(7488):376–381，doi:10.1038/nature12873，97 位作者。
   → **追加为条目 47**（不插队，见 §三-A2），GB 2 处、NC 1 处已加 `[47]`。

### D. 1 Mb 窗口敏感性（step121）——**结果对本文有利**

把两个被混称为"1 Mb 窗口"的约定分开扫：

- **KNOWN_KB 收紧 → 倍数上升**：melanoma × Soskic **4.09 → 18.26×**（P 0.028 → 3.4e-4）；
  eQTLGen 在 4.4 附近平稳。**若 1 Mb 是挑出来的，收紧应当削弱结果，实际相反。**
- **LOCUS_KB 收紧 → 倍数也上升**：eQTLGen **4.44 → 8.02×**。
  **报 1 Mb 是在压低自己的效应量。**
- **连续距离是双峰的**：melanoma × Soskic 的显著位点在 `5 · 11 · 74` kb，
  然后直接跳到 `2,167` kb——**1 Mb 这条线落在一段空隙里**；
  eQTLGen 30 个里 14 个在 50 kb 内、一个是 0 bp，背景中位数 10,360 kb。
- ⚠ **唯一例外：HCC-high**。它仅有的那个已知显著位点在 **408 kb** 外，
  故窗口 < 500 kb 时该格为 **0/2、倍数 0.00**。它本就是六格中未达 P<0.05 的两格之一。

四格的 1000 kb 行**逐位重现已发表倍数**，脚本内置该核对并在对不上时报错退出。

---

## 三、★ 还剩什么

### A. 投稿前必须做

1. **NC 稿要补 Methods 章节。** 现在 NC 只有 Abstract/Introduction/Results/
   Discussion/Display items/References。本轮把 NC 正文引用从 **2 条提到 16 条**
   （全部挂在 NC 本来就有的论断上，只多 35 词），但**仍有 31 条未引**，
   因为它们全是数据集与软件类引用，GB 放在 Methods 里，而 NC 没有 Methods。
   → **正确的修法是写 Methods，不是继续往 Results 里塞引用。**
   NC 格式本来就要求 Methods。详见 `REFERENCES.md` 末节。
2. **GB 的 7 条未引条目**（17、18、32、36、37、38、39）如何处置。
   三条路（删除／挂到 Methods 已有的 Supplementary 指针上／另立 Supplementary
   references）各有代价，**编号已因插入 ORCS 与 Okada 动过两次，再动风险高**；
   建议**留到排版阶段一次性重排**。见 `REFERENCES.md`。
3. **条目 39（DepMap）的 release 号——原待办做不成。**
   仓库里**没有任何使用 DepMap 的脚本或数据文件**，
   "TPI1 is DepMap-essential" 与 "22Q2" 来自一次未留痕的门户手查。
   三条替代路径见 `REFERENCES.md`。⚠ GB 正文从未出现 "DepMap"，只影响全文源。
4. **决定要不要把 §二-D 的窗口敏感性写进正文**（`SUPP_window_sensitivity.md` §4
   已备好一段可直接用的英文）。⚠ GB 正文贴着 8,000 词上限，加则须减。

### B. 应该做但不紧急

- **RA 两格的窗口敏感性**：`step108` 的 Okada 坐标是边扫 sumstats 边解、没落盘，
  RA 也没有 per-record 注释表，补它要重跑那一遍全量扫描。设计现成（step121 直接扩）。
- **跨格综合统计**：S35 §3.1 已预注册 CMH + 随机效应 meta + I²，
  因合格格子只有一个而无法运行。若日后有新格子，设计现成。

### C. 若要再冲一次分数

**C2（多信号/条件化 coloc）仍是唯一没试过的，判断不变：
没有新的 LD 来源就不要开工。** 自跑 SuSiE 只有 proxy LD，
在 MC1R 上已证明会过度拆分（19 vs 3，`Fig4_coloc_heidi` panel b 现在把这条画在脸上），
而 FinnGen 的 in-sample credible set 到不了候选位点。

---

## 四、审稿人认可的强项（不要在压缩中砍掉）

- **MC1R 自我校准**：框架在最明确的位点都可能不命名 MC1R
- 候选名单完全翻转、known-vs-novel 恢复率差异
- 预注册 + 错误披露 + 多层反证
- RA 排除 MHC 后仍稳健
- **（新）窗口敏感性**：两个 1 Mb 约定都是保守选择，收紧只会让结果更强

---

## 五、技术坑（v6 第五节之外的新增）

36. **★ `manuscript/build_gb.py` 会覆盖投稿母稿，而它的锚点早已失效。**
    `MANUSCRIPT_GB.md` 在第四轮审稿后被手工大改（补 Methods 与 References、
    摘要压到 382 词、九图目录、三幕重排），脚本的 `CUTS` 锚点没跟着更新。
    跑一次会打出一串 `[anchor missing]`，产出 **14,259 词**正文覆盖掉 **7,886 词**的母稿。
    **本轮已改为默认写 `MANUSCRIPT_GB_regenerated.md`**，要覆盖须显式 `--overwrite`。
    （是我跑了一次才发现的，母稿已从 git 恢复。）
37. **`assemble.py` 是安全的**，与 `build_gb.py` 不同：它确实与源同步，
    改完 v2 跑一次，diff 只有你自己的改动。**改 v2 后记得跑。**
38. **引用审计的正则必须处理 en dash 范围 `[1–3]`。**
    只匹配逗号会漏判——本轮就是因此把"7 条未引"错记成事实，实为 8 条，
    且漏掉了 `[33]` 其实是**该引未引**而非多余。
39. **`git add -A` 会扫进别的会话正在写的文件。**
    本轮把另一个会话的 `findings.md`/`progress.md`/`task_plan.md` 提交了又撤回。
    **在这个仓库里用 `git add <具体路径>`。**
40. **Windows GBK 控制台**（v6 坑 #34 的通用修法）：脚本开头加
    `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`，
    否则 `⚠`、`→` 之类字符会让 print 在**跑完之后**崩掉，看着像分析失败。

---

## 六、方法论纪律（v6 第六节之外的新增）

31. **★ "画图"是最有效的正文审计手段。** 本轮三处正文错误
    （零模型、signature 混用、Okada 缺引）**全部**是在为某个面板找数据源时撞出来的。
    因为画图逼你回答"这个数到底出自哪张表的哪一行"，而写字不逼。
32. **图上的数字必须和它的判据用同一套记录算。**
    `121c` 初版对位点内**全部**记录取最小距离，而二分类只看**显著**记录，
    于是 HCC-high 报出 235 kb 却在 250 kb 阈值下判 0/2，自相矛盾。
    **两个量只要会被并排读，就必须同源。**
33. **阳性对照"超标"不是异常，是它该干的事。**
    差点把 Proliferation 超过零模型读成反例——它是预设阳性对照，
    不超过才是检验作废。**读判据前先读脚本里那段 if。**
34. **数字对 ≠ 可溯源。** 本轮三次重算（119c、120a、121 的 1000 kb 行）
    **全部逐位重现**了已发表的数。重算的价值不在于抓错，
    而在于**下一次有人问"这个数怎么来的"时有脚本可跑**。
35. **做不成就写清做不成，别改题。** DepMap 那项原文是"与脚本核对"，
    而根本没有脚本——正确的交付是**说明它做不成并给出替代路径**，
    不是找个别的东西核对一下算完成。

---

## 七、文件地图（本窗口新增）

**图件脚本**：`figures/make_gb_fig1..fig9_*.py` · `figures/make_gb_all.py`（总驱动）
**图件产物**：`figures/Fig1_locus_attribution` … `Fig9_patients`（各 PDF + PNG）
**分析脚本**：`step119_grid_rebuild.py` · `step120_tpi1_window.py` · `step121_window_sensitivity.py`
**结果表**：`119a`–`119c` · `120a` · `121a`–`121c`
**文档**：`manuscript/FIGURES_GB_mapping.md`（九图对照 + 正文问题）·
`manuscript/SUPP_window_sensitivity.md`（S36）

---

## 八、当前字数

| | 词数 | 状态 |
|---|---|---|
| **GB** | 正文约 **7,904**（+18，零模型那句改对后变长） | 6,000–8,000 内 ✅，但**贴着上限** |
| NC | 正文 4,182（含新增 14 处引用标记，+35 词） | ✅ |
| assembled | ~28,124（`assemble.py` 生成，**永不手改**） | — |

⚠ GB 再加内容须同时减内容。

---

## 九、下一轮建议顺序

1. **NC 补 Methods**（§三-A1，投稿硬要求，且能一次消化 31 条未引）
2. **GB 未引条目与编号的最终处置**（§三-A2，建议留到排版）
3. **DepMap 那条选一条路走**（§三-A3）
4. 决定窗口敏感性是否进正文（§三-A4，文字已备好）
5. RA 两格的窗口敏感性（§三-B）
6. 然后再决定要不要碰 C2——**没有新 LD 来源就别开工**
